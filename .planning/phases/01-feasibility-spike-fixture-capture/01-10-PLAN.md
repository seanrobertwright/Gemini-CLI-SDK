---
phase: 01-feasibility-spike-fixture-capture
plan: 10
type: execute
wave: 4
depends_on:
  - 06
  - 07
  - 08
files_modified:
  - spec/protocol.md
  - spec/errors.md
autonomous: true
requirements:
  - PRS-09
must_haves:
  truths:
    - "spec/protocol.md is no longer a placeholder; it documents every observed event type, cites fixture filenames per normative claim, and covers the tool_use/tool_result pairing mechanism (or documents the absence of one)"
    - "spec/errors.md is no longer a placeholder; it tabulates stderr patterns from error-rate-limit.stderr.txt and error-auth.stderr.txt, maps each to a proposed typed error class, and cites the fixture filename per row"
    - "`node scripts/validate-fixtures.mjs citations` exits 0 (every normative claim in both docs cites a fixture filename)"
    - "Both docs reference the pinned version from .gemini-cli-compat as the capture baseline"
  artifacts:
    - path: "spec/protocol.md"
      provides: "Draft wire protocol documentation, one section per event type, each citing the fixture filename where it was first observed"
      min_lines: 100
      contains: "spec/fixtures/"
    - path: "spec/errors.md"
      provides: "Draft error pattern table; rows map stderr fingerprints to proposed typed errors; every row cites the fixture stderr file"
      min_lines: 60
      contains: "spec/fixtures/"
  key_links:
    - from: "spec/protocol.md"
      to: "spec/fixtures/*.ndjson"
      via: "citation-per-claim (every normative statement names the fixture that evidences it)"
      pattern: "spec/fixtures/"
    - from: "spec/errors.md"
      to: "spec/fixtures/error-rate-limit.stderr.txt + spec/fixtures/error-auth.stderr.txt"
      via: "stderr tail samples are quoted verbatim with citation"
      pattern: "error-(rate-limit|auth)\\.stderr\\.txt"
---

<objective>
Draft `spec/protocol.md` and `spec/errors.md` as normative-but-citation-grounded specification documents. Every claim about the wire protocol must cite the specific fixture file where that behavior was observed, and every error pattern must point to the captured stderr file it was derived from. These drafts are what Phase 3 (parser) and Phase 5 (error taxonomy) will translate into concrete type hierarchies — the drafts are the pre-implementation contract.

Purpose: ROADMAP.md Phase 1 success criterion #4 requires both docs to exist and cite fixture filenames as evidence for every normative claim. CONTEXT.md §"Integration Points" says Phase 5's error taxonomy YAML consumes stderr fragments captured as part of the error fixtures, and that `spec/errors.md` cites those fixtures as evidence. VALIDATION.md §"Per-Task Verification Map" specifies that the `citations` validator subcommand greps for `spec/fixtures/` references — this plan makes that check pass.

Output: Two fully drafted spec documents that replace the plan-01-01 placeholders.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md
@.planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md
@.planning/phases/01-feasibility-spike-fixture-capture/01-VALIDATION.md
@spec/feasibility.md
@spec/events.schema.json
@spec/fixtures.manifest.json
@spec/protocol.md
@spec/errors.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft spec/protocol.md from captured fixtures + events.schema.json</name>
  <files>
    spec/protocol.md
  </files>
  <read_first>
    spec/events.schema.json (plan 01-09 — the normative type list)
    spec/feasibility.md (for the pinned version and verdicts)
    .gemini-cli-compat (for the pinned version string)
    spec/fixtures/simple-text.ndjson
    spec/fixtures/tool-use-builtin.ndjson
    spec/fixtures/resume-session-turn1.ndjson
    spec/fixtures/resume-session-turn2.ndjson
    spec/fixtures/thinking.ndjson
    spec/fixtures/multimodal-image.ndjson
    spec/fixtures/multimodal-pdf.ndjson
    spec/fixtures/large-output.ndjson
    spec/fixtures/abort-midstream.ndjson
    spec/fixtures/event-unknown.ndjson
    .planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md (§"Pattern 1" for the discriminated-union rationale; §"Open Questions" #1 for the tool_use/tool_result pairing concern)
  </read_first>
  <action>
    Replace the placeholder content of `spec/protocol.md` with a full draft. Use the Write tool to overwrite the file entirely.

    **Required sections (target ~150-250 lines total):**

    1. **Preamble** — State the document is a normative draft derived from real fixtures. Cite `.gemini-cli-compat` for the pinned version. State that upstream docs (linked) enumerate type NAMES but NOT field schemas; this document fills the gap empirically. Note `additionalProperties: true` policy from `spec/events.schema.json`.

    2. **Transport** — Document how NDJSON is delivered:
       - Format: newline-delimited JSON over stdout, one event per line
       - Line terminators: LF on POSIX, CRLF on Windows; parser must tolerate both (PRS-02)
       - Line size limit: soft 1 MiB cap per PRS-01
       - Non-JSON lines: any stdout line that fails `JSON.parse` is treated as `{type: 'cli_log'}` by the parser (PRS-04)
       - Cite: `spec/fixtures/simple-text.ndjson` for the happy-path shape, `spec/fixtures/large-output.ndjson` for multi-KB streaming evidence

    3. **Per-event-type sections** — One subsection per `$defs` entry in `spec/events.schema.json`. For each type, document:
       - The `type` discriminator value (e.g., `init`, `message`, `tool_use`, `tool_result`, `error`, `result`)
       - The required + optional fields (from the schema)
       - Example JSON object (extracted from a fixture — sanitized to remove any residual host-specific paths)
       - Which fixture(s) evidence it — cite at least one `spec/fixtures/<slug>.ndjson` per type

    4. **Tool use / tool result pairing** — This is load-bearing for PRS-07. Document:
       - Whether `tool_use` events carry a stable correlation ID (e.g., `tool_use.id`) that `tool_result` events reference
       - If there's no correlation ID, document that pairing is positional (Nth tool_use pairs with Nth tool_result)
       - Cite `spec/fixtures/tool-use-builtin.ndjson` verbatim
       - If the captured fixture reveals surprises, document them (RESEARCH.md §"Open Questions" #1 flags this as empirically unresolved pre-capture)

    5. **Session resume mechanics** — Reference the feasibility verdict. Document:
       - How `session_id` appears in the `init` event
       - How turn 2's `--resume <id>` affects the init event of the second stream
       - The verdict impact on the transcript-prepend fallback (cites `spec/feasibility.md`)
       - Cite `spec/fixtures/resume-session-turn1.ndjson` and `spec/fixtures/resume-session-turn2.ndjson`

    6. **Thinking events** — Document whether extended reasoning emits `thinking`-type events in headless mode:
       - If yes: show the field shape observed in `spec/fixtures/thinking.ndjson`
       - If no: document the gap and note that Phase 3 synthesizes a `thinking` variant from structural knowledge per RESEARCH.md §"Open Questions" #4
       - Either way, cite `spec/fixtures/thinking.ndjson`

    7. **Multimodal references** — Document the `@path/to/file` syntax as observed:
       - Whether the attachment appears as an input event type or just influences the assistant response
       - Cite `spec/fixtures/multimodal-image.ndjson` and `spec/fixtures/multimodal-pdf.ndjson`

    8. **Truncation / abort semantics** — Document what the stream looks like when the child is killed mid-run:
       - Whether a partial final line is emitted
       - Whether a terminal `result` event is emitted (it should NOT be, per Phase 5's ERR-06 requirement)
       - Cite `spec/fixtures/abort-midstream.ndjson`

    9. **Unknown event handling** — Document the lenient fallback contract (PRS-03):
       - Unknown `type` values yield `{type: 'unknown', raw: <original event>}` in the parser output
       - Cite `spec/fixtures/event-unknown.ndjson` as the synthetic test case

    10. **Open questions / known gaps** — Copy forward any items from RESEARCH.md §"Open Questions" that the fixture corpus didn't resolve. Each should note which fixture (or lack of fixture) is the evidence.

    **Citation rule (enforced by validator):** Every subsection MUST contain at least one mention of `spec/fixtures/<slug>.ndjson`. The `validate-fixtures.mjs citations` subcommand greps for `spec/fixtures/` references in this file.

    **Do NOT include:**
    - Implementation code (this is a spec, not a parser)
    - References to Phase 3+ file paths that don't exist yet
    - Any unredacted host-specific content
  </action>
  <verify>
    <automated>test -f spec/protocol.md && [ $(wc -l < spec/protocol.md) -ge 100 ] && ! grep -q "Drafted in Phase 1 from captured fixtures" spec/protocol.md && grep -c "spec/fixtures/" spec/protocol.md | awk '$1<5 {exit 1}' && grep -qi "simple-text" spec/protocol.md && grep -qi "tool-use-builtin" spec/protocol.md && grep -qi "abort-midstream" spec/protocol.md && grep -qi "event-unknown" spec/protocol.md && grep -qi "resume-session" spec/protocol.md && ! grep -qE "AIzaSy[A-Za-z0-9_-]{33}" spec/protocol.md</automated>
  </verify>
  <done>
    `spec/protocol.md` is >=100 lines. It no longer contains the plan-01-01 placeholder text. It cites `spec/fixtures/` at least 5 times and mentions at least 5 distinct fixture slugs by name. It contains no API key patterns. It has sections for transport, per-event-type, tool pairing, session resume, thinking, multimodal, truncation, and unknown handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Draft spec/errors.md from captured stderr fixtures</name>
  <files>
    spec/errors.md
  </files>
  <read_first>
    spec/fixtures/error-rate-limit.ndjson
    spec/fixtures/error-rate-limit.stderr.txt
    spec/fixtures/error-rate-limit.expected.json
    spec/fixtures/error-auth.ndjson
    spec/fixtures/error-auth.stderr.txt
    spec/fixtures/error-auth.expected.json
    spec/fixtures/abort-midstream.ndjson
    spec/fixtures/abort-midstream.expected.json
    .gemini-cli-compat
    .planning/REQUIREMENTS.md (§"Error Taxonomy" — ERR-01 through ERR-07 enumerate the typed error classes Phase 5 will implement; this draft previews that taxonomy)
    .planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md (§"Open Questions" #3 — what stderr format gemini-cli emits for rate limits)
    .planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md (Integration Points — errors.md cites error fixtures)
  </read_first>
  <action>
    Replace the placeholder content of `spec/errors.md` with a full draft pattern table. Use the Write tool.

    **Required sections (target ~100-150 lines):**

    1. **Preamble** — State the doc is a draft cataloging observed error shapes, derived from Phase 1 error captures. Cite the pinned version. Note that Phase 5 translates this table into a YAML source + typed error hierarchy per REQUIREMENTS.md §"Error Taxonomy".

    2. **Classification dimensions** — Document the two signal sources:
       - Stream-json `error`-type events (per the schema)
       - Exit code + stderr tail matching
       Per ERR-05, both paths must produce the same typed error for the same underlying condition.

    3. **Observed patterns table** — A markdown table with columns:
       | Pattern label | Fixture evidence | Exit code | Stream-json signal | Stderr fingerprint | Proposed typed error | Archon retry bucket | Retryable |

       Populate rows from real captures:

       - **Auth failure**
         - Fixture: `spec/fixtures/error-auth.ndjson` + `spec/fixtures/error-auth.stderr.txt`
         - Exit code: (read from error-auth.expected.json)
         - Stream-json signal: (whether an `error` event appeared; read from error-auth.ndjson)
         - Stderr fingerprint: quote 1-2 lines verbatim from `error-auth.stderr.txt` (redacted)
         - Proposed typed error: `AuthError` with subtype `NotConfigured` or `Forbidden403`
         - Archon bucket: `auth`
         - Retryable: `false`

       - **Rate limit failure**
         - Fixture: `spec/fixtures/error-rate-limit.ndjson` + `spec/fixtures/error-rate-limit.stderr.txt`
         - Exit code: (read from error-rate-limit.expected.json)
         - Stream-json signal: (whether an `error` event appeared)
         - Stderr fingerprint: quote 1-2 lines from `error-rate-limit.stderr.txt` — look for "429", "quota", "rate", "Too Many Requests" etc. If the capture didn't trigger a clean rate-limit (see plan 01-06 task 2 fallback options), quote whatever error actually appeared and mark the row with a `NOTE:` indicating it's a speculative pattern.
         - Proposed typed error: `RateLimitError`
         - Archon bucket: `rate_limit`
         - Retryable: `true`
         - Retry-after: document whether the fixture reveals a `Retry-After` header or delay hint

       - **Subprocess crash / mid-stream abort**
         - Fixture: `spec/fixtures/abort-midstream.ndjson`
         - Exit code: (read from abort-midstream.expected.json — non-zero)
         - Stream-json signal: No terminal `result` event (this is the ERR-06 test case)
         - Stderr fingerprint: may be empty if the signal arrived before any stderr was produced
         - Proposed typed error: `ProcessError` (or `AbortError` if the SDK initiated the abort itself; for Phase 1 purposes, it's `ProcessError`)
         - Archon bucket: `crash`
         - Retryable: `false` (caller decides retry policy per REQUIREMENTS.md SDK-out-of-scope item)

    4. **Gaps and open questions** — Document error scenarios that Phase 1 did NOT capture but Phase 5 still needs:
       - Model access errors (e.g., 2.5-pro deprecated post-2026-06-17 cutoff)
       - Invalid prompt / content policy violations
       - Unsupported-feature errors
       - `InvalidPromptError` shapes
       Each should be flagged as "Phase 5 must capture additional fixtures" or "Phase 5 synthesizes from docs only".

    5. **Phase 5 handoff** — Explicit section stating that Phase 5 will:
       - Translate this table into `spec/errors.yaml` (single source)
       - Generate both `ts/src/errors.ts` and `python/src/gemini_sdk/errors.py` from that YAML
       - Build an `ErrorMapper` that pattern-matches the signals above into typed errors
       - Map each typed error to exactly one of Archon's 5 retry buckets (rate_limit, auth, model_access, crash, unknown)

    **Citation rule (enforced by validator):** Every non-preamble section must cite at least one fixture filename. The `validate-fixtures.mjs citations` subcommand greps for `spec/fixtures/` references in this file.

    **Do NOT include:**
    - Actual error class implementations (this is Phase 5's job)
    - Unredacted stderr content (all quotes must be from the already-redacted `.stderr.txt` files)
  </action>
  <verify>
    <automated>test -f spec/errors.md && [ $(wc -l < spec/errors.md) -ge 60 ] && ! grep -q "Drafted in Phase 1 from captured error fixtures" spec/errors.md && grep -c "spec/fixtures/" spec/errors.md | awk '$1<3 {exit 1}' && grep -qi "error-auth" spec/errors.md && grep -qi "error-rate-limit" spec/errors.md && grep -qi "abort-midstream" spec/errors.md && grep -qi "archon" spec/errors.md && grep -qE "rate_limit|RateLimitError" spec/errors.md && ! grep -qE "AIzaSy[A-Za-z0-9_-]{33}" spec/errors.md</automated>
  </verify>
  <done>
    `spec/errors.md` is >=60 lines. It no longer contains the placeholder text. It cites `spec/fixtures/` at least 3 times and mentions error-auth, error-rate-limit, and abort-midstream fixtures by name. It references Archon's retry buckets (rate_limit, auth, crash, etc.) and the RateLimitError class. It contains no API key patterns.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the citations validator and confirm all phase-gate checks pass</name>
  <files>
  </files>
  <read_first>
    spec/protocol.md (task 1)
    spec/errors.md (task 2)
    scripts/validate-fixtures.mjs (plan 01-02 — the `citations` subcommand)
  </read_first>
  <action>
    Run the citations validator subcommand + the full Phase 1 validation suite to confirm every phase-gate check is green:

    ```
    node scripts/validate-fixtures.mjs citations
    node scripts/validate-fixtures.mjs
    node scripts/validate-schema-ts.mjs
    bash scripts/validate-schema-py.sh
    bash scripts/audit-fixtures.sh
    node scripts/validate-fixtures.mjs feasibility
    node scripts/validate-fixtures.mjs pin
    node scripts/validate-fixtures.mjs manifest
    ```

    All eight commands MUST exit 0.

    If `citations` fails:
    - The error message will name the file and the section lacking citations
    - Add the missing `spec/fixtures/<slug>` reference to that section
    - Re-run

    If any other validator fails, this plan discovered a regression in an upstream plan — STOP, note the issue, and reply with a description rather than proceeding.

    Once all eight pass, Phase 1 is effectively complete modulo the SUMMARY files and phase-gate verification.
  </action>
  <verify>
    <automated>node scripts/validate-fixtures.mjs citations 2>&1 | grep -qE "PASS|ok" && node scripts/validate-fixtures.mjs 2>&1 | grep -qE "PASS" && node scripts/validate-schema-ts.mjs 2>&1 | grep -qE "PASS" && bash scripts/validate-schema-py.sh 2>&1 | grep -qE "PASS" && bash scripts/audit-fixtures.sh 2>&1 | grep -qE "PASS|INFO: 0" && node scripts/validate-fixtures.mjs feasibility 2>&1 | grep -qE "PASS" && node scripts/validate-fixtures.mjs pin 2>&1 | grep -qE "PASS" && node scripts/validate-fixtures.mjs manifest 2>&1 | grep -qE "PASS|12/12"</automated>
  </verify>
  <done>
    All eight validator invocations exit 0. The `citations` subcommand specifically confirms both `spec/protocol.md` and `spec/errors.md` cite fixture filenames per normative claim. The `manifest` subcommand reports 12/12 fixtures present. The `feasibility` subcommand reports all three verdicts as non-pending.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, Phase 1 is READY for `/gsd:verify-work`:
- Every success criterion from ROADMAP.md §"Phase 1" is met:
  - ✓ 6+ (actually 11) captured fixtures with sibling .expected.json files (PRS-09)
  - ✓ spec/events.schema.json pinned and compiles in both TS + Pydantic generators (PRS-08)
  - ✓ Pinned gemini-cli version in .gemini-cli-compat + 3 feasibility verdicts in spec/feasibility.md
  - ✓ spec/protocol.md + spec/errors.md drafts cite fixture filenames per normative claim
- Every automated validation script exits 0

Full chain: `node scripts/validate-fixtures.mjs && node scripts/validate-schema-ts.mjs && bash scripts/validate-schema-py.sh && bash scripts/audit-fixtures.sh && node scripts/validate-fixtures.mjs citations feasibility pin manifest`
</verification>

<success_criteria>
- spec/protocol.md is a full draft (>=100 lines) with >= 5 fixture citations and sections for every event type
- spec/errors.md is a full draft (>=60 lines) with >= 3 fixture citations and the pattern table
- Neither doc contains its original placeholder text
- `node scripts/validate-fixtures.mjs citations` exits 0
- All eight validator commands in task 3 exit 0
- No API key patterns in either doc
</success_criteria>

<output>
After completion, create `.planning/phases/01-feasibility-spike-fixture-capture/01-10-SUMMARY.md` documenting:
- The event types covered by each protocol.md subsection
- The error patterns covered by each errors.md row
- Which gaps were documented (thinking, multimodal, error patterns not triggered)
- Final line counts for both docs
- Confirmation that all 8 validator checks pass
- Explicit note that this plan closes Phase 1 (subject to /gsd:verify-work sign-off)
</output>
