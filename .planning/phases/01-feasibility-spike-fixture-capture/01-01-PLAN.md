---
phase: 01-feasibility-spike-fixture-capture
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - .gitattributes
  - .gemini-cli-compat
  - spec/protocol.md
  - spec/errors.md
  - spec/feasibility.md
  - spec/capture.md
  - spec/events.schema.json
  - spec/fixtures/.gitkeep
  - spec/fixtures/_assets/.gitkeep
  - spec/fixtures.manifest.json
autonomous: true
requirements:
  - PRS-08
  - PRS-09
must_haves:
  truths:
    - "Repo root has a committed dev-dependency manifest that pins ajv + json-schema-to-typescript@15.0.4"
    - "Git does not normalize CRLF inside spec/fixtures/** (byte-exact fixture guarantee)"
    - ".gemini-cli-compat file exists at repo root (seed empty; W1 will write the pinned version)"
    - "spec/ tree has placeholder files that downstream tasks can append to atomically"
    - "spec/fixtures.manifest.json enumerates all 11 required fixture slugs so manifest validation fails loudly until W3 populates them"
    - "spec/events.schema.json starts as a minimal valid JSON Schema 2020-12 skeleton that passes ajv-cli compile"
  artifacts:
    - path: "package.json"
      provides: "Dev dependency manifest for ajv, ajv-formats, json-schema-to-typescript@15.0.4"
      contains: "\"json-schema-to-typescript\": \"15.0.4\""
    - path: ".gitattributes"
      provides: "CRLF normalization exemption for fixture corpus"
      contains: "spec/fixtures/** -text"
    - path: ".gemini-cli-compat"
      provides: "Seed file for pinned gemini-cli version (W1 writes actual version)"
    - path: "spec/events.schema.json"
      provides: "Minimal JSON Schema 2020-12 skeleton with empty oneOf"
      contains: "https://json-schema.org/draft/2020-12/schema"
    - path: "spec/fixtures.manifest.json"
      provides: "Canonical list of 11 required fixture slugs"
      contains: "simple-text"
    - path: "spec/protocol.md"
      provides: "Seed for event wire protocol draft (populated in W4)"
    - path: "spec/errors.md"
      provides: "Seed for error pattern draft (populated in W4)"
    - path: "spec/feasibility.md"
      provides: "Seed for feasibility verdicts (populated in W2)"
      contains: "resume_verdict: pending"
    - path: "spec/capture.md"
      provides: "Seed for capture procedure doc (populated in W2/W3)"
  key_links:
    - from: ".gitattributes"
      to: "spec/fixtures/"
      via: "git CRLF normalization disable rule"
      pattern: "spec/fixtures/\\*\\* -text"
    - from: "spec/fixtures.manifest.json"
      to: "scripts/validate-fixtures.mjs manifest (from plan 02)"
      via: "manifest subcommand reads this file"
      pattern: "\"slugs\""
---

<objective>
Create repo-root infrastructure and the spec/ directory skeleton so that subsequent waves (validation scripts, capture engine, smoke tests, fixture capture, schema freeze) can write to known-existing placeholder files without racing or clobbering.

Purpose: Phase 1 ships no SDK code — it ships spec documents, fixtures, and scripts. Those live under `spec/` and `scripts/`, which don't exist yet. This plan creates the empty skeleton so every W1+ task can append to atomic files rather than creating them mid-wave.

Output: `package.json`, `.gitattributes`, `.gemini-cli-compat` (empty seed), full `spec/` directory tree with seeded placeholders, and `spec/fixtures.manifest.json` naming all 11 required fixture slugs.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md
@.planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md
@.planning/phases/01-feasibility-spike-fixture-capture/01-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
<!-- This plan CREATES interfaces consumed by later plans. The key contracts are: -->

spec/fixtures.manifest.json (consumed by scripts/validate-fixtures.mjs manifest subcommand, plan 02):
```json
{
  "version": 1,
  "slugs": [
    "simple-text",
    "tool-use-builtin",
    "resume-session-turn1",
    "resume-session-turn2",
    "error-rate-limit",
    "error-auth",
    "event-unknown",
    "thinking",
    "multimodal-image",
    "multimodal-pdf",
    "large-output",
    "abort-midstream"
  ]
}
```

spec/events.schema.json (consumed by scripts/validate-schema-ts.mjs and scripts/validate-schema-py.sh in plan 02; populated in plan 09):
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gemini-sdk.dev/spec/events.schema.json",
  "title": "Gemini CLI stream-json event",
  "oneOf": []
}
```

spec/feasibility.md frontmatter (consumed by scripts/validate-fixtures.mjs feasibility subcommand, plan 02; filled in plan 05):
```yaml
---
resume_verdict: pending
config_dir_verdict: pending
flush_verdict: pending
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repo-root dev manifest, .gitattributes, and .gemini-cli-compat seed</name>
  <files>
    package.json
    .gitattributes
    .gemini-cli-compat
  </files>
  <read_first>
    .planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md (for pinned tool versions; see "Standard Stack" section — json-schema-to-typescript@15.0.4, ajv@8, ajv-formats)
    .planning/phases/01-feasibility-spike-fixture-capture/01-VALIDATION.md (for the "Wave 0 Requirements" checklist)
    .planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md (for the "Single-version fixture capture" decision)
    .planning/research/PITFALLS.md (Pitfall 5: CRLF drift; read the relevant lines to understand why the .gitattributes rule is load-bearing)
  </read_first>
  <action>
    Create three repo-root files using the Write tool:

    1. `package.json` — ESM package, private, with devDependencies exactly as follows (these are the versions pinned by RESEARCH.md §"Standard Stack"):

    ```json
    {
      "name": "gemini-sdk-spec",
      "private": true,
      "type": "module",
      "description": "Phase 1 specification and fixture infrastructure for the Gemini CLI SDK. Not published.",
      "scripts": {
        "validate": "node scripts/validate-fixtures.mjs",
        "validate:all": "node scripts/validate-fixtures.mjs && node scripts/validate-schema-ts.mjs && bash scripts/validate-schema-py.sh && bash scripts/audit-fixtures.sh"
      },
      "devDependencies": {
        "ajv": "^8.17.1",
        "ajv-formats": "^3.0.1",
        "json-schema-to-typescript": "15.0.4",
        "typescript": "^5.6.3"
      }
    }
    ```

    Do NOT run `npm install` — the execute phase will handle that. This plan only commits the manifest.

    2. `.gitattributes` — exactly this content (note the trailing newline):

    ```
    # Disable git CRLF normalization for captured fixtures so byte-exact NDJSON survives round-trip on Windows.
    # See Pitfall 5 in .planning/research/PITFALLS.md and Phase 1 CONTEXT.md "Secrets sanitization" section.
    spec/fixtures/** -text
    spec/fixtures/_assets/** -text binary
    ```

    The second line marks `_assets/` as binary so image and PDF assets are never diffed as text.

    3. `.gemini-cli-compat` — create an EMPTY file (zero bytes). W1 plan (01-04) will write the actual pinned version string `0.37.1` after verifying `gemini --version` on the capture host. Create it now as a placeholder so the `pin` validator subcommand from plan 02 has something to stat.

    Use the Write tool with an empty string as content for `.gemini-cli-compat`.

    Windows note: the Write tool handles paths with forward slashes on Windows; use `package.json`, `.gitattributes`, `.gemini-cli-compat` as file names (they are at repo root D:\repos\Gemini-SDK\).
  </action>
  <verify>
    <automated>test -f package.json && test -f .gitattributes && test -f .gemini-cli-compat && grep -q "json-schema-to-typescript" package.json && grep -q "15.0.4" package.json && grep -q "spec/fixtures/\*\* -text" .gitattributes && [ ! -s .gemini-cli-compat ]</automated>
  </verify>
  <done>
    `package.json` exists and contains the literal string `"json-schema-to-typescript": "15.0.4"`. `.gitattributes` exists and contains the literal line `spec/fixtures/** -text`. `.gemini-cli-compat` exists and is exactly zero bytes. All three are at repo root.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create spec/ directory skeleton with seeded placeholder files</name>
  <files>
    spec/protocol.md
    spec/errors.md
    spec/feasibility.md
    spec/capture.md
    spec/events.schema.json
    spec/fixtures/.gitkeep
    spec/fixtures/_assets/.gitkeep
    spec/fixtures.manifest.json
  </files>
  <read_first>
    .planning/phases/01-feasibility-spike-fixture-capture/01-VALIDATION.md (Wave 0 Requirements — spec/ placeholder subsection lists the EXACT seed content required)
    .planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md (Fixture scope section — the 11 required fixture slugs are enumerated there)
    .planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md (§"Recommended Phase 1 Output Structure" — shows the full spec/fixtures/ file list we're stubbing)
  </read_first>
  <action>
    Create the spec/ directory tree with seeded placeholder files. Use the Write tool for every file (not bash heredocs).

    1. `spec/protocol.md`:
    ```
    # Event Wire Protocol

    (Drafted in Phase 1 from captured fixtures.)
    ```

    2. `spec/errors.md`:
    ```
    # Error Patterns

    (Drafted in Phase 1 from captured error fixtures.)
    ```

    3. `spec/feasibility.md` — frontmatter is load-bearing because the `feasibility` validator subcommand (plan 02) greps these keys:
    ```
    ---
    resume_verdict: pending
    config_dir_verdict: pending
    flush_verdict: pending
    captured_against: pending
    ---

    # Phase 1 Feasibility Verdicts

    (Populated in Wave 2 by scripts/capture-fixtures.mjs feasibility.)
    ```

    4. `spec/capture.md`:
    ```
    # Fixture Capture Procedure

    (Documented in Phase 1 W2/W3. Describes exact prompt text, env setup, and CLI invocation for each fixture so re-capture is deterministic.)
    ```

    5. `spec/events.schema.json` — minimal valid JSON Schema 2020-12 skeleton. Use EXACTLY this JSON text (empty oneOf array is intentional — plan 09 populates it):
    ```json
    {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://gemini-sdk.dev/spec/events.schema.json",
      "title": "Gemini CLI stream-json event",
      "description": "Discriminated union of all event types emitted by gemini --output-format stream-json. Field schemas derived empirically from spec/fixtures/*.ndjson captured against the version pinned in .gemini-cli-compat. Populated in Phase 1 Wave 4.",
      "oneOf": []
    }
    ```

    Note: `oneOf: []` is valid JSON Schema 2020-12 but will cause `ajv compile` to succeed with a "matches nothing" warning. That is intentional for the W0 seed state; plan 09 populates oneOf before plan 09's acceptance check runs `validate-fixtures.mjs schema` against the fixtures.

    6. `spec/fixtures/.gitkeep` — empty file (zero bytes) so the directory is trackable.

    7. `spec/fixtures/_assets/.gitkeep` — empty file (zero bytes).

    8. `spec/fixtures.manifest.json` — canonical list of 11 required fixture slugs. Note that `resume-session` is split into a PAIR of fixture files (turn1 and turn2) per CONTEXT.md, so the manifest has 12 entries:
    ```json
    {
      "version": 1,
      "description": "Canonical list of fixture slugs required by Phase 1. Each slug <name> must have a <name>.ndjson and <name>.expected.json under spec/fixtures/. resume-session is captured as a PAIR (turn1 + turn2) per CONTEXT.md.",
      "slugs": [
        "simple-text",
        "tool-use-builtin",
        "resume-session-turn1",
        "resume-session-turn2",
        "error-rate-limit",
        "error-auth",
        "event-unknown",
        "thinking",
        "multimodal-image",
        "multimodal-pdf",
        "large-output",
        "abort-midstream"
      ]
    }
    ```

    All files go under `D:\repos\Gemini-SDK\spec\`. Use forward slashes in Write tool paths for cross-platform consistency.
  </action>
  <verify>
    <automated>test -f spec/protocol.md && test -f spec/errors.md && test -f spec/feasibility.md && test -f spec/capture.md && test -f spec/events.schema.json && test -f spec/fixtures/.gitkeep && test -f spec/fixtures/_assets/.gitkeep && test -f spec/fixtures.manifest.json && node -e "const s=JSON.parse(require('fs').readFileSync('spec/events.schema.json','utf8')); if(s['\$schema']!=='https://json-schema.org/draft/2020-12/schema') process.exit(1); if(!Array.isArray(s.oneOf)) process.exit(2);" && node -e "const m=JSON.parse(require('fs').readFileSync('spec/fixtures.manifest.json','utf8')); if(m.slugs.length!==12) process.exit(3); if(!m.slugs.includes('simple-text')) process.exit(4); if(!m.slugs.includes('abort-midstream')) process.exit(5);" && grep -q "resume_verdict: pending" spec/feasibility.md</automated>
  </verify>
  <done>
    Every file listed in `<files>` exists. `spec/events.schema.json` parses as valid JSON with `$schema = https://json-schema.org/draft/2020-12/schema` and an array-typed `oneOf`. `spec/fixtures.manifest.json` parses as valid JSON with exactly 12 entries in `slugs` including `simple-text`, `resume-session-turn1`, `resume-session-turn2`, and `abort-midstream`. `spec/feasibility.md` contains the literal line `resume_verdict: pending` inside its frontmatter block.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, the following must hold at repo root:
- `package.json`, `.gitattributes`, `.gemini-cli-compat` (empty) exist
- `spec/` directory exists with all 8 seeded placeholder files
- `spec/events.schema.json` is valid JSON Schema 2020-12 (empty oneOf is intentional)
- `spec/fixtures.manifest.json` has all 12 slug entries

Run: `test -f package.json && test -f .gitattributes && test -f .gemini-cli-compat && test -f spec/events.schema.json && test -f spec/fixtures.manifest.json && test -f spec/fixtures/.gitkeep && test -f spec/fixtures/_assets/.gitkeep`
</verification>

<success_criteria>
- All files listed in frontmatter `files_modified` exist on disk
- `package.json` pins `json-schema-to-typescript@15.0.4` exactly
- `.gitattributes` disables CRLF normalization for `spec/fixtures/**`
- `spec/events.schema.json` is valid JSON Schema 2020-12 skeleton that `npx ajv-cli@5 compile -s spec/events.schema.json --spec=draft2020` accepts (once ajv-cli is available — not required for this plan's verify)
- `spec/fixtures.manifest.json` enumerates all 12 fixture slugs (11 scenarios; resume-session is a 2-turn pair)
</success_criteria>

<output>
After completion, create `.planning/phases/01-feasibility-spike-fixture-capture/01-01-SUMMARY.md` documenting:
- Exact files created and their byte sizes
- The `spec/fixtures.manifest.json` slug list (for downstream plans to reference)
- Any deviations from the planned content
</output>
