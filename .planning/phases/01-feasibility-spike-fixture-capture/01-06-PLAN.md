---
phase: 01-feasibility-spike-fixture-capture
plan: 06
type: execute
wave: 3
depends_on:
  - 05
files_modified:
  - scripts/capture-fixtures.mjs
  - spec/fixtures/_assets/workspace/test.txt
  - spec/fixtures/tool-use-builtin.ndjson
  - spec/fixtures/tool-use-builtin.expected.json
  - spec/fixtures/error-rate-limit.ndjson
  - spec/fixtures/error-rate-limit.expected.json
  - spec/fixtures/error-rate-limit.stderr.txt
  - spec/fixtures/error-auth.ndjson
  - spec/fixtures/error-auth.expected.json
  - spec/fixtures/error-auth.stderr.txt
  - spec/fixtures/event-unknown.ndjson
  - spec/fixtures/event-unknown.expected.json
autonomous: false
requirements:
  - PRS-09
must_haves:
  truths:
    - "spec/fixtures/tool-use-builtin.ndjson is captured from a real run that triggered a built-in read_file tool call"
    - "spec/fixtures/error-rate-limit.ndjson + .stderr.txt are captured from a real rate-limit failure (or, if grace credit interferes, a loop of rapid-fire invocations)"
    - "spec/fixtures/error-auth.ndjson + .stderr.txt are captured from a real auth failure using GEMINI_API_KEY=invalid-key-12345"
    - "spec/fixtures/event-unknown.ndjson is a synthetic fixture constructed by copying the simple-text init line and mutating the type field to an invented value"
    - "Every .ndjson has a sibling .expected.json describing the MessageChunk sequence the parser should emit"
    - "`node scripts/validate-fixtures.mjs parse schema pairs manifest` exits 0 for all four fixtures (manifest still reports missing slugs from other plans)"
    - "No fixture contains the real GEMINI_API_KEY, absolute user paths, or unredacted session IDs"
  artifacts:
    - path: "spec/fixtures/tool-use-builtin.ndjson"
      provides: "Built-in tool call capture (read_file in a prepared workspace)"
    - path: "spec/fixtures/error-rate-limit.ndjson"
      provides: "Rate-limit failure capture with stream-json error event or non-zero exit"
    - path: "spec/fixtures/error-rate-limit.stderr.txt"
      provides: "Redacted stderr tail for Phase 5 error mapper"
    - path: "spec/fixtures/error-auth.ndjson"
      provides: "Auth failure capture"
    - path: "spec/fixtures/error-auth.stderr.txt"
      provides: "Redacted stderr tail for Phase 5"
    - path: "spec/fixtures/event-unknown.ndjson"
      provides: "Synthetic unknown-type fixture for PRS-03 parser lenient fallback test"
    - path: "spec/fixtures/_assets/workspace/test.txt"
      provides: "Fixture workspace file for tool-use-builtin scenario"
  key_links:
    - from: "scripts/capture-fixtures.mjs"
      to: "spec/fixtures/_assets/workspace/test.txt"
      via: "tool-use-builtin scenario cwd"
      pattern: "_assets/workspace"
    - from: "spec/fixtures/event-unknown.ndjson"
      to: "spec/fixtures/simple-text.ndjson"
      via: "synthetic construction derived from the init line"
      pattern: "cosmic_ray_hit"
---

<objective>
Capture five of the eleven Phase 1 fixtures: `tool-use-builtin`, `error-rate-limit`, `error-auth`, `event-unknown`, plus prepare the workspace asset needed for the tool-use capture. These five are grouped because they either share capture infrastructure (the error pair uses similar stderr handling) or are independently runnable (the synthetic event-unknown doesn't spawn gemini at all).

Purpose: Per CONTEXT.md §"Fixture scope", these are four of the six required fixtures from ROADMAP.md success criterion #1. Phase 3's parser tests consume tool-use-builtin to verify paired tool_use/tool_result emission (PRS-07). Phase 5's error taxonomy consumes the two error fixtures + their stderr tails. Phase 3's lenient-fallback test (PRS-03) consumes event-unknown.

Output: Five new `.ndjson` fixtures, their `.expected.json` sidecars, two `.stderr.txt` files, and one workspace asset file.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md
@.planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md
@.planning/phases/01-feasibility-spike-fixture-capture/01-VALIDATION.md
@spec/feasibility.md
@scripts/capture-fixtures.mjs
@spec/fixtures/simple-text.ndjson
@spec/fixtures/simple-text.expected.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the tool-use-builtin workspace asset and unstub tool-use-builtin + the two error scenarios</name>
  <files>
    spec/fixtures/_assets/workspace/test.txt
    scripts/capture-fixtures.mjs
  </files>
  <read_first>
    scripts/capture-fixtures.mjs (current state after plan 01-05; the `runScenario` function and `IMPLEMENTED` set exist)
    .planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md (§"11-Fixture Scenario Playbook" rows 2, 4, 5 — the exact triggers and expected events)
    .planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md (§"Fixture scope" rows 2, 4, 5)
    .planning/research/PITFALLS.md (Pitfall #4: orphan subprocesses; #5: silent death — relevant when error scenarios exit non-zero and we still need to capture stdout/stderr cleanly)
  </read_first>
  <action>
    **1. Create the workspace asset:**

    Create `spec/fixtures/_assets/workspace/test.txt` with content that will fit cleanly inside a tool_use_result (small, deterministic, non-secret):

    ```
    This is a fixture file used by the tool-use-builtin scenario. Phase 1 of the Gemini SDK.
    The gemini-cli read_file tool should return this content verbatim.
    ```

    Use the Write tool. Keep it under 500 bytes.

    **2. Update scripts/capture-fixtures.mjs — extend the `runScenario` function to handle error scenarios:**

    The existing handler from plan 01-04 was written for `simple-text` (a successful scenario). Error scenarios need:
    - Accept non-zero exit codes without failing the handler (`expectNonZeroExit: true` in the scenario)
    - Capture stderr to a separate `.stderr.txt` file using the redactor
    - Avoid pre-flight API key check when the scenario has `env.GEMINI_API_KEY = 'invalid-key-12345'` (error-auth deliberately uses a bad key)

    Modify the `runScenario` function to handle these cases. Specifically:
    - When `scenario.expectNonZeroExit === true`, do NOT treat a non-zero exit as a hard failure; log it and include it in the expected.json
    - When `scenario.env?.GEMINI_API_KEY` is explicitly set (even to an invalid value), skip the GEMINI_API_KEY pre-flight check — the scenario owns its own auth
    - When `scenario.captureStderr === true` OR stderr has content, always write the `.stderr.txt` sibling

    **3. Remove `stubbed: true` from and add to the IMPLEMENTED set:**
    - `tool-use-builtin` — scenario already has `cwd: 'spec/fixtures/_assets/workspace'` from plan 01-03
    - `error-rate-limit` — scenario has `captureStderr: true, expectNonZeroExit: true`
    - `error-auth` — scenario has `env: { GEMINI_API_KEY: 'invalid-key-12345' }, captureStderr: true, expectNonZeroExit: true`

    Update the `IMPLEMENTED` set:
    ```javascript
    const IMPLEMENTED = new Set(['simple-text', 'tool-use-builtin', 'error-rate-limit', 'error-auth']);
    ```

    **4. Confirm test.txt exists BEFORE the tool-use-builtin capture would run:**
    Add a small pre-check in the tool-use-builtin path: if `scenario.slug === 'tool-use-builtin'` and the cwd file doesn't exist, fail loudly with a message pointing at this plan's task 1.
  </action>
  <verify>
    <automated>test -f spec/fixtures/_assets/workspace/test.txt && [ $(wc -c < spec/fixtures/_assets/workspace/test.txt) -lt 500 ] && grep -q "tool-use-builtin" scripts/capture-fixtures.mjs && grep -q "error-rate-limit" scripts/capture-fixtures.mjs && grep -q "error-auth" scripts/capture-fixtures.mjs && grep -q "IMPLEMENTED" scripts/capture-fixtures.mjs && node -e "import('./scripts/capture-fixtures.mjs').then(()=>console.log('OK')).catch(e=>{console.error(e);process.exit(1)})" 2>&1 | grep -q OK</automated>
  </verify>
  <done>
    `spec/fixtures/_assets/workspace/test.txt` exists and is <500 bytes. `scripts/capture-fixtures.mjs` imports cleanly. The IMPLEMENTED set includes all four slugs (simple-text, tool-use-builtin, error-rate-limit, error-auth). The runScenario handler supports `expectNonZeroExit` and `captureStderr` flags.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture the three real fixtures (tool-use-builtin, error-rate-limit, error-auth) and human-review</name>
  <what-built>
    The capture script handlers from task 1 are ready. This checkpoint executes three individual captures against live gemini-cli, verifies they produce valid fixtures with no secret leaks, and has a human visually review each committed file before proceeding.
  </what-built>
  <read_first>
    .planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md (§"Secrets sanitization" — belt-and-braces human review)
    .planning/phases/01-feasibility-spike-fixture-capture/01-VALIDATION.md (§"Manual-Only Verifications" — human review row)
    .planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md (§"11-Fixture Scenario Playbook" row 4 — note about grace credit interference for rate-limit fixture)
  </read_first>
  <how-to-verify>
    On the capture host, with a valid `GEMINI_API_KEY` in the environment:

    1. Capture tool-use-builtin:
       ```
       node scripts/capture-fixtures.mjs tool-use-builtin
       ```
       Expect `PASS: captured tool-use-builtin`. Open `spec/fixtures/tool-use-builtin.ndjson` and verify it contains a `tool_use` event (or whatever gemini-cli actually emits for a `read_file` call — document if it differs).

    2. Capture error-auth (this one is deterministic):
       ```
       node scripts/capture-fixtures.mjs error-auth
       ```
       Expect a non-zero exit code, the script prints `PASS: captured error-auth (N events, exit=<nonzero>)`. Verify:
       - `spec/fixtures/error-auth.ndjson` exists (may be short or contain one `error` event)
       - `spec/fixtures/error-auth.stderr.txt` exists and contains recognizable auth-failure text (e.g., "API key not valid", "401", "unauthorized")
       - Neither file contains `invalid-key-12345` verbatim — it's a non-secret but we redacted it anyway because it's injected via env

    3. Capture error-rate-limit:
       ```
       node scripts/capture-fixtures.mjs error-rate-limit
       ```
       Expect this to be the TRICKY one. Per RESEARCH.md, grace credit on a fresh API key may absorb the first few requests. Two strategies:
       - Option A: Run a shell loop `for i in 1..60; do node scripts/capture-fixtures.mjs error-rate-limit; done` until one run captures a 429
       - Option B: Use a separately-created, quota-capped API key that exhausts on the first call
       - Option C: If rate-limit can't be reliably triggered in this session, capture whatever error actually appears (the script stores whatever stderr arrived) and document the limitation
       Then open `spec/fixtures/error-rate-limit.ndjson` and `.stderr.txt`. If the stderr mentions "quota", "rate", "429", or "Too Many Requests", the capture is good. If it captured a different error (like auth), re-run with a valid key.

    4. Run the validator:
       ```
       node scripts/validate-fixtures.mjs parse pairs
       bash scripts/audit-fixtures.sh
       ```
       Both must exit 0. If trufflehog flags anything, do NOT commit — fix the redactor and re-capture.

    5. Human review. Open each of these files and visually scan for anything that looks like a secret:
       - spec/fixtures/tool-use-builtin.ndjson
       - spec/fixtures/error-rate-limit.ndjson + .stderr.txt
       - spec/fixtures/error-auth.ndjson + .stderr.txt

    6. Reply with:
       - `approved` if all three captures look clean and the rate-limit capture contains rate-limit text (or you explicitly chose Option C with a documented limitation)
       - `issues: <description>` if any capture has leaks, wrong content, or the rate-limit capture didn't trigger
  </how-to-verify>
  <resume-signal>Reply with `approved` or `issues: <description>`.</resume-signal>

  <files>
    (none — human checkpoint; no files modified by Claude in this task)
  </files>
  <action>
    This task is a human-verification/action checkpoint. Follow the instructions in <how-to-verify> above. Claude does not execute commands in this task; the preceding or following auto tasks own the file changes.
  </action>
  <verify>
    <automated>echo "human checkpoint; verification happens via resume-signal from the user"</automated>
  </verify>
  <done>
    User has replied with `approved` (or described issues). Any issues raised are resolved before the next task runs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate the synthetic event-unknown.ndjson from simple-text's init line</name>
  <files>
    spec/fixtures/event-unknown.ndjson
    spec/fixtures/event-unknown.expected.json
  </files>
  <read_first>
    spec/fixtures/simple-text.ndjson (the source line for the synthetic construction)
    .planning/phases/01-feasibility-spike-fixture-capture/01-RESEARCH.md (Pitfall #7: synthetic fixtures must be based on a real line, with only the `type` field mutated)
    .planning/phases/01-feasibility-spike-fixture-capture/01-CONTEXT.md (§"Required" fixture #6: `event-unknown` — described as the only synthetic fixture, with explicit rationale)
  </read_first>
  <action>
    Generate `spec/fixtures/event-unknown.ndjson` by reading the first line of `spec/fixtures/simple-text.ndjson` (which should be an `init` event), parsing it as JSON, mutating `type` to `"cosmic_ray_hit"`, and serializing as a single-line NDJSON file.

    Use the Bash tool to run a short Node one-liner (no new script file needed):

    ```bash
    node -e '
    const fs = require("node:fs");
    const src = fs.readFileSync("spec/fixtures/simple-text.ndjson", "utf8");
    const firstLine = src.split(/\r?\n/).find(l => l.trim().length > 0);
    if (!firstLine) { console.error("FAIL: simple-text.ndjson is empty"); process.exit(1); }
    const obj = JSON.parse(firstLine);
    if (obj.type !== "init") {
      console.error("WARN: first line of simple-text is type=" + obj.type + ", not init; using it anyway");
    }
    obj.type = "cosmic_ray_hit";
    obj._synthetic = true;
    obj._derived_from = "simple-text.ndjson line 1";
    obj._note = "Mutated from a real captured init event by replacing type with an invented value. Used by Phase 3 PRS-03 to verify the parser yields {type:\"unknown\", raw} without throwing.";
    fs.writeFileSync("spec/fixtures/event-unknown.ndjson", JSON.stringify(obj) + "\n");
    console.log("OK: event-unknown.ndjson written (" + JSON.stringify(obj).length + " bytes)");
    '
    ```

    Then create the sidecar `spec/fixtures/event-unknown.expected.json` using the Write tool:

    ```json
    {
      "fixture": "event-unknown.ndjson",
      "captured_against": "SYNTHETIC — derived from simple-text.ndjson by type-mutation",
      "captured_at": "<set to ISO timestamp at write time; can use a frozen placeholder like 2026-04-11T00:00:00Z>",
      "description": "Synthetic fixture: a captured init event with its type field mutated to an invented value (cosmic_ray_hit). Used by Phase 3 PRS-03 to verify the parser's lenient fallback yields {type:'unknown', raw: <original>} without throwing.",
      "chunks": [
        {
          "type": "unknown",
          "raw": {
            "_comment": "The raw field should hold the full original JSON object. Phase 3's parser test asserts this shape."
          }
        }
      ],
      "exit_code": 0,
      "stderr_patterns": [],
      "synthetic": true,
      "derived_from": "simple-text.ndjson line 1"
    }
    ```

    Then run the validator:
    ```
    node scripts/validate-fixtures.mjs parse pairs
    ```
    It must exit 0 with event-unknown now counted.

    Also update `spec/capture.md` to append a "Synthetic fixtures" section explaining that `event-unknown` is constructed (not captured) and describing the construction procedure for re-running. Use the Edit tool to append a new `## Synthetic fixtures` section to the end of the file.
  </action>
  <verify>
    <automated>test -f spec/fixtures/event-unknown.ndjson && test -f spec/fixtures/event-unknown.expected.json && node -e "const l=require('fs').readFileSync('spec/fixtures/event-unknown.ndjson','utf8').split('\n').filter(Boolean); if(l.length!==1)process.exit(1); const o=JSON.parse(l[0]); if(o.type!=='cosmic_ray_hit')process.exit(2); if(!o._synthetic)process.exit(3); console.log('OK')" | grep -q OK && node -e "const e=JSON.parse(require('fs').readFileSync('spec/fixtures/event-unknown.expected.json','utf8')); if(e.synthetic!==true)process.exit(1); if(!e.chunks||e.chunks[0].type!=='unknown')process.exit(2); console.log('OK')" | grep -q OK && node scripts/validate-fixtures.mjs parse 2>&1 | grep -qE "PASS|ok" && node scripts/validate-fixtures.mjs pairs 2>&1 | grep -qE "PASS|ok" && grep -qi "synthetic" spec/capture.md</automated>
  </verify>
  <done>
    `spec/fixtures/event-unknown.ndjson` exists as a single-line NDJSON file containing a JSON object with `type: "cosmic_ray_hit"` and `_synthetic: true` marker fields. `spec/fixtures/event-unknown.expected.json` exists with `synthetic: true`, `derived_from` citing `simple-text.ndjson line 1`, and a `chunks[0].type: 'unknown'` entry. `spec/capture.md` has a new section mentioning "synthetic" and explaining the construction. The validator passes parse + pairs for event-unknown.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
- Five fixtures are committed: tool-use-builtin, error-rate-limit (+ stderr), error-auth (+ stderr), event-unknown, and the workspace asset
- `node scripts/validate-fixtures.mjs parse pairs` exits 0 for every new fixture
- `bash scripts/audit-fixtures.sh` exits 0 (no secrets detected)
- `node scripts/validate-fixtures.mjs manifest` shows 5/12 fixtures present (simple-text from W1 + 4 from this plan)

Full chain: `node scripts/validate-fixtures.mjs && bash scripts/audit-fixtures.sh`
</verification>

<success_criteria>
- tool-use-builtin fixture captured from a real run with a built-in tool call
- error-rate-limit fixture captured from a real rate-limit failure (or documented fallback)
- error-auth fixture captured from a real auth failure
- event-unknown fixture constructed synthetically from simple-text's init line
- All four have valid `.expected.json` sidecars
- workspace/test.txt exists for the tool-use capture
- Manifest validator reports 5/12 fixtures present
- No committed file contains secrets
</success_criteria>

<output>
After completion, create `.planning/phases/01-feasibility-spike-fixture-capture/01-06-SUMMARY.md` documenting:
- Final byte sizes and event counts for each of the 5 new fixtures
- Whether the rate-limit capture needed fallback Option B or C (per task 2 instructions)
- Whether tool-use-builtin emitted a `tool_use`/`tool_result` pair or used a different event shape (Phase 3 PRS-07 needs to know)
- Any redactor refinements discovered during human review
- The actual `type` values observed in each fixture (for plan 01-09's schema derivation)
</output>
