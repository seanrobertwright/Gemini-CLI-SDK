---
phase: 11-docs-site-compat-matrix-release
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - docs/ts/quickstart.md
  - docs/ts/migration-claude.md
  - docs/ts/migration-codex.md
  - docs-python/quickstart.md
  - docs-python/migration-claude.md
  - docs-python/migration-codex.md
  - docs/compat-matrix.md
  - docs/known-issues.md
  - docs/known-issues.data.ts
  - docs/known-issues.yml
  - docs/archon-integration.md
autonomous: true
requirements: [DOC-02, DOC-04, DOC-05, DOC-06, DOC-07]
must_haves:
  truths:
    - "Quickstart page walks a developer through: install gemini-cli, set GEMINI_API_KEY, first query(), multi-turn session, first MCP server (TS and Python each)"
    - "Compat matrix page lists the tested gemini-cli range (0.37.x) and documents the GEMINI_SDK_COMPAT env var contract (strict/silent/warn)"
    - "Known issues page renders a sortable table of upstream bugs from docs/known-issues.yml — 5 minimum entries covering #14180, #13388, #3485, #22970, #4945"
    - "Migration guide for Claude Agent SDK maps 5 call-site patterns (construct client, send query, stream chunks, tool use, session resume) side-by-side"
    - "Migration guide for Codex SDK mirrors the same 5 patterns"
    - "Archon integration guide documents applying the pr-artifacts bundle and setting DEFAULT_AI_ASSISTANT=gemini"
    - "`pnpm docs:build` still exits 0 with all new content pages"
  artifacts:
    - path: "docs/ts/quickstart.md"
      provides: "TS Quickstart with 5 steps: install CLI, API key, first query, multi-turn, MCP"
      min_lines: 80
    - path: "docs-python/quickstart.md"
      provides: "Python Quickstart mirror"
      min_lines: 80
    - path: "docs/compat-matrix.md"
      provides: "Compat range table + GEMINI_SDK_COMPAT behavior reference"
      min_lines: 40
    - path: "docs/known-issues.yml"
      provides: "Structured data for known-issues table; 5+ upstream issues"
    - path: "docs/known-issues.data.ts"
      provides: "VitePress data loader — reads known-issues.yml via js-yaml"
    - path: "docs/known-issues.md"
      provides: "Rendered sortable table referencing live GitHub issue links"
    - path: "docs/archon-integration.md"
      provides: "DEFAULT_AI_ASSISTANT=gemini walkthrough sourced from pr-artifacts/README.md and PR_BODY.md"
      min_lines: 60
    - path: "docs/ts/migration-claude.md"
      provides: "Claude Agent SDK → Gemini SDK mapping (5 patterns)"
      min_lines: 60
    - path: "docs/ts/migration-codex.md"
      provides: "Codex SDK → Gemini SDK mapping (5 patterns)"
      min_lines: 60
  key_links:
    - from: "docs/known-issues.md"
      to: "docs/known-issues.yml"
      via: "Vue setup script imports data from ./known-issues.data.ts which reads the yml file"
      pattern: "known-issues.data"
    - from: "docs/compat-matrix.md"
      to: ".gemini-cli-compat"
      via: "page documents the pinned version (0.37.1) and the derived range (0.37.x)"
      pattern: "0.37"
---

<objective>
Author documentation content consumed by the VitePress + MkDocs scaffolding from plan 11-02, backed by the compat probe from plan 11-01. Covers quickstart (DOC-02), compat matrix (DOC-04), known-issues appendix (DOC-05), migration guides (DOC-06), and Archon integration guide (DOC-07). All content is concrete, copy-ready — placeholders have been filled in.

Purpose: Make the docs site actually useful. Scaffolding (11-02) proves the pipeline; this plan gives it substance.
Output: Developer can visit the built site and complete a quickstart end-to-end.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-docs-site-compat-matrix-release/11-CONTEXT.md
@.planning/phases/11-docs-site-compat-matrix-release/11-RESEARCH.md
@.planning/phases/11-docs-site-compat-matrix-release/11-01-SUMMARY.md
@.planning/phases/11-docs-site-compat-matrix-release/11-02-SUMMARY.md
@.planning/phases/10-archon-adapter-ts-only/pr-artifacts/README.md
@.planning/phases/10-archon-adapter-ts-only/pr-artifacts/PR_BODY.md
@ts/src/index.ts
@python/src/gemini_sdk/__init__.py
@.gemini-cli-compat
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write quickstart + compat-matrix + known-issues pages</name>
  <files>docs/ts/quickstart.md, docs-python/quickstart.md, docs/compat-matrix.md, docs/known-issues.md, docs/known-issues.data.ts, docs/known-issues.yml</files>
  <read_first>
    - ts/src/index.ts (public exports — ensures quickstart code snippets reference real symbols)
    - python/src/gemini_sdk/__init__.py (public Python exports)
    - ts/src/compat.ts (compat probe contract for the matrix page)
    - python/src/gemini_sdk/compat.py (Python side)
    - .gemini-cli-compat (pinned version = `0.37.1`)
    - .planning/phases/11-docs-site-compat-matrix-release/11-RESEARCH.md §"Known-Issues YAML Schema" (lines 563–586) + §"Pattern 6"
  </read_first>
  <action>

  **1. `docs/ts/quickstart.md`** — write the following EXACT 5-step content (substitute code examples with real TS/JS only; Python version in step 2 below):

  ```markdown
  # TypeScript Quickstart

  Drive `gemini-cli` from TypeScript with a streaming `query()` generator.

  ## Prerequisites

  - Node.js 18+ (20 or 22 recommended)
  - `gemini-cli` 0.37.x installed on PATH — **not bundled**, see [compat matrix](../compat-matrix.md)
  - A Gemini API key

  ## 1. Install gemini-cli (runtime prerequisite)

  ```bash
  npm install -g @google/gemini-cli
  gemini --version  # should print 0.37.x
  ```

  The SDK does not bundle or auto-install `gemini-cli`. See the [compat matrix](../compat-matrix.md) for supported versions.

  ## 2. Install the SDK

  ```bash
  npm install @gemini-sdk/core
  ```

  ## 3. Set your API key

  ```bash
  export GEMINI_API_KEY="your-key-from-ai.google.dev"
  ```

  `GEMINI_API_KEY` is the canonical default. Vertex AI via `GOOGLE_APPLICATION_CREDENTIALS` or `GOOGLE_API_KEY` is also supported — see the auth reference.

  ## 4. Your first `query()`

  ```typescript
  import { query } from '@gemini-sdk/core';

  for await (const chunk of query({ prompt: 'Say hello' })) {
    if (chunk.type === 'assistant') {
      process.stdout.write(chunk.text);
    }
  }
  ```

  ## 5. Multi-turn session

  ```typescript
  import { queryFull, query } from '@gemini-sdk/core';

  const first = await queryFull({ prompt: 'Remember the number 7.' });
  const session = first.session;  // captured from init event

  for await (const chunk of query({
    prompt: 'What number did I just say?',
    resumeSessionId: session.id,
  })) {
    if (chunk.type === 'assistant') process.stdout.write(chunk.text);
  }
  ```

  ## 6. Your first MCP server

  ```typescript
  import { query } from '@gemini-sdk/core';

  for await (const chunk of query({
    prompt: 'Use the time tool to report current UTC.',
    mcpServers: {
      time: { command: 'node', args: ['./time-mcp-server.js'] },
    },
    allowedMcpServerNames: ['time'],
  })) {
    console.log(chunk);
  }
  ```

  The SDK writes a temp `settings.json` in an isolated `GEMINI_CONFIG_DIR` for the query and cleans up in `finally` — your real `~/.gemini/settings.json` is never touched.

  ## Next steps

  - [API reference](./api/)
  - [Migration from Claude Agent SDK](./migration-claude.md)
  - [Migration from Codex SDK](./migration-codex.md)
  - [Archon integration](../archon-integration.md)
  ```

  **2. `docs-python/quickstart.md`** — mirror of the TS quickstart with Python syntax. Same 5-step structure:

  ```markdown
  # Python Quickstart

  Drive `gemini-cli` from Python 3.10+ with an async `query()` iterator.

  ## Prerequisites

  - Python 3.10+ (3.13 recommended)
  - `gemini-cli` 0.37.x installed on PATH — see [compat matrix](../compat-matrix.md)
  - A Gemini API key

  ## 1. Install gemini-cli

  ```bash
  npm install -g @google/gemini-cli
  gemini --version
  ```

  ## 2. Install the SDK

  ```bash
  uv add gemini-sdk
  # or: pip install gemini-sdk
  ```

  ## 3. Set your API key

  ```bash
  export GEMINI_API_KEY="your-key-from-ai.google.dev"
  ```

  ## 4. Your first `query()`

  ```python
  import anyio
  from gemini_sdk import query

  async def main():
      async for chunk in query(prompt="Say hello"):
          if chunk["type"] == "assistant":
              print(chunk["text"], end="", flush=True)

  anyio.run(main)
  ```

  ## 5. Multi-turn session

  ```python
  from gemini_sdk import query, query_full

  async def main():
      first = await query_full(prompt="Remember the number 7.")
      session_id = first["session"]["id"]

      async for chunk in query(prompt="What number did I just say?", resume_session_id=session_id):
          if chunk["type"] == "assistant":
              print(chunk["text"], end="", flush=True)
  ```

  ## 6. Your first MCP server

  ```python
  async for chunk in query(
      prompt="Use the time tool to report current UTC.",
      mcp_servers={"time": {"command": "node", "args": ["./time-mcp-server.js"]}},
      allowed_mcp_server_names=["time"],
  ):
      print(chunk)
  ```

  ## Next steps

  - [API reference](./api.md)
  - [Migration guides](./migration-claude.md) — Claude Agent SDK / Codex SDK
  ```

  **3. `docs/compat-matrix.md`** — EXACT content:

  ```markdown
  # Compatibility Matrix

  | Component | Tested Range | Notes |
  |-----------|-------------|-------|
  | `gemini-cli` | `0.37.x` | Pinned in `.gemini-cli-compat`. The SDK derives the tested range as `~<pinned>` (same major.minor). |
  | Node.js | 18, 20, 22 | CI matrix gates every release. |
  | Python | 3.10, 3.11, 3.12, 3.13 | CI matrix gates every release. |
  | Platforms | Windows, macOS, Linux | Windows is a hard-required CI job (not continue-on-error). |

  ## Runtime compat probe

  On the first `query()` call per process, the SDK spawns `gemini --version` once, compares against the pinned range, and caches the result for the rest of the process lifetime.

  ### `GEMINI_SDK_COMPAT` — override probe behavior

  | Value | Behavior |
  |-------|----------|
  | _(unset)_ or any unrecognized value | **Default — warn:** emit `[gemini-sdk] tested against gemini-cli 0.37.x, detected <detected> — proceeding` to stderr and continue. |
  | `strict` | Throw an error (TS) / raise `RuntimeError` (Python) with the same message; SDK refuses to proceed. |
  | `silent` | Suppress the warning entirely. SDK still runs. |

  ### When to use each mode

  - **warn (default):** Development and most production use. You see drift but you're not blocked.
  - **strict:** CI pipelines that pin to a specific gemini-cli version and want to fail loudly when the container base image drifts.
  - **silent:** Bulk batch workers where stderr noise is unwelcome and you accept the drift risk.

  ## gemini-cli is NOT bundled

  The SDK declares `gemini-cli` as a runtime prerequisite but does not install it. Users are responsible for installing and updating `gemini-cli` themselves. See the [Archon pattern discussion](https://github.com/coleam00/Archon/discussions) for the rationale.
  ```

  **4. `docs/known-issues.yml`** — EXACT content (5 entries):

  ```yaml
  # known-issues.yml — source of truth for docs/known-issues.md table
  # Schema: {upstream_issue, title, sdk_defense, status: open|fixed}
  - upstream_issue: "#14180"
    title: "--resume + -p interop broken in some gemini-cli versions"
    sdk_defense: "Transcript-prepend fallback in Session + ArgvBuilder (SES-04); primary path verified pass on 0.37.1"
    status: open
  - upstream_issue: "#13388"
    title: "No native JSON schema enforcement in output"
    sdk_defense: "Best-effort schema injection + Zod/Pydantic runtime validation + one retry (OUT-01..04); marked @experimental"
    status: open
  - upstream_issue: "#3485"
    title: "MCP server config format drift across versions"
    sdk_defense: "Isolated temp GEMINI_CONFIG_DIR per query; user's ~/.gemini/settings.json never mutated (MCP-02)"
    status: open
  - upstream_issue: "#22970"
    title: "OAuth ToS risk in headless/SDK contexts"
    sdk_defense: "GEMINI_API_KEY is canonical default; OAuth auto-login explicitly forbidden (AUT-05, AUT-08)"
    status: open
  - upstream_issue: "#4945"
    title: "Encoding issues in non-UTF-8 environments"
    sdk_defense: "Force UTF-8 at subprocess spawn; decode stderr/stdout with replacement on error (FDN-04)"
    status: open
  - upstream_issue: "#13604"
    title: "Windows EBUSY on settings.json cleanup"
    sdk_defense: "fs.rm with maxRetries:3 retryDelay:200 in cleanupConfigDir (MCP-04)"
    status: open
  ```

  **5. `docs/known-issues.data.ts`** — EXACT content (VitePress data loader):

  ```typescript
  import { readFileSync } from 'node:fs';
  import { resolve, dirname } from 'node:path';
  import { fileURLToPath } from 'node:url';
  import yaml from 'js-yaml';

  export interface KnownIssue {
    upstream_issue: string;
    title: string;
    sdk_defense: string;
    status: 'open' | 'fixed';
  }

  export default {
    watch: ['./known-issues.yml'],
    load(): KnownIssue[] {
      const here = dirname(fileURLToPath(import.meta.url));
      const ymlPath = resolve(here, 'known-issues.yml');
      const raw = readFileSync(ymlPath, 'utf-8');
      return yaml.load(raw) as KnownIssue[];
    },
  };
  ```

  **6. `docs/known-issues.md`** — EXACT content:

  ```markdown
  # Known Issues

  Upstream `gemini-cli` bugs the SDK defends against. Entries auto-render from `docs/known-issues.yml` — edit that file to add/update/flip status.

  <script setup lang="ts">
  import { data as issues } from './known-issues.data.ts';

  function issueUrl(ref: string): string {
    const num = ref.replace(/^#/, '');
    return `https://github.com/google-gemini/gemini-cli/issues/${num}`;
  }
  </script>

  <table>
    <thead>
      <tr>
        <th>Upstream</th>
        <th>Title</th>
        <th>SDK Defense</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
      <tr v-for="issue in issues" :key="issue.upstream_issue">
        <td><a :href="issueUrl(issue.upstream_issue)" target="_blank" rel="noopener">{{ issue.upstream_issue }}</a></td>
        <td>{{ issue.title }}</td>
        <td>{{ issue.sdk_defense }}</td>
        <td>
          <strong v-if="issue.status === 'fixed'" style="color: #22c55e">fixed</strong>
          <span v-else style="color: #f59e0b">open</span>
        </td>
      </tr>
    </tbody>
  </table>

  ## Reporting new issues

  File new issues under [google-gemini/gemini-cli](https://github.com/google-gemini/gemini-cli/issues). When the SDK adds a defense for a new upstream bug, append an entry to `docs/known-issues.yml`.
  ```

  **7. Verify:** Run `pnpm docs:typedoc && pnpm docs:build` at repo root. Build must exit 0. The VitePress build validates that `known-issues.data.ts` loads, that `known-issues.yml` parses, and that all markdown renders.
  </action>
  <verify>
    <automated>pnpm docs:typedoc &amp;&amp; pnpm docs:build &amp;&amp; test -f docs/.vitepress/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/ts/quickstart.md` exits 0
    - `test -f docs-python/quickstart.md` exits 0
    - `test -f docs/compat-matrix.md` exits 0
    - `test -f docs/known-issues.yml` exits 0
    - `test -f docs/known-issues.data.ts` exits 0
    - `test -f docs/known-issues.md` exits 0
    - `wc -l docs/ts/quickstart.md | awk '{print $1}'` returns >= 80
    - `wc -l docs-python/quickstart.md | awk '{print $1}'` returns >= 80
    - `wc -l docs/compat-matrix.md | awk '{print $1}'` returns >= 40
    - `grep -q "gemini --version" docs/ts/quickstart.md`
    - `grep -q "GEMINI_API_KEY" docs/ts/quickstart.md`
    - `grep -q "resumeSessionId" docs/ts/quickstart.md`
    - `grep -q "mcpServers" docs/ts/quickstart.md`
    - `grep -q "resume_session_id" docs-python/quickstart.md`
    - `grep -q "mcp_servers" docs-python/quickstart.md`
    - `grep -q "GEMINI_SDK_COMPAT" docs/compat-matrix.md`
    - `grep -q "0.37" docs/compat-matrix.md`
    - `grep -q "strict" docs/compat-matrix.md`
    - `grep -q "silent" docs/compat-matrix.md`
    - `grep -c "upstream_issue" docs/known-issues.yml` returns >= 5
    - `grep -q "#14180" docs/known-issues.yml`
    - `grep -q "#13388" docs/known-issues.yml`
    - `grep -q "#3485" docs/known-issues.yml`
    - `grep -q "#22970" docs/known-issues.yml`
    - `grep -q "#4945" docs/known-issues.yml`
    - `pnpm docs:build` exits 0
  </acceptance_criteria>
  <done>VitePress builds with quickstart, compat-matrix, and known-issues pages live; YAML drives the table.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration guides (Claude + Codex) + Archon integration guide</name>
  <files>docs/ts/migration-claude.md, docs/ts/migration-codex.md, docs-python/migration-claude.md, docs-python/migration-codex.md, docs/archon-integration.md, docs/.vitepress/config.mts</files>
  <read_first>
    - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/README.md (apply sequence — source material for DOC-07)
    - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/PR_BODY.md (PR narrative — source material for DOC-07)
    - docs/.vitepress/config.mts (sidebar config — need to add migration + archon-integration entries)
    - ts/src/index.ts (actual exports — migration snippets must reference real symbols)
    - adapter-archon/src/ (adapter shape)
  </read_first>
  <action>

  **1. `docs/ts/migration-claude.md`** — EXACT content:

  ```markdown
  # Migrating from Claude Agent SDK

  This is not a full API cross-reference — those rot quickly. Instead, here are the top 5 call-site patterns mapped between `@anthropic-ai/claude-agent-sdk` and `@gemini-sdk/core`.

  ## Pattern 1 — Construct the client

  | Claude | Gemini |
  |--------|--------|
  | `const client = new ClaudeAgentClient({ apiKey })` | No client class — `query()` is a module-level function. Pass `GEMINI_API_KEY` via env. |

  ```diff
  - import { ClaudeAgentClient } from '@anthropic-ai/claude-agent-sdk';
  - const client = new ClaudeAgentClient({ apiKey: process.env.ANTHROPIC_API_KEY });
  + import { query } from '@gemini-sdk/core';
  + // process.env.GEMINI_API_KEY is picked up automatically
  ```

  ## Pattern 2 — Send a query

  ```diff
  - for await (const chunk of client.sendQuery({ prompt: 'Hi' })) { ... }
  + for await (const chunk of query({ prompt: 'Hi' })) { ... }
  ```

  ## Pattern 3 — Stream chunks

  Both SDKs emit the same 8-variant `MessageChunk` union (`assistant | system | thinking | result | rate_limit | tool | tool_result | workflow_dispatch`). Discriminate on `chunk.type`.

  ```typescript
  for await (const chunk of query({ prompt })) {
    switch (chunk.type) {
      case 'assistant': process.stdout.write(chunk.text); break;
      case 'tool': console.log('[tool]', chunk.toolName, chunk.input); break;
      case 'result': console.log('[done]', chunk.usage); break;
    }
  }
  ```

  ## Pattern 4 — Tool use

  | Claude | Gemini |
  |--------|--------|
  | `tools: [myTool]` (caller-defined tools allowed in v1) | `allowedTools: ['read_file']` + MCP passthrough. Caller-defined JS tools deferred to v2 (see Known Issues #13388). |

  ```diff
  - for await (const chunk of client.sendQuery({
  -   prompt,
  -   tools: [myCustomTool],
  - })) { ... }
  + for await (const chunk of query({
  +   prompt,
  +   allowedTools: ['read_file'],
  +   approvalMode: 'yolo',
  +   mcpServers: { /* register MCP server for custom tools */ },
  + })) { ... }
  ```

  ## Pattern 5 — Session resume

  ```diff
  - const session = await client.createSession();
  - for await (const chunk of client.sendQuery({ prompt, sessionId: session.id })) { ... }
  + import { queryFull, query } from '@gemini-sdk/core';
  + const first = await queryFull({ prompt: 'turn 1' });
  + for await (const chunk of query({ prompt: 'turn 2', resumeSessionId: first.session.id })) { ... }
  ```

  ## Key differences

  - **No client construction step.** `query()` is stateless; auth comes from env.
  - **Caller-defined tools deferred to v2.** Use MCP servers for custom tool logic (see [MCP passthrough docs](./api/)).
  - **Approval mode vocabulary differs:** Gemini uses `default | auto_edit | yolo | plan` (maps to `gemini-cli --approval-mode`), not Claude's `permissionMode` names.
  ```

  **2. `docs/ts/migration-codex.md`** — EXACT content:

  ```markdown
  # Migrating from Codex SDK

  Mapping of 5 top call-site patterns between `@openai/codex-sdk` and `@gemini-sdk/core`.

  ## Pattern 1 — Construct the client

  ```diff
  - import { CodexClient } from '@openai/codex-sdk';
  - const client = new CodexClient({ apiKey: process.env.OPENAI_API_KEY });
  + import { query } from '@gemini-sdk/core';
  + // GEMINI_API_KEY picked up from env
  ```

  ## Pattern 2 — Send a query

  ```diff
  - const stream = client.chat({ messages: [{ role: 'user', content: 'Hi' }] });
  - for await (const event of stream) { ... }
  + for await (const chunk of query({ prompt: 'Hi' })) { ... }
  ```

  Gemini's `query()` takes a single `prompt` string (the SDK wraps headless `gemini-cli -p`). For multi-turn, use `resumeSessionId` — see Pattern 5.

  ## Pattern 3 — Stream chunks

  Codex yields OpenAI-style chunks (`choices[0].delta.content`). Gemini yields normalized `MessageChunk` variants:

  ```diff
  - for await (const event of stream) {
  -   process.stdout.write(event.choices[0].delta.content ?? '');
  - }
  + for await (const chunk of query({ prompt })) {
  +   if (chunk.type === 'assistant') process.stdout.write(chunk.text);
  + }
  ```

  ## Pattern 4 — Tool use

  ```diff
  - const tools = [{ type: 'function', function: { name: 'search', ... } }];
  - client.chat({ messages, tools });
  + query({
  +   prompt,
  +   allowedTools: ['read_file'],
  +   approvalMode: 'yolo',
  +   // MCP passthrough for custom tools — see MCP docs
  + });
  ```

  ## Pattern 5 — Session / conversation resume

  Codex manages conversations server-side via `conversation_id`. Gemini's session is client-owned:

  ```diff
  - const conv = await client.createConversation();
  - client.chat({ conversationId: conv.id, messages: [...] });
  + const first = await queryFull({ prompt: 'turn 1' });
  + query({ prompt: 'turn 2', resumeSessionId: first.session.id });
  ```

  ## Key differences

  - **Single prompt string** instead of a messages array. Multi-turn via `resumeSessionId`.
  - **No streaming of function-call arguments** — tool invocations arrive as full `tool` chunks.
  - **No server-side conversation state** — `Session` is immutable, identifier-only, trivially serializable.
  ```

  **3. Python migration guides:** Create `docs-python/migration-claude.md` and `docs-python/migration-codex.md` — structurally identical to the TS versions but with Python syntax (`async for`, `snake_case`, `dict`-style chunks instead of discriminated unions, `await query_full(...)`, `resume_session_id`). Each must be >= 60 lines and cover the same 5 patterns.

  **4. `docs/archon-integration.md`** — EXACT content (sourced from pr-artifacts README + PR_BODY):

  ```markdown
  # Archon Integration Guide

  Use Gemini as a third AI assistant in [coleam00/Archon](https://github.com/coleam00/Archon) alongside Claude and Codex. `DEFAULT_AI_ASSISTANT=gemini` end-to-end.

  ## Status

  The adapter currently ships as a **local bundle** applied to a user-owned Archon fork. An upstream PR to `coleam00/Archon` is intentionally deferred per project decision — see the [Archon-local project memory](https://github.com/seanrobertwright/Gemini-SDK/blob/master/.planning/STATE.md).

  ## What's in the bundle

  The SDK repo stages nine files under `.planning/phases/10-archon-adapter-ts-only/pr-artifacts/`:

  ```
  pr-artifacts/
  ├── README.md                  apply instructions
  ├── PR_BODY.md                 draft PR narrative (for reference)
  ├── registry.patch             unified diff for packages/providers/src/registry.ts
  ├── env.example.patch          unified diff for Archon .env.example
  └── gemini/                    copy this whole directory into packages/providers/src/community/
      ├── provider.ts
      ├── capabilities.ts
      ├── registration.ts
      ├── options-translator.ts
      └── index.ts
  ```

  ## Applying the bundle

  From the root of a fresh clone of your Archon fork (pin to the SHA in `.archon-compat`):

  ```bash
  GEMINI_SDK_DIR=/path/to/Gemini-SDK

  # 1. Copy the provider source files into the community tree.
  cp -r "$GEMINI_SDK_DIR/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini" \
        packages/providers/src/community/

  # 2. Apply the registry patch.
  git apply "$GEMINI_SDK_DIR/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/registry.patch"

  # 3. Apply the .env.example patch.
  git apply "$GEMINI_SDK_DIR/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/env.example.patch"

  # 4. Add the SDK as a dependency. For pre-1.0.0, use the GitHub tarball ref.
  #    Post-1.0.0, use the npm package:
  cd packages/providers
  bun add @gemini-sdk/core
  cd ../..

  # 5. Install + verify.
  bun install
  bun test packages/providers
  ```

  ## Configuring DEFAULT_AI_ASSISTANT=gemini

  In your Archon `.env`:

  ```bash
  DEFAULT_AI_ASSISTANT=gemini
  GEMINI_API_KEY=your-key-from-ai.google.dev
  # Optional:
  # GEMINI_BIN_PATH=/custom/path/to/gemini
  # GEMINI_SDK_COMPAT=warn   # or strict / silent
  ```

  Start Archon normally. The Gemini provider registers at `DEFAULT_AI_ASSISTANT=gemini`, workflow-dispatch events match the Claude/Codex clients (verified by contract test in plan 10-05).

  ## Namespace discipline

  The adapter uses **only** `GEMINI_*` and `GEMINI_SDK_*` env var names — guaranteed no collisions with Archon's existing Claude/Codex vars (ARC-09). The `scripts/lint-env-namespace.sh` CI gate enforces this at every commit.

  ## Release gate

  Tagging `v1.0.0` of this SDK requires the local smoke-test script (`scripts/local-release-smoke.sh`) to exit 0 — which means the bundle applies cleanly AND one real `query()` succeeds against a real `gemini-cli` from inside Archon. See REL-07 in the roadmap.
  ```

  **5. Update `docs/.vitepress/config.mts` sidebar** — read the current file, then extend the sidebar to include migration + archon-integration entries:

  ```typescript
  sidebar: {
    '/ts/api/': typedocSidebar,
    '/ts/': [
      { text: 'Getting Started', items: [{ text: 'Quickstart', link: '/ts/quickstart' }] },
      { text: 'Migration', items: [
        { text: 'From Claude Agent SDK', link: '/ts/migration-claude' },
        { text: 'From Codex SDK', link: '/ts/migration-codex' },
      ]},
      { text: 'API Reference', link: '/ts/api/' },
    ],
    '/': [
      { text: 'Compat Matrix', link: '/compat-matrix' },
      { text: 'Known Issues', link: '/known-issues' },
      { text: 'Archon Integration', link: '/archon-integration' },
    ],
  },
  ```

  Also add to `nav`:
  ```typescript
  { text: 'Archon', link: '/archon-integration' },
  ```

  **6. Verify:** `pnpm docs:build` must exit 0 with all new pages rendered.
  </action>
  <verify>
    <automated>pnpm docs:build &amp;&amp; test -f docs/.vitepress/dist/ts/migration-claude.html &amp;&amp; test -f docs/.vitepress/dist/archon-integration.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/ts/migration-claude.md` exits 0
    - `test -f docs/ts/migration-codex.md` exits 0
    - `test -f docs-python/migration-claude.md` exits 0
    - `test -f docs-python/migration-codex.md` exits 0
    - `test -f docs/archon-integration.md` exits 0
    - `wc -l docs/ts/migration-claude.md | awk '{print $1}'` returns >= 60
    - `wc -l docs/ts/migration-codex.md | awk '{print $1}'` returns >= 60
    - `wc -l docs-python/migration-claude.md | awk '{print $1}'` returns >= 60
    - `wc -l docs-python/migration-codex.md | awk '{print $1}'` returns >= 60
    - `wc -l docs/archon-integration.md | awk '{print $1}'` returns >= 60
    - `grep -q "Pattern 1" docs/ts/migration-claude.md`
    - `grep -q "Pattern 5" docs/ts/migration-claude.md` (all 5 patterns covered)
    - `grep -q "Pattern 5" docs/ts/migration-codex.md`
    - `grep -q "DEFAULT_AI_ASSISTANT=gemini" docs/archon-integration.md`
    - `grep -q "pr-artifacts" docs/archon-integration.md`
    - `grep -q "GEMINI_API_KEY" docs/archon-integration.md`
    - `grep -q "migration-claude" docs/.vitepress/config.mts`
    - `grep -q "archon-integration" docs/.vitepress/config.mts`
    - `pnpm docs:build` exits 0
    - `test -f docs/.vitepress/dist/ts/migration-claude.html` exits 0
    - `test -f docs/.vitepress/dist/archon-integration.html` exits 0
  </acceptance_criteria>
  <done>Migration guides + Archon integration guide live; sidebar navigable; build green.</done>
</task>

</tasks>

<verification>
- Running `pnpm docs:typedoc && pnpm docs:build` produces a complete site.
- All 5 upstream issue references (#14180, #13388, #3485, #22970, #4945) grep-findable in docs/known-issues.yml.
- Migration guides explicitly cover 5 patterns each.
- Archon integration guide traces the apply sequence from pr-artifacts/README.md.
</verification>

<success_criteria>
- DOC-02: Quickstart end-to-end walkthrough exists in both TS and Python sections.
- DOC-04: Compat matrix page documents pinned range + GEMINI_SDK_COMPAT contract.
- DOC-05: Known-issues appendix with live upstream links, YAML-driven.
- DOC-06: Migration guides for Claude + Codex SDK, 5-pattern scope each.
- DOC-07: Archon integration guide with bundle-apply instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-site-compat-matrix-release/11-04-SUMMARY.md`.
</output>
