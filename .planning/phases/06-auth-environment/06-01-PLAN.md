---
phase: 06-auth-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ts/src/auth/resolveAuth.ts
  - ts/src/auth/index.ts
  - ts/src/auth/resolveAuth.spec.ts
  - ts/src/index.ts
autonomous: true
requirements: [AUT-01, AUT-02, AUT-03, AUT-04, AUT-06]
must_haves:
  truths:
    - "resolveAuth({GEMINI_API_KEY:'k'}) returns mode='api-key' with empty warnings"
    - "resolveAuth({GOOGLE_APPLICATION_CREDENTIALS:'/sa.json'}) returns mode='vertex-sa' with empty warnings"
    - "resolveAuth({GOOGLE_API_KEY:'g'}) returns mode='vertex-key' with empty warnings"
    - "resolveAuth({}) returns mode='adc' (no-env fallback) with empty warnings"
    - "resolveAuth with two modes set emits exactly one warning naming the winner + the full precedence chain"
    - "GOOGLE_CLOUD_PROJECT / _PROJECT_ID / _LOCATION flow through buildEnv unchanged regardless of mode"
  artifacts:
    - path: "ts/src/auth/resolveAuth.ts"
      provides: "pure resolveAuth() + AUTH_PRECEDENCE constant + AuthMode + ResolvedAuth types"
      min_lines: 40
    - path: "ts/src/auth/index.ts"
      provides: "barrel re-exporting resolveAuth, AUTH_PRECEDENCE, AuthMode, ResolvedAuth"
    - path: "ts/src/auth/resolveAuth.spec.ts"
      provides: "vitest suite covering 4 modes + multi-mode warning + GCP passthrough"
      min_lines: 80
  key_links:
    - from: "ts/src/auth/resolveAuth.spec.ts"
      to: "ts/src/auth/resolveAuth.ts"
      via: "import { resolveAuth, AUTH_PRECEDENCE } from './resolveAuth.js'"
      pattern: "from ['\"]\\./resolveAuth\\.js['\"]"
    - from: "ts/src/auth/resolveAuth.spec.ts"
      to: "ts/src/process/EnvBuilder.ts"
      via: "import { buildEnv } from '../process/EnvBuilder.js' for composition snapshot"
      pattern: "buildEnv\\("
    - from: "ts/src/index.ts"
      to: "ts/src/auth/index.ts"
      via: "re-export auth module for external consumers"
      pattern: "from ['\"]\\./auth/index\\.js['\"]"
---

<objective>
Ship the TypeScript `resolveAuth()` pure function — the Phase 6 diagnostic that inspects `process.env`, applies the documented precedence chain `GEMINI_API_KEY > GOOGLE_APPLICATION_CREDENTIALS > GOOGLE_API_KEY > ADC`, and returns the resolved `mode`, `envOverrides` (empty today; reserved for future per-call override), and a `warnings` list. No subprocess, no I/O, no public API growth beyond the new `auth/` barrel.

Purpose: Satisfies AUT-01/02/03/06 directly in TS; positions AUT-04 passthrough as a composition test with `buildEnv`. Plan 06-02 mirrors this in Python. Plan 06-03 wires it into `query()`.

Output:
- `ts/src/auth/resolveAuth.ts` — pure function + `AUTH_PRECEDENCE` exported constant (per RESEARCH Pitfall 3: single source of chain order)
- `ts/src/auth/index.ts` — barrel
- `ts/src/auth/resolveAuth.spec.ts` — 6+ tests covering all 4 modes + multi-mode warning + GCP passthrough snapshot
- `ts/src/index.ts` — add `export * from './auth/index.js'`
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auth-environment/06-CONTEXT.md
@.planning/phases/06-auth-environment/06-RESEARCH.md

@ts/src/process/EnvBuilder.ts
@ts/src/process/EnvBuilder.spec.ts
@ts/src/index.ts

<interfaces>
<!-- Contracts locked by CONTEXT.md and RESEARCH.md. Executor implements to these. -->

```typescript
// ts/src/auth/resolveAuth.ts
export type AuthMode = 'api-key' | 'vertex-sa' | 'vertex-key' | 'adc' | 'none';

export interface ResolvedAuth {
  mode: AuthMode;
  envOverrides: Record<string, string>;  // empty today; reserved for future
  warnings: string[];                    // non-empty when multiple modes configured
}

/** Locked precedence order — warning builder joins with ' > '; spec asserts against this constant. */
export const AUTH_PRECEDENCE: readonly string[] = [
  'GEMINI_API_KEY',
  'GOOGLE_APPLICATION_CREDENTIALS',
  'GOOGLE_API_KEY',
  'ADC',
];

export function resolveAuth(
  env: NodeJS.ProcessEnv,
  options?: Record<string, never>
): ResolvedAuth;
```

From ts/src/process/EnvBuilder.ts (existing, DO NOT MODIFY):
```typescript
export function buildEnv(overrides?: Record<string, string>): Record<string, string>;
// Allowlist already contains: GEMINI_API_KEY, GOOGLE_APPLICATION_CREDENTIALS,
// GOOGLE_API_KEY, GOOGLE_CLOUD_PROJECT, GOOGLE_CLOUD_PROJECT_ID, GOOGLE_CLOUD_LOCATION.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ts/src/auth/resolveAuth.ts + barrel</name>
  <read_first>
    - ts/src/process/EnvBuilder.ts (pattern to mirror: pure function, ALLOWED_KEYS-style const)
    - ts/src/errors/index.ts (barrel style)
    - .planning/phases/06-auth-environment/06-RESEARCH.md lines 150-250 (canonical resolveAuth implementation)
  </read_first>
  <files>ts/src/auth/resolveAuth.ts, ts/src/auth/index.ts, ts/src/index.ts</files>
  <behavior>
    - resolveAuth({GEMINI_API_KEY:'k'}) → {mode:'api-key', envOverrides:{}, warnings:[]}
    - resolveAuth({GOOGLE_APPLICATION_CREDENTIALS:'/sa.json'}) → {mode:'vertex-sa', envOverrides:{}, warnings:[]}
    - resolveAuth({GOOGLE_API_KEY:'g'}) → {mode:'vertex-key', envOverrides:{}, warnings:[]}
    - resolveAuth({}) → {mode:'adc', envOverrides:{}, warnings:[]}
    - resolveAuth({GEMINI_API_KEY:'k', GOOGLE_API_KEY:'g'}) → mode='api-key', warnings.length===1, warning contains 'GEMINI_API_KEY > GOOGLE_APPLICATION_CREDENTIALS > GOOGLE_API_KEY > ADC'
    - AUTH_PRECEDENCE === ['GEMINI_API_KEY','GOOGLE_APPLICATION_CREDENTIALS','GOOGLE_API_KEY','ADC']
  </behavior>
  <action>
    Create `ts/src/auth/resolveAuth.ts` implementing the interface locked in <interfaces> above.

    Concrete implementation requirements:
    1. Export `AuthMode` type literal union exactly: `'api-key' | 'vertex-sa' | 'vertex-key' | 'adc' | 'none'`.
    2. Export `ResolvedAuth` interface with fields `mode: AuthMode`, `envOverrides: Record<string,string>`, `warnings: string[]`.
    3. Export const `AUTH_PRECEDENCE` as `readonly string[]` with exact values `['GEMINI_API_KEY','GOOGLE_APPLICATION_CREDENTIALS','GOOGLE_API_KEY','ADC']`. Use `as const` or explicit `readonly string[]` type.
    4. Implement `resolveAuth(env, options?)`:
       - Build `configured` array by checking `env.GEMINI_API_KEY`, `env.GOOGLE_APPLICATION_CREDENTIALS`, `env.GOOGLE_API_KEY` with truthy check (`!!env.X`).
       - Winner = first configured mode or `'adc'` if array empty.
       - `envOverrides` MUST be `{}` (empty object literal) — DO NOT duplicate existing env vars; they already flow via EnvBuilder allowlist. This is per RESEARCH "Anti-Patterns to Avoid".
       - When `configured.length > 1`, push ONE warning string built using `AUTH_PRECEDENCE.join(' > ')` for the chain segment. Template exactly (TS template literal):
         ```
         [gemini-sdk] Multiple auth modes configured: ${names}.
         Using ${winnerName} per documented precedence:
           ${AUTH_PRECEDENCE.join(' > ')}.
         See docs/auth.md.
         ```
       - `names` = comma-space-joined env var names of configured entries in precedence order.
       - `winnerName` = env var name of the first configured entry (e.g. `'GEMINI_API_KEY'`).
       - When zero modes configured: `mode='adc'`, zero warnings (per CONTEXT.md "No info-level warning when ADC is the only resolved mode").
    5. Do NOT import from `../errors/` or any other module. Pure function — zero imports.
    6. Do NOT probe filesystem, do NOT read `process.env` directly (parameter only).
    7. The `options` parameter is reserved for future — accept and ignore. Type it as `Record<string, never>` or `Readonly<{}>`.
    8. `mode: 'none'` is declared in the union but UNREACHABLE via current API per RESEARCH Open Question #1. Add a JSDoc comment on the union member: `/** Reserved for future explicit opt-out (e.g. options.auth='off'); unreachable via current detection. */`.

    Create `ts/src/auth/index.ts`:
    ```typescript
    export { resolveAuth, AUTH_PRECEDENCE } from './resolveAuth.js';
    export type { AuthMode, ResolvedAuth } from './resolveAuth.js';
    ```

    Edit `ts/src/index.ts`: Add line `export * from './auth/index.js';` alongside existing module re-exports. Preserve all existing exports verbatim.
  </action>
  <verify>
    <automated>cd ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ts/src/auth/resolveAuth.ts` returns 0
    - `test -f ts/src/auth/index.ts` returns 0
    - `grep -c 'export const AUTH_PRECEDENCE' ts/src/auth/resolveAuth.ts` equals 1
    - `grep -c "'GEMINI_API_KEY'" ts/src/auth/resolveAuth.ts` ≥ 1 (chain literal or env check)
    - `grep -c "'GOOGLE_APPLICATION_CREDENTIALS'" ts/src/auth/resolveAuth.ts` ≥ 1
    - `grep -c "'GOOGLE_API_KEY'" ts/src/auth/resolveAuth.ts` ≥ 1
    - `grep -c 'export type AuthMode' ts/src/auth/resolveAuth.ts` equals 1
    - `grep -c "export \\* from './auth/index.js'" ts/src/index.ts` equals 1
    - `grep -c 'from ' ts/src/auth/resolveAuth.ts` equals 0 (zero imports — pure)
    - `cd ts && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Pure `resolveAuth` compiles, exports barrel + top-level re-export. Zero runtime dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ts/src/auth/resolveAuth.spec.ts covering AUT-01..06</name>
  <read_first>
    - ts/src/auth/resolveAuth.ts (just written)
    - ts/src/process/EnvBuilder.spec.ts (mirror vi.stubEnv snapshot-diff idiom)
    - .planning/phases/06-auth-environment/06-RESEARCH.md lines 250-290 (test pattern example)
  </read_first>
  <files>ts/src/auth/resolveAuth.spec.ts</files>
  <behavior>
    Test cases (each maps to a requirement):
    - "AUT-01 api-key only — winner=api-key, no warnings, buildEnv passes GEMINI_API_KEY through"
    - "AUT-02 vertex-sa only — winner=vertex-sa, no warnings, buildEnv passes GOOGLE_APPLICATION_CREDENTIALS through"
    - "AUT-03 vertex-key only — winner=vertex-key, no warnings, buildEnv passes GOOGLE_API_KEY through"
    - "AUT-05 zero env — winner=adc, no warnings, no envOverrides"
    - "AUT-06 api-key + vertex-sa — winner=api-key, exactly one warning containing full chain"
    - "AUT-06 all four explicit modes — winner=api-key (highest precedence), warning names all losers"
    - "AUT-04 GCP project vars flow through buildEnv regardless of mode"
    - "AUTH_PRECEDENCE constant equals ['GEMINI_API_KEY','GOOGLE_APPLICATION_CREDENTIALS','GOOGLE_API_KEY','ADC']"
  </behavior>
  <action>
    Create `ts/src/auth/resolveAuth.spec.ts` using vitest. Mirror the structure of `ts/src/process/EnvBuilder.spec.ts`:

    1. Import via ESM with `.js` extension suffix per project convention:
       ```typescript
       import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
       import { resolveAuth, AUTH_PRECEDENCE } from './resolveAuth.js';
       import { buildEnv } from '../process/EnvBuilder.js';
       ```

    2. Use `beforeEach(() => { vi.unstubAllEnvs(); })` and `afterEach(() => { vi.unstubAllEnvs(); })` to guarantee isolation. Also unstub `GEMINI_API_KEY`, `GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_API_KEY`, `GOOGLE_CLOUD_PROJECT`, `GOOGLE_CLOUD_PROJECT_ID`, `GOOGLE_CLOUD_LOCATION` explicitly via `vi.stubEnv(key, '')` then delete — because `vi.unstubAllEnvs` restores original state; tests should PASS IN `process.env` free of these vars. If any of these are set in the host's real env (e.g. developer workstation), explicitly `vi.stubEnv(k, '')` in each test's setup.

    3. Eight tests required (listed in <behavior> above). For each, call `resolveAuth(process.env)` and assert on the returned object. For AUT-01..03, also call `buildEnv(r.envOverrides)` and assert the expected var appears while others don't.

    4. The multi-mode regex assertion MUST use the exported constant, not a hardcoded string (RESEARCH Pitfall 3):
       ```typescript
       expect(r.warnings[0]).toContain(AUTH_PRECEDENCE.join(' > '));
       expect(r.warnings[0]).toContain('GEMINI_API_KEY'); // winner name
       ```

    5. For AUT-04 passthrough: stub `GEMINI_API_KEY='k'` + `GOOGLE_CLOUD_PROJECT='proj'` + `GOOGLE_CLOUD_LOCATION='us-central1'`, call `resolveAuth` then `buildEnv(r.envOverrides)`, assert `env.GOOGLE_CLOUD_PROJECT === 'proj'` and `env.GOOGLE_CLOUD_LOCATION === 'us-central1'`.

    6. For the AUTH_PRECEDENCE constant test, use `expect(AUTH_PRECEDENCE).toEqual([...])` with the exact literal array.

    7. Test names MUST follow vitest `it()` style matching Python parity convention from Phase 5: description is the first string argument. Python mirror in plan 06-02 will use same descriptions in docstrings.
  </action>
  <verify>
    <automated>cd ts && pnpm exec vitest run src/auth/resolveAuth.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ts/src/auth/resolveAuth.spec.ts` returns 0
    - `grep -c "it(" ts/src/auth/resolveAuth.spec.ts` ≥ 8
    - `grep -c 'AUTH_PRECEDENCE' ts/src/auth/resolveAuth.spec.ts` ≥ 2
    - `grep -c 'vi.stubEnv' ts/src/auth/resolveAuth.spec.ts` ≥ 4
    - `grep -c "from '../process/EnvBuilder" ts/src/auth/resolveAuth.spec.ts` ≥ 1
    - `cd ts && pnpm exec vitest run src/auth/resolveAuth.spec.ts` passes (exit 0) with ≥ 8 tests
    - `cd ts && pnpm exec vitest run src/auth/resolveAuth.spec.ts 2>&1 | grep -E "[0-9]+ passed"` shows all passing
  </acceptance_criteria>
  <done>Vitest runs ≥ 8 green tests covering AUT-01..06 behaviors. No host-env dependency — runs deterministic in CI.</done>
</task>

</tasks>

<verification>
- `cd ts && pnpm exec tsc --noEmit` — no type errors
- `cd ts && pnpm exec vitest run src/auth` — all tests green
- `grep -rn 'auth login' ts/src/auth/ 2>/dev/null | wc -l` equals 0 (plan 06-04 linter forward-compatibility)
- Import the module from outside: `cd ts && node --input-type=module -e "import('./dist/auth/index.js').catch(e=>console.error(e.message))"` (after a prior build) — smoke-only, non-blocking
</verification>

<success_criteria>
- All 5 must_haves truths verifiable via `pnpm exec vitest run src/auth`
- AUTH_PRECEDENCE exported and referenced by both impl and spec (single source of chain order)
- Zero imports in `resolveAuth.ts` (pure function enforced by grep)
- Top-level re-export present so Phase 10 Archon adapter can consume `resolveAuth` if needed
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-environment/06-01-SUMMARY.md` capturing: decisions made, anomalies, file list, test count, deferred items (e.g. `mode: 'none'` reachability Open Question).
</output>
