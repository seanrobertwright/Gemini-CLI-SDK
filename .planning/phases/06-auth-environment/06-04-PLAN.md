---
phase: 06-auth-environment
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lint-auth-login.sh
  - .github/workflows/ci.yml
  - docs/auth.md
autonomous: true
requirements: [AUT-05, AUT-08, AUT-09]
must_haves:
  truths:
    - "scripts/lint-auth-login.sh exists, is executable, exits 0 on current clean tree, exits non-zero if 'auth login' appears in ts/src or python/src"
    - "CI parity job runs scripts/lint-auth-login.sh as a blocking step"
    - "docs/auth.md documents precedence chain, references discussion #22970 + ToS rationale, and explicitly states there is no GOOGLE_AUTH_TOKEN passthrough"
    - "Lint scope is source-only: tests and docs may reference 'auth login' prose without tripping the linter"
  artifacts:
    - path: "scripts/lint-auth-login.sh"
      provides: "Bash linter matching `lint-errors.sh` shape; grep -E only; set -euo pipefail"
      min_lines: 20
    - path: ".github/workflows/ci.yml"
      provides: "Parity job step invoking bash scripts/lint-auth-login.sh"
    - path: "docs/auth.md"
      provides: "Precedence chain, AUT-08 rationale (discussion #22970 + ToS), AUT-09 rationale (GOOGLE_AUTH_TOKEN non-existence)"
      min_lines: 60
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/lint-auth-login.sh"
      via: "run: bash scripts/lint-auth-login.sh step in parity job"
      pattern: "lint-auth-login\\.sh"
    - from: "docs/auth.md"
      to: "https://github.com/google-gemini/gemini-cli/discussions/22970"
      via: "markdown link citing AUT-08 rationale"
      pattern: "22970"
    - from: "docs/auth.md"
      to: "ts/src/process/EnvBuilder.ts"
      via: "prose referencing the allowlist as the enforcement mechanism for AUT-09"
      pattern: "ALLOWED_KEYS|allowlist"
---

<objective>
Ship the three "no-code" Phase 6 artifacts: the CI linter that enforces AUT-05 (never automate OAuth login), the CI workflow wiring so the linter blocks merge, and `docs/auth.md` covering AUT-08 + AUT-09 documentation requirements. These three deliverables are independent of plans 06-01/02/03 and can land in Wave 1 in parallel.

Purpose: Satisfies AUT-05 (enforcement mechanism), AUT-08 (documented ToS/#22970 rationale), AUT-09 (documented GOOGLE_AUTH_TOKEN non-existence). The linter is the literal grep SC-4 from ROADMAP.

Output:
- `scripts/lint-auth-login.sh` — bash linter, `set -euo pipefail`, grep -E only, exits 0 clean / 1 on match
- `.github/workflows/ci.yml` — parity job gains one `- name: Lint auth login prohibition` step
- `docs/auth.md` — new file: precedence chain, AUT-08 rationale, AUT-09 rationale, pointer to `resolveAuth` module
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-auth-environment/06-CONTEXT.md
@.planning/phases/06-auth-environment/06-RESEARCH.md

@scripts/lint-errors.sh
@.github/workflows/ci.yml
@ts/src/process/EnvBuilder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/lint-auth-login.sh + wire into CI</name>
  <read_first>
    - scripts/lint-errors.sh (template to mirror — set -euo pipefail, REPO_ROOT pattern, grep -E only)
    - .github/workflows/ci.yml (locate the parity job and the existing lint-errors.sh step)
    - .planning/phases/06-auth-environment/06-RESEARCH.md lines 280-310 (canonical lint script + CI yaml)
  </read_first>
  <files>scripts/lint-auth-login.sh, .github/workflows/ci.yml</files>
  <action>
    **Create `scripts/lint-auth-login.sh`** with content exactly:
    ```bash
    #!/usr/bin/env bash
    # scripts/lint-auth-login.sh — AUT-05 + Phase 6 SC-4 enforcement.
    # Fails if the literal string 'auth login' appears in ts/src or python/src.
    # Scope is source-only: tests and docs may legitimately mention 'auth login' in
    # prohibition prose (e.g. "SDK NEVER calls auth login"). See .planning/phases/
    # 06-auth-environment/06-CONTEXT.md §"ADC pickup + AUT-09 + CI linter" for rationale.
    #
    # Compat: macOS BSD grep (no -P). Git Bash on Windows. Use grep -E only.
    #
    set -euo pipefail

    REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
    cd "$REPO_ROOT"

    # Source-only scope
    matches="$(grep -rn 'auth login' ts/src python/src 2>/dev/null || true)"
    if [ -n "$matches" ]; then
      echo "[lint-auth-login] FAIL: 'auth login' must not appear in SDK source." >&2
      echo "$matches" >&2
      echo "SDK NEVER automates interactive OAuth (AUT-05 / Phase 6 SC-4)." >&2
      exit 1
    fi
    echo "[lint-auth-login] OK: no interactive OAuth automation detected in ts/src or python/src."
    ```

    Make executable: `chmod +x scripts/lint-auth-login.sh` (note: commit with `git update-index --chmod=+x` if on Windows to record the exec bit properly).

    **Edit `.github/workflows/ci.yml`:** Locate the parity job (should contain an existing `bash scripts/lint-errors.sh` step or similar). Add a sibling step immediately after it:
    ```yaml
          - name: Lint auth login prohibition
            run: bash scripts/lint-auth-login.sh
    ```
    Place this step in the SAME job that currently runs `scripts/lint-errors.sh`. Use the same indentation level. Do not alter any other workflow steps, matrix definitions, or cache keys.

    **Self-test the linter before commit:**
    - Run `bash scripts/lint-auth-login.sh` from repo root — MUST exit 0 on the current tree (no `auth login` expected anywhere in `ts/src` or `python/src` as of this plan).
    - Run a negative self-test: temporarily `echo '// auth login test' > /tmp/fake.ts` then run `bash -c "cd /tmp && REPO_ROOT=/tmp bash D:/repos/Gemini-SDK/scripts/lint-auth-login.sh"` — does NOT need to pass; this is just sanity that matches would trip the linter. Clean up /tmp/fake.ts afterwards. (This step is optional — not required in acceptance criteria.)
  </action>
  <verify>
    <automated>bash scripts/lint-auth-login.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/lint-auth-login.sh` returns 0
    - `test -x scripts/lint-auth-login.sh` returns 0 (or git tracks it as executable — check `git ls-files --stage scripts/lint-auth-login.sh` starts with `100755`)
    - `grep -c 'set -euo pipefail' scripts/lint-auth-login.sh` equals 1
    - `grep -c "grep -rn 'auth login' ts/src python/src" scripts/lint-auth-login.sh` equals 1
    - `grep -c 'lint-auth-login.sh' .github/workflows/ci.yml` ≥ 1
    - `bash scripts/lint-auth-login.sh` exits 0 (clean tree)
    - Negative test: `echo "gemini auth login" > /tmp/lint_test_dir_ts_src_fake.ts 2>/dev/null; bash -c 'cd $(mktemp -d) && mkdir -p ts/src python/src && echo "auth login" > ts/src/x.ts && REPO_ROOT=$(pwd) bash '"$PWD"'/scripts/lint-auth-login.sh; echo exit=$?' 2>&1 | grep -q 'exit=1'` — optional; don't gate on this if sandbox complicates it
    - No other CI workflow files modified beyond `.github/workflows/ci.yml`
  </acceptance_criteria>
  <done>Linter exists, is executable, exits 0 on clean tree, is wired into the CI parity job as a blocking step.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/auth.md</name>
  <read_first>
    - ts/src/process/EnvBuilder.ts (allowlist contents — reference ALLOWED_KEYS by name)
    - .planning/phases/06-auth-environment/06-CONTEXT.md §"ADC pickup + AUT-09 + CI linter" (verbatim rationale for AUT-08 / AUT-09)
    - .planning/REQUIREMENTS.md AUT-08 + AUT-09 text
    - Upstream: https://github.com/google-gemini/gemini-cli/discussions/22970 (do NOT fetch; just cite by URL)
  </read_first>
  <files>docs/auth.md</files>
  <action>
    Create `docs/auth.md` as a new markdown file. Required sections and content (headings must match exactly so downstream doc-site generators can anchor):

    ```markdown
    # Authentication

    This SDK is a transparent wrapper around `gemini-cli` — it does not implement its own auth layer. Instead, `resolveAuth()` inspects your environment, applies a documented precedence chain, and passes the result through to `gemini-cli` via `EnvBuilder`'s allowlist.

    ## Precedence Chain

    When multiple auth modes are configured simultaneously, the SDK resolves the winner using this fixed order:

    1. `GEMINI_API_KEY` — canonical default (AUT-01)
    2. `GOOGLE_APPLICATION_CREDENTIALS` — Vertex AI via service account JSON (AUT-02)
    3. `GOOGLE_API_KEY` — alternative Vertex path (AUT-03)
    4. ADC / Sign-in-with-Google — transparent fallback (AUT-05)

    If more than one is set, `resolveAuth()` emits a single `console.warn` (TypeScript) or `warnings.warn(UserWarning)` (Python) per `query()` call, naming the winner and reprinting the full chain. Single-mode configurations emit no warnings.

    The chain constant is exported as `AUTH_PRECEDENCE` from `ts/src/auth/index.ts` and `python/src/gemini_sdk/auth/__init__.py`, so tests and downstream callers can reference it without hardcoding.

    ## Why API Key Is the Default

    Google's recommended default for headless/SDK contexts is `GEMINI_API_KEY`. See:

    - [gemini-cli discussion #22970](https://github.com/google-gemini/gemini-cli/discussions/22970) — March 2026 Google routing change and the rationale for API-key-canonical behavior.
    - Google Gemini CLI FAQ ToS note — OAuth flows carry Terms of Service risk for automated/SDK usage; API keys do not.

    The SDK **never** invokes `gemini auth login` or any interactive OAuth entrypoint. This is enforced by `scripts/lint-auth-login.sh` in CI — if the string `auth login` ever appears in `ts/src` or `python/src`, the parity job fails merge. (AUT-05)

    ## Vertex AI

    To use Vertex AI, set either:

    - `GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json` — recommended for production.
    - `GOOGLE_API_KEY=<your-vertex-key>` — alternative path.

    For Vertex project/region scoping, the SDK passes through the following env vars unconditionally (regardless of auth mode):

    - `GOOGLE_CLOUD_PROJECT`
    - `GOOGLE_CLOUD_PROJECT_ID`
    - `GOOGLE_CLOUD_LOCATION`

    These flow through `EnvBuilder`'s allowlist; `resolveAuth()` does not strip them. (AUT-04)

    ## ADC (Application Default Credentials)

    If you have already authenticated via `gcloud auth application-default login` (or an equivalent ADC-producing mechanism) and no API-key / service-account env var is set, `gemini-cli` will transparently pick up your ADC credentials. The SDK does **not** probe `~/.config/gcloud` or invoke `gcloud` itself — it is a do-nothing fallback. (AUT-05)

    ## No `GOOGLE_AUTH_TOKEN` Passthrough

    There is **no** env-var path for supplying a pre-obtained short-lived OAuth access token. `GOOGLE_AUTH_TOKEN` is deliberately absent from the `EnvBuilder` `ALLOWED_KEYS` allowlist — its absence IS the enforcement.

    If you have a bearer token and no other credential, substitute one of:

    - Service account JSON (`GOOGLE_APPLICATION_CREDENTIALS`)
    - Google Cloud API key (`GOOGLE_API_KEY`)
    - ADC (run `gcloud auth application-default login` once on the host)

    Attempting to forward `GOOGLE_AUTH_TOKEN` through `options.env` will not help — `gemini-cli` does not read this variable. This is documented here rather than enforced via runtime check because the allowlist is the architectural gate. (AUT-09)

    ## Typed Auth Errors

    Auth failures surface through the typed `AuthError` hierarchy generated from `spec/errors.yaml` (Phase 5):

    - `AuthError.NotConfigured` — no credentials detected
    - `AuthError.Forbidden403` — authenticated but forbidden (e.g. model not accessible to the project)
    - `AuthError.Expired` — credential expired mid-session
    - `AuthError.ToSViolation` — ToS-suspended account

    All four are non-retryable (`.retryable === false`) and map to Archon's `auth` retry bucket. The classifier lives in `ts/src/errors/ErrorMapper.ts` / `python/src/gemini_sdk/errors/error_mapper.py`. (AUT-07)

    ## References

    - Requirements: [AUT-01 through AUT-09](../.planning/REQUIREMENTS.md#authentication)
    - Implementation: `ts/src/auth/resolveAuth.ts`, `python/src/gemini_sdk/auth/resolve_auth.py`
    - Enforcement: `scripts/lint-auth-login.sh` (AUT-05), `EnvBuilder` allowlist (AUT-09)
    - Upstream: https://github.com/google-gemini/gemini-cli/discussions/22970
    ```

    If the file needs to reference additional upstream docs, the link to `https://github.com/google-gemini/gemini-cli/blob/main/docs/get-started/authentication.md` may be added under References. Do NOT fetch the URL at author time — just cite it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('docs/auth.md','utf8');const checks=['AUTH_PRECEDENCE','GEMINI_API_KEY','GOOGLE_APPLICATION_CREDENTIALS','GOOGLE_API_KEY','GOOGLE_AUTH_TOKEN','22970','AUT-05','AUT-08','AUT-09','lint-auth-login.sh','AuthError'];const miss=checks.filter(c=>!t.includes(c));if(miss.length){console.error('missing:',miss);process.exit(1)}console.log('docs/auth.md: all required strings present')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/auth.md` returns 0
    - `wc -l docs/auth.md | awk '{print $1}'` ≥ 60
    - `grep -c 'GEMINI_API_KEY > GOOGLE_APPLICATION_CREDENTIALS > GOOGLE_API_KEY > ADC' docs/auth.md` ≥ 0 (chain may be prose-formatted; see line below instead)
    - `grep -c 'GEMINI_API_KEY' docs/auth.md` ≥ 2
    - `grep -c 'GOOGLE_APPLICATION_CREDENTIALS' docs/auth.md` ≥ 2
    - `grep -c 'GOOGLE_API_KEY' docs/auth.md` ≥ 2
    - `grep -c 'GOOGLE_AUTH_TOKEN' docs/auth.md` ≥ 1
    - `grep -c '22970' docs/auth.md` ≥ 1
    - `grep -cE 'AUT-05|AUT-08|AUT-09' docs/auth.md` ≥ 3
    - `grep -c 'lint-auth-login' docs/auth.md` ≥ 1
    - `grep -c 'AuthError' docs/auth.md` ≥ 1
    - `grep -c 'AUTH_PRECEDENCE' docs/auth.md` ≥ 1
    - `grep -c 'allowlist' docs/auth.md` ≥ 1 (AUT-09 rationale references the allowlist as the gate)
  </acceptance_criteria>
  <done>docs/auth.md exists with all required sections and string citations. Future doc-site build (Phase 11) can render this directly.</done>
</task>

</tasks>

<verification>
- `bash scripts/lint-auth-login.sh` — exits 0 on clean tree
- `grep -c 'lint-auth-login.sh' .github/workflows/ci.yml` ≥ 1
- `node -e "const t=require('fs').readFileSync('docs/auth.md','utf8'); ['22970','GOOGLE_AUTH_TOKEN','AuthError','AUTH_PRECEDENCE'].forEach(s=>{if(!t.includes(s)) throw new Error(s+' missing')})"` exits 0
</verification>

<success_criteria>
- All 4 must_haves truths verifiable via the automated commands
- AUT-05 enforced in CI (grep-based, source-only scope)
- AUT-08 + AUT-09 documented with citations
- Zero changes to source code (plans 06-01/02/03 own that)
- CI workflow diff is minimal: exactly one new step added to the parity job
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-environment/06-04-SUMMARY.md` capturing: files created, CI step added, any doc citations that needed adjustment (e.g. if upstream URL moved).
</output>
