---
phase: 10-archon-adapter-ts-only
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lint-env-namespace.sh
  - scripts/lint-env-namespace.spec.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [ARC-09]
must_haves:
  truths:
    - "scripts/lint-env-namespace.sh exits non-zero when adapter-archon/src/** references env vars outside the allowlist"
    - "scripts/lint-env-namespace.sh exits 0 when adapter-archon/src/** references only allowlisted env vars"
    - "CI runs the linter on every push and PR and blocks merge on failure"
  artifacts:
    - path: scripts/lint-env-namespace.sh
      provides: "Grep-based env-namespace CI linter for adapter-archon/src/**"
      contains: "GEMINI_SDK_"
    - path: scripts/lint-env-namespace.spec.sh
      provides: "Self-test for the linter using temp fixture directories"
    - path: .github/workflows/ci.yml
      provides: "CI wiring that runs lint-env-namespace on every PR"
      contains: "lint-env-namespace"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/lint-env-namespace.sh
      via: "bash scripts/lint-env-namespace.sh invocation in a required CI job"
      pattern: "lint-env-namespace"
---

<objective>
Ship the env-var namespace linter and wire it into CI. The linter scans `adapter-archon/src/**` only, greps for `process.env.NAME` and `env['NAME']` patterns, subtracts an explicit allowlist, and fails with a non-zero exit if any non-namespaced var appears. CI runs this on every PR as a blocking job.

Purpose: ARC-09 explicitly requires a grep-based linter that fails merge. Independent of the adapter implementation — writing it in Wave 1 means every adapter file from plan 10-03 onward is checked immediately.
Output: `scripts/lint-env-namespace.sh`, a self-test `scripts/lint-env-namespace.spec.sh`, and a new CI job in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-archon-adapter-ts-only/10-CONTEXT.md
@.planning/phases/10-archon-adapter-ts-only/10-RESEARCH.md

<interfaces>
Reference: existing shell-lint pattern from scripts/lint-auth-login.sh (Phase 6):
- Shebang `#!/usr/bin/env bash`, `set -euo pipefail`
- REPO_ROOT via `cd "$(dirname "$0")/.."`
- Compat note: macOS BSD grep, no -P; Git Bash on Windows; use -E only
- Scope declared in header comment; failure echoes rationale + exits 1

Allowlist (from CONTEXT.md "Env-var namespace linter (ARC-09)"):
  GEMINI_*, GEMINI_SDK_*, PATH, HOME, USERPROFILE, TMPDIR, TEMP, TMP, NODE_ENV, DEBUG
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lint-env-namespace.sh plus self-test spec</name>
  <files>scripts/lint-env-namespace.sh, scripts/lint-env-namespace.spec.sh</files>
  <read_first>
    - D:/repos/Gemini-SDK/scripts/lint-auth-login.sh — mirror shebang, set-flags, REPO_ROOT pattern, Git-Bash-safe grep usage
    - D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/10-RESEARCH.md — section "scripts/lint-env-namespace.sh skeleton" for bash starting point
    - D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/10-CONTEXT.md — decisions "Env-var namespace linter (ARC-09)" for allowlist + scope
  </read_first>
  <behavior>
    - spec Test A (pass): fixture dir uses only GEMINI_API_KEY, GEMINI_SDK_FOO, NODE_ENV, DEBUG, PATH -> linter exits 0
    - spec Test B (fail, dot form): fixture has process.env.OPENAI_API_KEY -> linter exits 1, stderr contains OPENAI_API_KEY
    - spec Test C (fail, bracket form): fixture has process.env['AWS_SECRET'] -> linter exits 1, stderr contains AWS_SECRET
    - spec Test D (scope isolation): scope override env var controls which directory is scanned; default scope is adapter-archon/src
  </behavior>
  <action>
    Create scripts/lint-env-namespace.sh with this exact body (adjusted from RESEARCH skeleton; the LINT_ENV_NS_SCOPE override makes the script self-testable):

    ```
    #!/usr/bin/env bash
    # scripts/lint-env-namespace.sh - ARC-09 enforcement.
    # Fails if adapter-archon/src/** references any env var outside the
    # GEMINI_* / GEMINI_SDK_* / platform-essential allowlist.
    # Scope: adapter-archon/src/** only. SDK code (ts/src/**) is out of scope.
    # Compat: macOS BSD grep (no -P). Git Bash on Windows. Use grep -E only.
    set -euo pipefail

    SCOPE_DIR="${LINT_ENV_NS_SCOPE:-$(cd "$(dirname "$0")/.." && pwd)/adapter-archon/src}"

    if [ ! -d "$SCOPE_DIR" ]; then
      echo "[lint-env-namespace] SKIP: $SCOPE_DIR does not exist yet."
      exit 0
    fi

    ALLOWLIST='^(GEMINI_|GEMINI_SDK_|PATH|HOME|USERPROFILE|TMPDIR|TEMP|TMP|NODE_ENV|DEBUG)'

    NAMES=$(grep -rhE "process\.env\.[A-Z_]{3,}|process\.env\[['\"][A-Z_]{3,}['\"]\]" \
      "$SCOPE_DIR" --include="*.ts" 2>/dev/null \
      | grep -oE "[A-Z_]{3,}" \
      | sort -u || true)

    BAD=$(echo "$NAMES" | grep -vE "$ALLOWLIST" || true)

    if [ -n "${BAD// /}" ]; then
      echo "[lint-env-namespace] FAIL: non-namespaced env vars in $SCOPE_DIR:" >&2
      echo "$BAD" >&2
      echo "Allowed prefixes: GEMINI_*, GEMINI_SDK_*; platform: PATH, HOME, USERPROFILE, TMPDIR, TEMP, TMP, NODE_ENV, DEBUG." >&2
      exit 1
    fi
    echo "[lint-env-namespace] OK: all env-var references in $SCOPE_DIR are namespaced."
    ```

    Create scripts/lint-env-namespace.spec.sh - builds fixture dirs with mktemp and invokes the linter via LINT_ENV_NS_SCOPE, asserting the three behaviors above (pass, fail-dot, fail-bracket). Exit 0 on all green; exit non-zero with message on any divergence.

    Make both files executable (chmod +x).
  </action>
  <verify>
    <automated>bash D:/repos/Gemini-SDK/scripts/lint-env-namespace.spec.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/lint-env-namespace.sh || test -f scripts/lint-env-namespace.sh` exits 0
    - `grep -q "GEMINI_SDK_" scripts/lint-env-namespace.sh` exits 0
    - `grep -q "LINT_ENV_NS_SCOPE" scripts/lint-env-namespace.sh` exits 0
    - `grep -q "set -euo pipefail" scripts/lint-env-namespace.sh` exits 0
    - `bash scripts/lint-env-namespace.spec.sh` exits 0 and stdout contains "ALL OK" (or equivalent confirmation)
    - `bash scripts/lint-env-namespace.sh` exits 0 against the real repo (adapter-archon/src/ is still empty/stub, linter SKIPs or passes)
  </acceptance_criteria>
  <done>Linter script exists, self-test passes all three cases (allowlisted-pass, dot-form-fail, bracket-form-fail), and running the linter against the current repo exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire lint-env-namespace into CI as a blocking job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - D:/repos/Gemini-SDK/.github/workflows/ci.yml — existing matrix + parity job structure to mirror
    - D:/repos/Gemini-SDK/scripts/lint-env-namespace.sh (just written) — invocation pattern
  </read_first>
  <action>
    Add a new top-level job `lint-env-namespace` to .github/workflows/ci.yml. It must:
    - Run on `ubuntu-latest`
    - Trigger on the same push/pull_request events as the existing `test` job
    - Steps: `actions/checkout@v4`, then `run: bash scripts/lint-env-namespace.sh`
    - Be a required job (no `continue-on-error`, no `if:` gate that would skip it on PRs)
    - Also run the self-test in the same job: `run: bash scripts/lint-env-namespace.spec.sh`

    Place the new job alongside the existing `parity` job. Do NOT modify the existing matrix `test` job or `parity` job. Do NOT add the job to branch-protection-configuration files (those are managed outside the repo).

    Additionally: verify `scripts/lint-env-namespace.sh` is also invoked as part of an existing job if and only if that prevents duplicate runs — preferred shape is a standalone job for clear CI status reporting. Do not skip on Windows/macOS — the script is bash and Linux-only execution is sufficient per CONTEXT.md §"Env-var namespace linter".
  </action>
  <verify>
    <automated>grep -q "lint-env-namespace" D:/repos/Gemini-SDK/.github/workflows/ci.yml &amp;&amp; grep -q "bash scripts/lint-env-namespace.sh" D:/repos/Gemini-SDK/.github/workflows/ci.yml &amp;&amp; grep -q "bash scripts/lint-env-namespace.spec.sh" D:/repos/Gemini-SDK/.github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a job whose key or `name:` is `lint-env-namespace` (or similar ID containing that substring)
    - The job runs `bash scripts/lint-env-namespace.sh` AND `bash scripts/lint-env-namespace.spec.sh` as discrete steps
    - The job does NOT contain `continue-on-error: true`
    - The existing matrix `test` job and `parity` job are unchanged (no diff in their step bodies; confirm via targeted grep)
    - `actionlint` or `yamllint` (if installed) reports no syntax errors; otherwise at minimum `python -c "import yaml, sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
  </acceptance_criteria>
  <done>CI YAML has a required blocking job that runs the linter and its self-test on every PR; YAML parses; existing jobs untouched.</done>
</task>

</tasks>

<verification>
- `bash scripts/lint-env-namespace.sh` exits 0 on the current tree
- `bash scripts/lint-env-namespace.spec.sh` exits 0 with all 3 cases passing
- `.github/workflows/ci.yml` parses and contains the new job with both script invocations
</verification>

<success_criteria>
- Linter exists, self-tests, and runs green on the current repo (adapter-archon/src/ stub)
- CI YAML has a new required blocking job wired to the linter
- Linter scope is strictly adapter-archon/src/** (SDK code not affected)
</success_criteria>

<output>
After completion, create `.planning/phases/10-archon-adapter-ts-only/10-02-SUMMARY.md`
</output>
