---
phase: 10-archon-adapter-ts-only
plan: 04
type: execute
wave: 3
depends_on: ["10-archon-adapter-ts-only-03"]
files_modified:
  - adapter-archon/src/capabilities.ts
  - adapter-archon/src/provider.ts
  - adapter-archon/src/registration.ts
  - adapter-archon/src/index.ts
  - adapter-archon/src/provider.spec.ts
autonomous: true
requirements: [ARC-01, ARC-02, ARC-03, ARC-04, ARC-06]
must_haves:
  truths:
    - "GeminiProvider implements IAgentProvider (structural type check green)"
    - "GeminiProvider.getType() returns 'gemini'"
    - "GeminiProvider.getCapabilities() returns the GEMINI_CAPABILITIES object"
    - "sendQuery signature: (prompt: string, cwd: string, resumeSessionId?: string, options?: SendQueryOptions) => AsyncGenerator<MessageChunk>"
    - "sendQuery emits one workflow_dispatch MessageChunk per SDK tool chunk (with workerConversationId: '' and workflowName = toolName)"
    - "Package source-publishes .ts (no build artifact in import path); node resolves @gemini-sdk/adapter-archon to src/index.ts"
    - "provider.ts line count is <= 250 (<= 200 stretch target)"
  artifacts:
    - path: adapter-archon/src/capabilities.ts
      provides: "GEMINI_CAPABILITIES: ProviderCapabilities"
      contains: "GEMINI_CAPABILITIES"
    - path: adapter-archon/src/provider.ts
      provides: "GeminiProvider class implementing IAgentProvider"
      contains: "class GeminiProvider"
    - path: adapter-archon/src/registration.ts
      provides: "registerGeminiProvider() for the Archon registry"
      contains: "registerGeminiProvider"
    - path: adapter-archon/src/index.ts
      provides: "Barrel export of GeminiProvider, registerGeminiProvider, GEMINI_CAPABILITIES"
    - path: adapter-archon/src/provider.spec.ts
      provides: "Unit tests: getType, getCapabilities, sendQuery delegation, workflow_dispatch emission"
  key_links:
    - from: adapter-archon/src/provider.ts
      to: adapter-archon/src/options-translator.ts
      via: "imports translateOptions / translateChunk / warnIgnoredOptions"
      pattern: "from ['\"]\\./options-translator"
    - from: adapter-archon/src/provider.ts
      to: "@gemini-sdk/core"
      via: "imports query()"
      pattern: "import \\{ query \\} from ['\"]@gemini-sdk/core"
    - from: adapter-archon/src/index.ts
      to: adapter-archon/src/provider.ts
      via: "re-exports GeminiProvider"
      pattern: "export \\{ GeminiProvider \\}"
---

<objective>
Ship the adapter proper: `capabilities.ts` (GEMINI_CAPABILITIES constant), `provider.ts` (GeminiProvider class - ~80-120 LOC), `registration.ts` (registerGeminiProvider shell; no real Archon registry to hit during standalone dev, so the function body wraps a local stub but exports the name), and the barrel `index.ts`. Unit tests cover getType, getCapabilities, sendQuery delegation, and workflow_dispatch emission cadence.

Purpose: Closes ARC-01/02/03/04/06. provider.ts stays a thin shim by delegating all translation to plan 10-03's pure functions. registration.ts lives here (not in plan 10-05) because the registry wiring target is the Archon clone's registry.ts, which is a 1-line edit packaged in the PR artifact in plan 10-05 — the local registration function exists so `packages/providers/src/community/gemini/index.ts` can export it for Archon to call.
Output: 4 source files + 1 spec file, all typechecking, all tests green.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-archon-adapter-ts-only/10-CONTEXT.md
@.planning/phases/10-archon-adapter-ts-only/10-RESEARCH.md
@.planning/phases/10-archon-adapter-ts-only/10-01-SUMMARY.md
@.planning/phases/10-archon-adapter-ts-only/10-03-SUMMARY.md
@adapter-archon/src/types.ts
@adapter-archon/src/options-translator.ts

<interfaces>
SDK exports (from @gemini-sdk/core):
```typescript
export function query(options: QueryOptions): AsyncGenerator<MessageChunk>;
// MessageChunk variants include ToolChunk { type:'tool', toolName, toolId, parameters }
```

Archon registration pattern (from RESEARCH §Pattern 5, packages/providers/src/community/pi/registration.ts):
```typescript
// Archon registry provides (imported at PR time from ../../registry.js):
//   registerProvider(config: ProviderRegistration): void
//   isRegisteredProvider(id: string): boolean
// During standalone dev, we cannot import these. Make the signature
// independent so the file can be dropped into Archon's source tree with
// a one-line import header swap.
```

GEMINI_CAPABILITIES (from RESEARCH §Pattern 4):
  sessionResume: true, mcp: true, hooks: false, skills: false, agents: false,
  toolRestrictions: true, structuredOutput: true, envInjection: true,
  costControl: false, effortControl: false, thinkingControl: false,
  fallbackModel: false, sandbox: false
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement capabilities.ts, provider.ts, registration.ts, index.ts</name>
  <files>adapter-archon/src/capabilities.ts, adapter-archon/src/provider.ts, adapter-archon/src/registration.ts, adapter-archon/src/index.ts</files>
  <read_first>
    - D:/repos/Gemini-SDK/adapter-archon/src/types.ts — IAgentProvider + ProviderCapabilities
    - D:/repos/Gemini-SDK/adapter-archon/src/options-translator.ts — translateOptions / translateChunk / warnIgnoredOptions imports
    - D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/10-RESEARCH.md — §Pattern 3 (sendQuery skeleton), §Pattern 4 (CAPABILITIES), §Pattern 5 (registration)
    - D:/repos/Gemini-SDK/ts/src/index.ts — @gemini-sdk/core barrel (what is exported)
  </read_first>
  <behavior>
    - capabilities.ts exports `GEMINI_CAPABILITIES: ProviderCapabilities` with exact values from RESEARCH §Pattern 4.
    - provider.ts exports `class GeminiProvider implements IAgentProvider`:
      * `getType(): string { return 'gemini'; }`
      * `getCapabilities(): ProviderCapabilities { return GEMINI_CAPABILITIES; }`
      * `async *sendQuery(prompt, cwd, resumeSessionId?, options?): AsyncGenerator<MessageChunk>` which:
          1. calls warnIgnoredOptions(options)
          2. builds `sdkOptions = translateOptions(prompt, cwd, resumeSessionId, options)`
          3. iterates `for await (const sdkChunk of query(sdkOptions))`
          4. if sdkChunk.type === 'tool', yields `{ type:'workflow_dispatch', workerConversationId: '', workflowName: sdkChunk.toolName }` BEFORE yielding the translated tool chunk
          5. yields `translateChunk(sdkChunk)`
      * No business logic beyond this delegation loop. Errors from query() propagate unchanged (GeminiError subclasses carry .bucket already).
    - registration.ts exports `registerGeminiProvider(registry: { registerProvider: (cfg: any) => void; isRegisteredProvider: (id: string) => boolean })`:
      * Accepts the registry API as a parameter (dependency injection) so it works standalone AND after being dropped into Archon's tree.
      * Guards `if (registry.isRegisteredProvider('gemini')) return;`
      * Calls `registry.registerProvider({ id: 'gemini', displayName: 'Gemini (Google)', factory: () => new GeminiProvider(), capabilities: GEMINI_CAPABILITIES, isModelCompatible: (m) => !m.startsWith('claude-') && !['sonnet','opus','haiku'].includes(m), builtIn: false });`
      * NOTE: when this file is dropped into the Archon clone during PR packaging (plan 10-05), the top-of-file comment will say "when co-located in Archon, swap the parameter for direct imports of ../../registry.js". The parameter form keeps standalone typecheck + tests clean.
    - index.ts barrel re-exports:
      * `export { GeminiProvider } from './provider.js';`
      * `export { registerGeminiProvider } from './registration.js';`
      * `export { GEMINI_CAPABILITIES } from './capabilities.js';`
      * `export type { IAgentProvider, SendQueryOptions, MessageChunk, ProviderCapabilities } from './types.js';`
  </behavior>
  <action>
    Create the four files per the shape in "behavior". Keep provider.ts under 150 LOC (well under the 250 cap). Use `// ARC-06: keep this file under 250 LOC (stretch 200)` comment at the top of provider.ts.

    Additionally ensure:
    - All env-var reads come from the `env` field of options (no `process.env.*` in adapter-archon/src) — the linter from plan 10-02 will catch any slip.
    - provider.ts does NOT import from `ts/src/**` directly; always through `@gemini-sdk/core`.
    - registration.ts parameter `registry` is typed as a minimal structural interface inline; avoid importing from `@archon/providers` (doesn't exist on our dev machine).
    - Overwrite adapter-archon/src/index.ts entirely (was `export {};` from Phase 2).
  </action>
  <verify>
    <automated>cd D:/repos/Gemini-SDK/adapter-archon &amp;&amp; pnpm typecheck &amp;&amp; bash D:/repos/Gemini-SDK/scripts/lint-env-namespace.sh &amp;&amp; [ $(wc -l &lt; adapter-archon/src/provider.ts) -le 250 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f adapter-archon/src/capabilities.ts` exits 0
    - `test -f adapter-archon/src/provider.ts` exits 0
    - `test -f adapter-archon/src/registration.ts` exits 0
    - `grep -q "export const GEMINI_CAPABILITIES" adapter-archon/src/capabilities.ts` exits 0
    - `grep -qE "sessionResume: *true" adapter-archon/src/capabilities.ts` exits 0
    - `grep -qE "mcp: *true" adapter-archon/src/capabilities.ts` exits 0
    - `grep -qE "hooks: *false" adapter-archon/src/capabilities.ts` exits 0
    - `grep -q "class GeminiProvider" adapter-archon/src/provider.ts` exits 0
    - `grep -q "implements IAgentProvider" adapter-archon/src/provider.ts` exits 0
    - `grep -q "getType()" adapter-archon/src/provider.ts` exits 0
    - `grep -q "return 'gemini'" adapter-archon/src/provider.ts` exits 0
    - `grep -q "workflow_dispatch" adapter-archon/src/provider.ts` exits 0
    - `grep -q "workerConversationId: ''" adapter-archon/src/provider.ts` exits 0
    - `grep -q "import { query }" adapter-archon/src/provider.ts` exits 0
    - `grep -q "from '@gemini-sdk/core'" adapter-archon/src/provider.ts` exits 0
    - `grep -q "export function registerGeminiProvider" adapter-archon/src/registration.ts` exits 0
    - `grep -q "isRegisteredProvider" adapter-archon/src/registration.ts` exits 0
    - `grep -q "export { GeminiProvider }" adapter-archon/src/index.ts` exits 0
    - `grep -q "export { registerGeminiProvider }" adapter-archon/src/index.ts` exits 0
    - `wc -l < adapter-archon/src/provider.ts` prints a number <= 250
    - `cd adapter-archon && pnpm typecheck` exits 0
    - `bash scripts/lint-env-namespace.sh` exits 0
  </acceptance_criteria>
  <done>GeminiProvider class exists, typechecks against IAgentProvider, all 4 files present, provider.ts <= 250 LOC, env-namespace linter clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author provider.spec.ts (getType, getCapabilities, sendQuery delegation, workflow_dispatch emission)</name>
  <files>adapter-archon/src/provider.spec.ts</files>
  <read_first>
    - D:/repos/Gemini-SDK/adapter-archon/src/provider.ts (just written)
    - D:/repos/Gemini-SDK/adapter-archon/src/options-translator.spec.ts — vi.mock pattern for ESM deps
    - D:/repos/Gemini-SDK/ts/src/query/query.spec.ts — vi.mock('@gemini-sdk/core') pattern (if present); otherwise use vi.hoisted + vi.mock
  </read_first>
  <behavior>
    - Test 1: `new GeminiProvider().getType() === 'gemini'`
    - Test 2: `new GeminiProvider().getCapabilities()` deep-equals GEMINI_CAPABILITIES (assert `sessionResume:true, mcp:true, hooks:false`)
    - Test 3: sendQuery delegates: mock `query()` from @gemini-sdk/core to yield one assistant chunk + one tool chunk + one result chunk; collect provider output; assert order = [assistant, workflow_dispatch, tool, result] with correct field renames (toolCallId, toolInput, workflowName)
    - Test 4: sendQuery passes positional args through translateOptions: spy on translateOptions (or inspect the QueryOptions passed to the mocked query fn) and assert prompt/cwd/session forwarded
    - Test 5: sendQuery propagates GeminiError: mock query() to throw a RateLimitError-like object (plain Error with bucket='rate_limit'); assert `for await` re-throws with the same object identity
    - Test 6 (signature): compile-time assertion via `const p: IAgentProvider = new GeminiProvider();` — presence of the line is enough; tsc fails if signature diverges (ARC-02)
  </behavior>
  <action>
    Create adapter-archon/src/provider.spec.ts using vitest. Use `vi.hoisted` + `vi.mock('@gemini-sdk/core', ...)` to stub `query()`:

    ```typescript
    const mocks = vi.hoisted(() => ({ query: vi.fn() }));
    vi.mock('@gemini-sdk/core', () => ({ query: mocks.query }));
    ```

    Provide an async generator factory helper `function* gen(chunks) { for (const c of chunks) yield c; }` wrapped with an async generator wrapper.

    Use `import { GeminiProvider, GEMINI_CAPABILITIES } from './index.js';` and `import type { IAgentProvider } from './types.js';`

    Test 6 compile-time assertion: add the line `const _structuralCheck: IAgentProvider = new GeminiProvider();` at module scope (outside any it()) with a `// ARC-02: compile-time signature match` comment. The file failing to typecheck is the failure mode.

    For Test 3, the chunks passed to the mock should be:
    - `{ type:'assistant', content:'hi' }`
    - `{ type:'tool', toolName:'read_file', toolId:'T1', parameters:{path:'/x'} }`
    - `{ type:'result', sessionId:'S', stopReason:'end' }`
    Expected output order: assistant (pass-through), workflow_dispatch (emitted), tool (translated: toolCallId:'T1', toolInput:{path:'/x'}), result (pass-through).
  </action>
  <verify>
    <automated>cd D:/repos/Gemini-SDK/adapter-archon &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f adapter-archon/src/provider.spec.ts` exits 0
    - `grep -q "vi.mock('@gemini-sdk/core'" adapter-archon/src/provider.spec.ts` exits 0
    - `grep -q "getType" adapter-archon/src/provider.spec.ts` exits 0
    - `grep -q "workflow_dispatch" adapter-archon/src/provider.spec.ts` exits 0
    - `grep -q "IAgentProvider" adapter-archon/src/provider.spec.ts` exits 0
    - `cd adapter-archon && pnpm test` exits 0 and reports at least 5 tests passing in provider.spec.ts plus the previously-green options-translator.spec.ts suite
  </acceptance_criteria>
  <done>provider.spec.ts passes all 6 behaviors; signature check compiles; full adapter test suite green.</done>
</task>

</tasks>

<verification>
- `cd adapter-archon && pnpm typecheck && pnpm test` green
- `bash scripts/lint-env-namespace.sh` green
- `wc -l adapter-archon/src/provider.ts` <= 250
- `node --input-type=module -e "import { GeminiProvider } from './adapter-archon/src/index.ts'; console.log(new GeminiProvider().getType());"` prints `gemini` (source-published `.ts`; works under Node's ESM loader only if adapter-archon has a loader shim — if not, skip this and rely on the typecheck/test signal)
</verification>

<success_criteria>
- ARC-01: adapter-archon implements IAgentProvider (compile + runtime signature)
- ARC-02: sendQuery signature matches Archon exactly
- ARC-03: getType() returns 'gemini'
- ARC-04: source-published .ts (index.ts points to src via package.json exports, no build step run)
- ARC-06: provider.ts <= 250 LOC (stretch 200)
- workflow_dispatch emission cadence matches Claude/Codex (one per tool call, workerConversationId='')
</success_criteria>

<output>
After completion, create `.planning/phases/10-archon-adapter-ts-only/10-04-SUMMARY.md`
</output>
