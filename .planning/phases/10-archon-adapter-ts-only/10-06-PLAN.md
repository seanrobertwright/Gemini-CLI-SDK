---
phase: 10-archon-adapter-ts-only
plan: 06
type: execute
wave: 4
depends_on: ["10-archon-adapter-ts-only-04"]
files_modified:
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/README.md
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/provider.ts
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/capabilities.ts
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/registration.ts
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/options-translator.ts
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/index.ts
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/registry.patch
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/env.example.patch
  - .planning/phases/10-archon-adapter-ts-only/pr-artifacts/PR_BODY.md
autonomous: false
requirements: [ARC-08]
must_haves:
  truths:
    - "PR artifact bundle exists with the exact files to copy into Archon at packages/providers/src/community/gemini/"
    - "Registry patch adds a single line to registerCommunityProviders() and references community/gemini/index.js"
    - ".env.example patch adds GEMINI_API_KEY and GEMINI_BIN_PATH entries"
    - "A draft PR is open at coleam00/Archon with head branch gemini-sdk-integration"
  artifacts:
    - path: .planning/phases/10-archon-adapter-ts-only/pr-artifacts/
      provides: "Exact file bundle to be copied into the Archon PR"
    - path: .planning/phases/10-archon-adapter-ts-only/pr-artifacts/PR_BODY.md
      provides: "Draft PR description text"
  key_links:
    - from: .planning/phases/10-archon-adapter-ts-only/pr-artifacts/registry.patch
      to: "Archon packages/providers/src/registry.ts"
      via: "unified diff adding registerGeminiProvider() to registerCommunityProviders()"
      pattern: "registerGeminiProvider"
    - from: .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/provider.ts
      to: adapter-archon/src/provider.ts
      via: "adapted copy: import paths swapped from ./types.js to ../../types.js and from @gemini-sdk/core to @gemini-sdk/gemini"
      pattern: "from '\\.\\./\\.\\./types"
---

<objective>
Stage the PR artifact bundle (files + registry patch + env.example patch + PR body) under `.planning/phases/10-archon-adapter-ts-only/pr-artifacts/` and open the draft PR against coleam00/Archon (ARC-08).

Purpose: Closes ARC-08. Split from plan 10-05 (was Task 2 + Task 4 there) so 10-05 stays focused on contract test + CI workflows. This plan runs in parallel with 10-05 in Wave 4 — both depend on 10-04's adapter source files but touch disjoint paths.
Output: 9 files under `pr-artifacts/` + the actual draft PR URL.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-archon-adapter-ts-only/10-CONTEXT.md
@.planning/phases/10-archon-adapter-ts-only/10-RESEARCH.md
@.planning/phases/10-archon-adapter-ts-only/10-04-SUMMARY.md
@adapter-archon/src/provider.ts
@adapter-archon/src/capabilities.ts
@adapter-archon/src/registration.ts
@adapter-archon/src/options-translator.ts
@adapter-archon/src/index.ts
@.archon-compat

<interfaces>
PR target (from RESEARCH §"State of the Art", §"Target PR Directory Structure"):
- Destination: packages/providers/src/community/gemini/
- Files to add: provider.ts, capabilities.ts, registration.ts, options-translator.ts, index.ts
- Registry patch: packages/providers/src/registry.ts, 1-line addition inside registerCommunityProviders()
- .env.example patch: Archon's top-level .env.example
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build PR artifact bundle under .planning/phases/.../pr-artifacts/</name>
  <files>.planning/phases/10-archon-adapter-ts-only/pr-artifacts/README.md, .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/provider.ts, .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/capabilities.ts, .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/registration.ts, .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/options-translator.ts, .planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/index.ts, .planning/phases/10-archon-adapter-ts-only/pr-artifacts/registry.patch, .planning/phases/10-archon-adapter-ts-only/pr-artifacts/env.example.patch, .planning/phases/10-archon-adapter-ts-only/pr-artifacts/PR_BODY.md</files>
  <read_first>
    - D:/repos/Gemini-SDK/adapter-archon/src/provider.ts
    - D:/repos/Gemini-SDK/adapter-archon/src/capabilities.ts
    - D:/repos/Gemini-SDK/adapter-archon/src/registration.ts
    - D:/repos/Gemini-SDK/adapter-archon/src/options-translator.ts
    - D:/repos/Gemini-SDK/adapter-archon/src/index.ts
    - D:/repos/Gemini-SDK/adapter-archon/src/types.ts — NOT copied; PR bundle imports from `../../types.js` (Archon's own types.ts)
    - D:/repos/Gemini-SDK/.archon-compat — SHA to reference in PR body
    - D:/repos/Gemini-SDK/spec/archon/mapping.md — referenced from PR body
    - D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/10-CONTEXT.md — §"PR packaging & timing (ARC-08)"
  </read_first>
  <action>
    1. Create the `pr-artifacts/gemini/` directory. Copy the 5 source files with three targeted adaptations each:
       - Replace `from './types.js'` with `from '../../types.js'` in all files (Archon's IAgentProvider/SendQueryOptions/MessageChunk/ProviderCapabilities types live 2 levels up at packages/providers/src/types.ts).
       - Replace `from '@gemini-sdk/core'` with `from '@gemini-sdk/gemini'` in provider.ts and options-translator.ts (the published npm name per CONTEXT.md; during draft PR, Archon's package.json will use a GitHub tarball dep).
       - In registration.ts, replace the DI parameter form with direct imports from the registry module: add `import { registerProvider, isRegisteredProvider } from '../../registry.js';` at top and change the function signature to `export function registerGeminiProvider(): void` (parameterless, matching the Pi pattern verbatim).
    2. Create `pr-artifacts/registry.patch` as a unified diff adding `import { registerGeminiProvider } from './community/gemini/index.js';` at top and `registerGeminiProvider();` inside `registerCommunityProviders()` body. Preserve the existing `registerPiProvider();` line.
    3. Create `pr-artifacts/env.example.patch` adding entries under Archon's existing AI Assistant Configuration section:
       ```
       # Gemini (community provider — packages/providers/src/community/gemini)
       GEMINI_API_KEY=
       GEMINI_BIN_PATH=
       ```
       NOTE: RESEARCH Secondary sources indicate GEMINI_API_KEY may already exist in a Pi-related section of Archon's .env.example. If so, the patch adds only GEMINI_BIN_PATH and a comment header; validate during execution by inspecting the cloned Archon's .env.example before authoring the diff.
    4. Create `pr-artifacts/README.md` documenting:
       - Pinned Archon SHA (read from .archon-compat).
       - Apply instructions: `cp -r pr-artifacts/gemini $ARCHON_ROOT/packages/providers/src/community/` and `cd $ARCHON_ROOT && git apply <repo>/.planning/.../pr-artifacts/registry.patch` and same for env.example.patch.
       - Known adaptations vs standalone adapter-archon/src/: the 3 bullet items above.
    5. Create `pr-artifacts/PR_BODY.md` with the draft PR description:
       - Heading: "Add Gemini community provider (draft)"
       - Summary bullets: what it is, SDK dep during draft (GitHub tarball), draft status until npm publish (Phase 11).
       - Files added: the 5 source files + registry edit + .env.example edit.
       - Verification: `bun install && bun test packages/providers` passes in the Archon clone.
       - Precedent: references Pi provider as the architectural template.
       - Footer: "Draft PR - will flip to ready-for-review in Phase 11 after @gemini-sdk/gemini v1.0.0 is published to npm. Dep will swap from `github:<repo>#<SHA>` to `^1.0.0` at that time."
       - Links: this SDK repo, spec/archon/mapping.md for option triage, Pi provider on Archon.
  </action>
  <verify>
    <automated>test -d D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini && for f in provider.ts capabilities.ts registration.ts options-translator.ts index.ts; do test -f "D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini/$f" || { echo MISSING $f; exit 1; }; done && test -f D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/registry.patch && test -f D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/env.example.patch && test -f D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/PR_BODY.md</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files under `pr-artifacts/gemini/` exist
    - `grep -q "from '\\.\\./\\.\\./types" pr-artifacts/gemini/provider.ts` exits 0 (path adapted)
    - `grep -q "@gemini-sdk/gemini" pr-artifacts/gemini/provider.ts` exits 0 (npm name adapted)
    - `grep -q "from '\\.\\./\\.\\./registry" pr-artifacts/gemini/registration.ts` exits 0 (registry import adapted)
    - `grep -q "export function registerGeminiProvider(): void" pr-artifacts/gemini/registration.ts` exits 0 (parameterless signature)
    - `grep -q "registerGeminiProvider" pr-artifacts/registry.patch` exits 0
    - `grep -q "GEMINI_BIN_PATH" pr-artifacts/env.example.patch` exits 0
    - `grep -qi "draft" pr-artifacts/PR_BODY.md` exits 0
    - `grep -q "Phase 11" pr-artifacts/PR_BODY.md` exits 0
    - Smoke: apply the patches in a scratch directory `git init` tree with the Pi registry.ts content and `git apply --check` exits 0 (optional smoke; reviewer may skip if not feasible)
  </acceptance_criteria>
  <done>PR artifact bundle complete with all 5 adapted source files, both patches, README, and PR body.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Open draft PR on coleam00/Archon (human-verified)</name>
  <files>(external: coleam00/Archon branch gemini-sdk-integration)</files>
  <read_first>
    - D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/PR_BODY.md
    - D:/repos/Gemini-SDK/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/README.md (apply instructions)
    - D:/repos/Gemini-SDK/.archon-compat
  </read_first>
  <what-built>
    Automated work completed in Task 1:
    - PR artifact bundle under .planning/phases/.../pr-artifacts/
    - Registry patch + env.example patch + PR_BODY.md ready
    - (Plan 10-05 produced the contract test + archon-contract/drift CI workflows in parallel)
  </what-built>
  <action>
    Claude MUST attempt these steps autonomously first before pausing for human verification:
    1. Fork coleam00/Archon (if not already) via `gh repo fork coleam00/Archon --remote --clone=false` - this may fail with auth; if so record the failure and continue to step 5.
    2. Clone the fork into a scratch dir: `gh repo clone <user>/Archon $TMP/archon-pr -- --depth=50`
    3. `cd $TMP/archon-pr && git checkout -b gemini-sdk-integration`
    4. Copy the bundle: `cp -r $REPO/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/gemini packages/providers/src/community/` and apply both patches.
    5. `bun install && bun test packages/providers` - must be green.
    6. Update package.json in the Archon fork to add the GitHub tarball dep: `"@gemini-sdk/gemini": "github:<this-repo>#<head-sha>"`.
    7. Commit: `git commit -m "feat(providers): add Gemini community provider (draft, depends on @gemini-sdk/gemini tarball)"`
    8. Push: `git push -u origin gemini-sdk-integration`
    9. Open draft PR: `gh pr create --repo coleam00/Archon --draft --title "Add Gemini community provider (draft)" --body-file $REPO/.planning/phases/10-archon-adapter-ts-only/pr-artifacts/PR_BODY.md --head <user>:gemini-sdk-integration --base dev`
    10. Capture the PR URL.

    If any step fails (auth, fork permissions, bun test red, tarball resolution), STOP and surface the exact error to the user at the checkpoint. Do NOT amend.
  </action>
  <how-to-verify>
    1. Run `gh pr list --repo coleam00/Archon --head gemini-sdk-integration --json url,isDraft,state,title` and confirm exactly one PR in state OPEN, isDraft: true, title starting with "Add Gemini community provider".
    2. Click through to the PR in a browser; verify the Files tab shows: 5 new files under packages/providers/src/community/gemini/, registry.ts modified with +2 lines (import + call), .env.example modified with GEMINI_* entries.
    3. Verify PR is against `coleam00/Archon:dev` (not main, not a fork's branch).
    4. Verify PR body mentions draft status and Phase 11 flip intent.
    5. Paste the PR URL into the checkpoint resume message.

    If Claude's automation failed at any step, review the error log. Common failures:
    - Auth: user must run `gh auth login` with access to their Archon fork
    - bun test red: inspect the Archon test output; likely a type-reference drift from the pinned SHA (report back to planner - may need to refresh .archon-compat)
    - Tarball resolution: ensure this repo's HEAD SHA is pushed to a public branch
  </how-to-verify>
  <acceptance_criteria>
    - `gh pr list --repo coleam00/Archon --head gemini-sdk-integration --state open --json url | grep -q "http"` exits 0 (open PR exists)
    - `gh pr view --repo coleam00/Archon <PR-NUMBER> --json isDraft | grep -q "true"` exits 0 (PR is draft)
    - User pastes the PR URL in the resume signal
  </acceptance_criteria>
  <resume-signal>Paste the PR URL (or type "BLOCKED: <error>" with the failing step and stderr so the planner can revise).</resume-signal>
  <done>Draft PR is open at coleam00/Archon on branch gemini-sdk-integration, URL recorded in the summary.</done>
</task>

</tasks>

<verification>
- `pr-artifacts/` bundle complete with 9 files
- `gh pr list --repo coleam00/Archon --head gemini-sdk-integration --state open` returns exactly one open draft PR
</verification>

<success_criteria>
- ARC-08: Draft PR open on coleam00/Archon branch gemini-sdk-integration with the 5-file bundle + registry edit + .env.example entries
- No Python work done (TS-only phase honored)
</success_criteria>

<output>
After completion, create `.planning/phases/10-archon-adapter-ts-only/10-06-SUMMARY.md` including the draft PR URL.
</output>
