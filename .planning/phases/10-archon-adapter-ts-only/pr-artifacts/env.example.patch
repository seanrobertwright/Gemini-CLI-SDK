--- a/.env.example
+++ b/.env.example
@@ -1,3 +1,8 @@
 # AI Assistant Configuration
 # (existing entries preserved — apply this hunk after the last provider block)
+
+# Gemini (community provider — packages/providers/src/community/gemini)
+# GEMINI_API_KEY may already be present if a Pi-related section defines it;
+# if so, drop the duplicate line during apply and keep only GEMINI_BIN_PATH.
+GEMINI_API_KEY=
+GEMINI_BIN_PATH=
