---
phase: 05-error-taxonomy-archon-5-bucket-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/fixtures/error-auth.ndjson
  - spec/fixtures/error-auth.stderr.txt
  - spec/fixtures/error-auth.expected.json
  - spec/fixtures/error-rate-limit.ndjson
  - spec/fixtures/error-rate-limit.stderr.txt
  - spec/fixtures/error-rate-limit.expected.json
  - spec/fixtures.manifest.json
  - ts/tests/errors.test.ts
  - ts/tests/errorMapper.test.ts
  - python/tests/errors/test_errors.py
  - python/tests/errors/test_error_mapper.py
  - python/tests/errors/__init__.py
autonomous: false
requirements: [ERR-04, ERR-05]
must_haves:
  truths:
    - "Real gemini-cli stderr + JSON error shapes exist for auth + rate-limit fixtures"
    - "Failing test scaffolds describe ERR-01..ERR-07 behaviors before implementation"
    - "Synthetic sidecars removed from re-captured fixtures"
  artifacts:
    - path: spec/fixtures/error-auth.stderr.txt
      provides: "Real stderr tail from API-key-only host (not synthetic)"
    - path: spec/fixtures/error-rate-limit.ndjson
      provides: "Real 429 stream-json error event with any retryAfter field present"
    - path: ts/tests/errors.test.ts
      provides: "Failing scaffolds for class hierarchy, bucket, retryable"
    - path: ts/tests/errorMapper.test.ts
      provides: "Failing scaffolds for fromStreamEvent + fromExit + parity (ERR-04/ERR-05)"
    - path: python/tests/errors/test_errors.py
      provides: "Python counterpart failing scaffolds"
    - path: python/tests/errors/test_error_mapper.py
      provides: "Python counterpart failing ErrorMapper parity scaffolds"
  key_links:
    - from: spec/fixtures/error-auth.expected.json
      to: "_throws: true + errorType"
      via: "contract test parametrize"
      pattern: '"_throws"\\s*:\\s*true'
    - from: spec/fixtures.manifest.json
      to: "error-auth, error-rate-limit entries"
      via: "remove synthetic:true sidecar flag"
      pattern: '"synthetic"\\s*:\\s*true'
---

<objective>
Wave 1: Establish real stderr + error-event shapes for the two currently-synthetic error fixtures, and commit failing test scaffolds that describe every ERR-0x requirement before any implementation lands.

Purpose: All downstream waves (YAML authoring, ErrorMapper regex writing, CI linter) depend on real gemini-cli output. Regexes written against synthetic text will drift from reality. Failing test scaffolds force Wave 2 and Wave 3 to converge on the same contract.

Output: Re-captured fixture pairs for `error-auth` and `error-rate-limit`; updated manifest; ERR-0x test scaffolds in both languages that fail (by design) until later waves land implementation.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-error-taxonomy-archon-5-bucket-mapping/05-CONTEXT.md
@.planning/phases/05-error-taxonomy-archon-5-bucket-mapping/05-RESEARCH.md
@.planning/phases/05-error-taxonomy-archon-5-bucket-mapping/05-VALIDATION.md
@spec/errors.md
@spec/fixtures.manifest.json
@scripts/capture-fixtures.mjs
@spec/fixtures/error-auth.ndjson
@spec/fixtures/error-auth.stderr.txt
@spec/fixtures/error-auth.expected.json
@spec/fixtures/error-rate-limit.ndjson
@spec/fixtures/error-rate-limit.stderr.txt
@spec/fixtures/error-rate-limit.expected.json
@spec/fixtures/abort-midstream.ndjson
@spec/fixtures/abort-midstream.expected.json

<interfaces>
<!-- Current synthetic expected.json shape, from Phase 3 conventions -->
<!-- Existing error-auth.expected.json uses _throws:true sentinel per STATE.md:
     "error-auth.expected.json uses _throws:true sentinel — non-rate-limit errors throw GeminiError, not yield a chunk" -->
<!-- Existing error-rate-limit.expected.json currently YIELDS a {type:'rate_limit'} chunk.
     Phase 5 flips this: rate-limit errors now THROW RateLimitError (per RESEARCH.md §"State of the Art"). -->

Expected JSON conventions:
- `{ "chunks": [...], "_throws": true, "_errorType": "AuthError" }` — terminal throw sentinel
- `{ "chunks": [...] }` — normal yield (no throw)

Test scaffolding convention (from Phase 3):
- Parametrize over `spec/fixtures/*.ndjson` using AST-extractable `run_*` helper names so `diff-test-names.sh` sees parity.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Re-capture error-auth + error-rate-limit against API-key-only host</name>
  <files>spec/fixtures/error-auth.ndjson, spec/fixtures/error-auth.stderr.txt, spec/fixtures/error-auth.expected.json, spec/fixtures/error-rate-limit.ndjson, spec/fixtures/error-rate-limit.stderr.txt, spec/fixtures/error-rate-limit.expected.json, spec/fixtures.manifest.json</files>
  <action>See <how-to-verify> below. Human runs capture-fixtures.mjs on API-key-only host; Claude updates expected.json + manifest.json after real captures land. Claude CANNOT run this unassisted because it requires an intentionally invalid API key AND genuine quota exhaustion on a real Gemini account.</action>
  <read_first>
    - scripts/capture-fixtures.mjs (understand invocation contract)
    - spec/fixtures/error-auth.ndjson (current synthetic shape)
    - spec/fixtures/error-auth.stderr.txt (synthetic text being replaced)
    - spec/fixtures/error-rate-limit.ndjson (current synthetic shape)
    - spec/fixtures/error-rate-limit.stderr.txt (synthetic text being replaced)
    - spec/fixtures.manifest.json (find error-auth + error-rate-limit entries and synthetic:true flags)
    - spec/errors.md §3, §4, §5 (observed patterns + gap table — especially retryAfter field gap)
  </read_first>
  <what-built>
    Re-capture context. Claude cannot run this end-to-end because: (a) it requires an intentionally invalid GEMINI_API_KEY to force UNAUTHENTICATED, and (b) rate-limit requires genuinely exhausting quota on a real account. The user must run the capture commands on a host configured for API-key-only auth (NOT OAuth).
  </what-built>
  <how-to-verify>
    1. On a Windows (or macOS/Linux) host with gemini-cli 0.37.1 installed:
       a. Set GEMINI_API_KEY to a known-invalid value (e.g. "invalid-key-for-phase-5-capture").
       b. Ensure ~/.gemini/oauth_creds.json is absent or renamed (API-key-only path).
       c. Run: `node scripts/capture-fixtures.mjs --slug error-auth --force`
       d. Confirm spec/fixtures/error-auth.stderr.txt and .ndjson contain REAL upstream strings (expect one of: "API key not valid", "UNAUTHENTICATED", "401", "PERMISSION_DENIED").
    2. For rate-limit: use a real API key that has exhausted free quota OR run N parallel requests until a 429 fires:
       a. Run: `node scripts/capture-fixtures.mjs --slug error-rate-limit --force`
       b. Confirm spec/fixtures/error-rate-limit.ndjson contains a real `{"type":"error"}` event with code 429 OR status "RESOURCE_EXHAUSTED".
       c. INSPECT the captured JSON for any of: `retryAfter`, `retry_after`, `retryDelay`, `retry_delay_ms` — document which (if any) field is present (this resolves RESEARCH.md Open Question #3).
    3. After capture:
       - Remove `"synthetic": true` from both entries in spec/fixtures.manifest.json.
       - Update spec/fixtures/error-auth.expected.json: set `_throws: true`, `_errorType: "AuthError"` (or more specific subtype based on real stderr — "NotConfigured" if "no API key", "Forbidden403" if "403", otherwise "AuthError").
       - Update spec/fixtures/error-rate-limit.expected.json: REPLACE the yielded `{type:'rate_limit'}` chunk with `_throws: true, _errorType: "RateLimitError"` (empty chunks array). Preserve any non-error chunks that preceded the error.
    4. Run: `node scripts/validate-fixtures.mjs` — must exit 0.
    5. Commit all changed files.

    If real re-capture is blocked (no spare API key, no quota exhaustion path), document the blocker in .planning/STATE.md and mark fixtures with `"synthetic": true, "synthetic_reason": "<why>"` so the CI linter can skip them. Plan 04's linter must tolerate explicitly-flagged synthetic fixtures.
  </how-to-verify>
  <resume-signal>Type "captured" (real fixtures landed) OR "blocked: &lt;reason&gt;" (synthetic flag retained with justification)</resume-signal>
  <verify>
    <automated>node scripts/validate-fixtures.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"synthetic": true' spec/fixtures.manifest.json` returns 0 (or matches the documented blocked list)
    - `grep -E '"_throws"\s*:\s*true' spec/fixtures/error-auth.expected.json` returns a match
    - `grep -E '"_errorType"\s*:\s*"(AuthError|NotConfigured|Forbidden403|Expired|ToSViolation)"' spec/fixtures/error-auth.expected.json` returns a match
    - `grep -E '"_errorType"\s*:\s*"RateLimitError"' spec/fixtures/error-rate-limit.expected.json` returns a match
    - `grep -E '"type"\s*:\s*"rate_limit"' spec/fixtures/error-rate-limit.expected.json` returns 0 matches (rate_limit chunk is no longer yielded)
    - `node scripts/validate-fixtures.mjs` exits 0
  </acceptance_criteria>
  <done>Real (or explicitly-flagged-blocked) fixture pairs exist; expected.json files use `_throws` sentinel with specific `_errorType`; validate-fixtures.mjs passes.</done>
</task>

<task type="auto">
  <name>Task 2: TS test scaffolds for ERR-01..ERR-07</name>
  <files>ts/tests/errors.test.ts, ts/tests/errorMapper.test.ts</files>
  <read_first>
    - ts/src/errors/GeminiNotFoundError.ts (existing class to reparent)
    - ts/src/errors/index.ts (barrel — will re-export generated classes after Wave 2)
    - ts/src/query/types.ts (AbortError to relocate)
    - ts/src/parser/dispatch.ts (integration point line 68-79)
    - ts/src/query/query.ts (sawResult flag integration)
    - ts/tests/ (find existing fixture-corpus parametrize pattern — look at any `dispatch` or `parser` fixture-loop test for naming convention like `run_*` helpers)
    - spec/fixtures/error-auth.expected.json + spec/fixtures/error-rate-limit.expected.json (post-Task-1)
    - .planning/phases/05-error-taxonomy-archon-5-bucket-mapping/05-RESEARCH.md §"Phase Requirements → Test Map" (lines 637-646)
  </read_first>
  <action>
    Create TWO failing test files that describe Phase 5 behavior. Use vitest (already configured in ts/package.json). Imports from `../src/errors/index.js` and `../src/errors/ErrorMapper.js` — these modules DO NOT exist yet (that is expected; tests must fail at import time, which is a valid RED state).

    === ts/tests/errors.test.ts ===
    - `describe('GeminiError hierarchy (ERR-01)')`:
      - `it('run_GeminiError_exists_and_is_base')` — asserts `GeminiError` imported and `new GeminiError()` produces `instanceof Error === true`.
      - `it('run_all_10_subclasses_extend_GeminiError')` — parametrized over: `RateLimitError`, `AuthError`, `ModelAccessError`, `InvalidPromptError`, `ProcessError`, `ProcessCrashError`, `ParseError`, `AbortError`, `UnsupportedFeatureError`, `GeminiNotFoundError`. For each: `new Cls()` is `instanceof GeminiError`.
      - `it('run_AuthError_subtypes_extend_AuthError')` — `NotConfigured`, `Forbidden403`, `Expired`, `ToSViolation` each `instanceof AuthError` and `instanceof GeminiError`.
    - `describe('Bucket + retryable invariants (ERR-02, ERR-03)')`:
      - `it('run_bucket_matches_archon_5')` — each class's `.bucket` is one of `'rate_limit'|'auth'|'model_access'|'crash'|'unknown'`.
      - `it('run_retryable_is_boolean')` — every class has `.retryable` as a boolean.
      - `it('run_RateLimitError_retryable_is_true')` — `new RateLimitError().retryable === true`.
      - `it('run_RateLimitError_retryAfterMs_populates_from_constructor_options')` — `new RateLimitError('msg', { retryAfterMs: 5000 }).retryAfterMs === 5000`.
      - `it('run_AuthError_retryable_is_false')` — `new AuthError().retryable === false`.
    - Explicit expected class→bucket table:
      ```
      RateLimitError → rate_limit
      AuthError + NotConfigured/Forbidden403/Expired/ToSViolation → auth
      ModelAccessError → model_access
      ProcessError, ProcessCrashError, AbortError → crash
      InvalidPromptError, ParseError, UnsupportedFeatureError, GeminiNotFoundError → unknown
      ```

    === ts/tests/errorMapper.test.ts ===
    - `describe('ErrorMapper.fromStreamEvent (ERR-04)')`:
      - `it('run_fromStreamEvent_code429_yields_RateLimitError')` — input `{type:'error', error:{code:429}}` produces `instanceof RateLimitError` with `.retryable===true`.
      - `it('run_fromStreamEvent_RESOURCE_EXHAUSTED_status_yields_RateLimitError')`.
      - `it('run_fromStreamEvent_code401_yields_AuthError')`.
      - `it('run_fromStreamEvent_code403_yields_Forbidden403')`.
      - `it('run_fromStreamEvent_unknown_event_yields_GeminiError_with_bucket_unknown')`.
      - `it('run_fromStreamEvent_retryAfter_populates_retryAfterMs')` — confirms dynamic extraction (field name TBD from Task 1 capture — use the field name captured; if unavailable, assert `undefined`).
    - `describe('ErrorMapper.fromExit (ERR-04)')`:
      - `it('run_fromExit_quota_stderr_yields_RateLimitError')`.
      - `it('run_fromExit_api_key_not_valid_yields_AuthError')`.
      - `it('run_fromExit_unmatched_stderr_yields_generic_GeminiError_bucket_unknown')` — assert `.bucket==='unknown'`, `.retryable===false`, `.message` contains exit code + stderr tail.
    - `describe('Two-path parity (ERR-05)')`:
      - `it('run_stream_and_exit_path_produce_same_class_for_rate_limit')` — feed both paths representations of the same rate-limit condition; assert `fromStreamEvent(...).constructor.name === fromExit(...).constructor.name`.
      - `it('run_stream_and_exit_path_produce_same_class_for_auth')` — same for auth.
    - `describe('Fixture corpus contract (ERR-04, ERR-05)')`:
      - Parametrize over `spec/fixtures/error-*.ndjson` + `.stderr.txt` pairs. For each: both paths produce the class matching `_errorType` in `.expected.json`.

    All tests MUST fail on first run (imports don't exist yet). Add `// Phase 5 RED — implementation lands in plan 05-02 / 05-03` as a top-of-file comment to explain the expected failure to reviewers.

    Verify scaffolding naming: run `grep -oE "it\\('run_[a-zA-Z0-9_]+" ts/tests/errors.test.ts ts/tests/errorMapper.test.ts | wc -l` — expect ≥ 15 `run_*` test names (these will be diffed against Python suite by `scripts/diff-test-names.sh`).
  </action>
  <verify>
    <automated>cd ts && pnpm test -- --run tests/errors.test.ts tests/errorMapper.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|expected.*to fail)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `ts/tests/errors.test.ts` exists
    - File `ts/tests/errorMapper.test.ts` exists
    - `grep -c "run_" ts/tests/errors.test.ts` ≥ 7
    - `grep -c "run_" ts/tests/errorMapper.test.ts` ≥ 10
    - `grep -E "instanceof GeminiError" ts/tests/errors.test.ts` returns ≥ 1 match
    - `grep -E "fromStreamEvent|fromExit" ts/tests/errorMapper.test.ts` returns ≥ 2 matches each
    - `grep -E "Phase 5 RED" ts/tests/errors.test.ts` returns a match
    - Running the tests reports failures (not pass) — either module-not-found or assertion failures, both are valid RED states
  </acceptance_criteria>
  <done>Two failing TS test files committed with ≥ 15 `run_*` scaffolds covering ERR-01..ERR-05. Tests fail because implementation does not exist yet.</done>
</task>

<task type="auto">
  <name>Task 3: Python test scaffolds (parity counterpart)</name>
  <files>python/tests/errors/__init__.py, python/tests/errors/test_errors.py, python/tests/errors/test_error_mapper.py</files>
  <read_first>
    - python/src/gemini_sdk/errors/not_found.py (existing class to reparent)
    - python/src/gemini_sdk/errors/__init__.py (barrel)
    - python/src/gemini_sdk/parser/ (find dispatch.py for integration point parity)
    - python/src/gemini_sdk/query/ (find query.py for sawResult parity)
    - ts/tests/errors.test.ts (CANONICAL — Python mirrors this exactly)
    - ts/tests/errorMapper.test.ts (CANONICAL — Python mirrors this exactly)
    - scripts/diff-test-names.sh (understand how parity is enforced; confirms `run_*` naming convention)
    - python/tests/ (find existing test layout + pytest fixture-corpus pattern)
  </read_first>
  <action>
    Create Python counterparts that mirror ts/tests/errors.test.ts and ts/tests/errorMapper.test.ts test-for-test. Parity rule (STATE.md: "TS is canonical, Python is mechanical port"): every `it('run_X')` in TS becomes `def run_X()` or `def test_run_X()` in Python with the same behavior.

    Framework: pytest >= 8.0 (already declared in python/pyproject.toml). Use `pytest.mark.parametrize` for parametrized cases. Imports from `gemini_sdk.errors` and `gemini_sdk.errors.error_mapper` — these DO NOT exist yet; tests must fail at import time (valid RED).

    === python/tests/errors/__init__.py ===
    Empty file (makes `errors` a test package).

    === python/tests/errors/test_errors.py ===
    - Module docstring: `"""Phase 5 RED — implementation lands in plan 05-02."""`.
    - `def test_run_GeminiError_exists_and_is_base()`: imports `GeminiError` from `gemini_sdk.errors`, asserts `isinstance(GeminiError(), Exception)`.
    - `@pytest.mark.parametrize("cls_name", [...10 class names...])` + `def test_run_all_10_subclasses_extend_GeminiError(cls_name)`: for each of `RateLimitError`, `AuthError`, `ModelAccessError`, `InvalidPromptError`, `ProcessError`, `ProcessCrashError`, `ParseError`, `AbortError`, `UnsupportedFeatureError`, `GeminiNotFoundError`, imports the class, asserts `isinstance(Cls(), GeminiError)`.
    - `@pytest.mark.parametrize("cls_name", ["NotConfigured", "Forbidden403", "Expired", "ToSViolation"])` + `def test_run_AuthError_subtypes_extend_AuthError(cls_name)`: each subtype `isinstance(_, AuthError)` and `isinstance(_, GeminiError)`.
    - `def test_run_bucket_matches_archon_5()`: every class's `.bucket` attribute in `{"rate_limit", "auth", "model_access", "crash", "unknown"}`.
    - `def test_run_retryable_is_boolean()`: every class `.retryable` is `bool`.
    - `def test_run_RateLimitError_retryable_is_true()`.
    - `def test_run_RateLimitError_retryAfterMs_populates_from_constructor_options()`: Python naming is `retry_after_ms` per PEP-8 (RESEARCH.md Pitfall 7). Test: `RateLimitError("msg", retry_after_ms=5000).retry_after_ms == 5000`.
    - `def test_run_AuthError_retryable_is_false()`.

    === python/tests/errors/test_error_mapper.py ===
    Mirror each TS `errorMapper.test.ts` case as a `def test_run_*()`. Python module path: `from gemini_sdk.errors.error_mapper import ErrorMapper`. Test the same inputs, same expected classes. For fixture-corpus parametrize: use `pytest.mark.parametrize` with a list built from `pathlib.Path('spec/fixtures').glob('error-*.ndjson')`.

    Rule: each TS test name `run_X` has a Python test `test_run_X` with identical semantics. The `test_` prefix is pytest's requirement; `diff-test-names.sh` already strips prefixes for parity (STATE.md mentions this convention in Phase 2).

    Cross-check: the count `grep -c "def test_run_" python/tests/errors/*.py` must equal `grep -c "it('run_" ts/tests/errors.test.ts ts/tests/errorMapper.test.ts` after accounting for parametrize expansion.
  </action>
  <verify>
    <automated>cd python &amp;&amp; uv run pytest tests/errors/ 2>&amp;1 | grep -E "(FAILED|ERROR|ModuleNotFoundError)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `python/tests/errors/__init__.py` exists (can be empty)
    - File `python/tests/errors/test_errors.py` exists
    - File `python/tests/errors/test_error_mapper.py` exists
    - `grep -c "def test_run_" python/tests/errors/test_errors.py` ≥ 7
    - `grep -c "def test_run_" python/tests/errors/test_error_mapper.py` ≥ 10
    - `grep -E "from gemini_sdk.errors import" python/tests/errors/test_errors.py` returns a match
    - `grep -E "from gemini_sdk.errors.error_mapper import" python/tests/errors/test_error_mapper.py` returns a match
    - `grep -E "Phase 5 RED" python/tests/errors/test_errors.py` returns a match
    - `grep -E "retry_after_ms" python/tests/errors/test_errors.py` returns a match (snake_case per PEP-8 / RESEARCH.md Pitfall 7)
    - Running `uv run pytest tests/errors/` reports errors/failures (not pass) — ModuleNotFoundError is the expected RED state
  </acceptance_criteria>
  <done>Two Python test files + `__init__.py` committed, mirroring TS scaffolds test-for-test with `test_run_*` naming. Tests fail with ModuleNotFoundError (expected).</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-fixtures.mjs` passes after re-capture
- [ ] `spec/fixtures.manifest.json` contains no un-justified `"synthetic": true` entries
- [ ] `spec/fixtures/error-rate-limit.expected.json` uses `_throws:true` + `_errorType:"RateLimitError"` (no yielded rate_limit chunk)
- [ ] `spec/fixtures/error-auth.expected.json` uses a specific AuthError subtype
- [ ] `cd ts && pnpm test -- --run tests/errors.test.ts tests/errorMapper.test.ts` runs and FAILS (RED state) with module-not-found or assertion failures
- [ ] `cd python && uv run pytest tests/errors/` runs and FAILS (RED state) with ModuleNotFoundError
- [ ] Test name parity: `bash scripts/diff-test-names.sh` either passes OR the specific new `run_*` names are added symmetrically (TS canonical)
</verification>

<success_criteria>
- Wave 0 of the VALIDATION.md strategy is satisfied: re-captured fixtures + test scaffolds in both languages exist
- Every TS `it('run_X')` has a Python `def test_run_X` counterpart
- Tests fail for a documented reason (implementation not yet written) — this is the correct state for Wave 1
- Downstream plans (02, 03, 04) have a concrete contract to implement against
</success_criteria>

<output>
After completion, create `.planning/phases/05-error-taxonomy-archon-5-bucket-mapping/05-01-SUMMARY.md` documenting:
- Re-capture outcome: real fixtures landed, OR explicit blocked list with reasons
- Actual `retryAfter` field name found in real 429 responses (resolves RESEARCH Open Question #3) OR "not present"
- AuthError subtype chosen for error-auth.expected.json based on real stderr
- Test name parity count (TS vs Python)
</output>
