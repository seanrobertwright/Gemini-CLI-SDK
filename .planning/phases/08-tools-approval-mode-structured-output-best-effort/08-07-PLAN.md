---
phase: 08-tools-approval-mode-structured-output-best-effort
plan: 07
type: execute
wave: 6
depends_on: ['08-02', '08-04', '08-06']
files_modified:
  - ts/tests-live/README.md
  - ts/tests-live/e2e.live.spec.ts
  - ts/vitest.live.config.ts
  - ts/package.json
  - .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md
  - docs/tools.md
  - docs/structured-output.md
autonomous: true
requirements: [TOL-01, TOL-02]
gap_closure: true

must_haves:
  truths:
    - "SC-1 is verifiable by an automated opt-in test: spawning live gemini-cli with allowedTools: ['read_file'] + a prompt that would otherwise call write_file produces an event stream containing zero write_file tool_use events, proved by a test that reads and asserts on the streamed event log"
    - "SC-2a is verifiable by an automated opt-in test: queryFull with approvalMode: 'yolo' in a temp sandbox workspace writes a file end-to-end without prompting, proved by post-run fs.stat() succeeding"
    - "SC-2b is verifiable by an automated opt-in test: queryFull with approvalMode: 'plan' in a temp sandbox workspace produces zero filesystem mutations, proved by post-run fs.stat() throwing ENOENT for any file the prompt asked to create"
    - "Default pnpm test suite remains free, fast, and hermetic (zero network, zero API spend) — live tests are skipped unless RUN_LIVE_E2E=1 AND GEMINI_API_KEY is present"
    - "TS:Python diff-test-names.sh parity (currently 205:205) is NOT broken by the new live suite — live tests live outside the scan path of scripts/diff-test-names.sh and outside the default vitest include pattern"
    - "08-VALIDATION.md reconciles with 08-VERIFICATION.md: SC-1 and SC-2 are reclassified from 'Manual-Only' to 'Automated-Opt-In' with a note explaining the RUN_LIVE_E2E env-var gate"
    - "docs/tools.md and docs/structured-output.md contributor sections document how to run the live suite locally (RUN_LIVE_E2E=1 pnpm test:live) so future contributors have a discoverable path"
  artifacts:
    - path: "ts/tests-live/e2e.live.spec.ts"
      provides: "Three live integration tests covering SC-1 (allowedTools enforcement), SC-2a (yolo file-write), SC-2b (plan no-mutation)"
      min_lines: 120
      contains: "RUN_LIVE_E2E"
    - path: "ts/vitest.live.config.ts"
      provides: "Separate vitest config scoped to the live test suite only"
      min_lines: 8
      contains: "tests-live"
    - path: "ts/tests-live/README.md"
      provides: "Contributor instructions for running the live suite (cost warning, env vars required, sandbox behavior)"
      min_lines: 25
      contains: "RUN_LIVE_E2E"
    - path: "ts/package.json"
      provides: "test:live npm script wiring to the vitest.live.config.ts runner"
      contains: "test:live"
  key_links:
    - from: "ts/tests-live/e2e.live.spec.ts"
      to: "ts/src/index.ts (query, queryFull, ApprovalMode public API)"
      via: "import { query, queryFull, ApprovalMode } from '../src/index.js'"
      pattern: "from.*\\.\\./src/index"
    - from: "ts/package.json scripts.test:live"
      to: "ts/vitest.live.config.ts"
      via: "vitest run --config vitest.live.config.ts"
      pattern: "vitest.live.config"
    - from: "ts/vitest.live.config.ts"
      to: "ts/tests-live/*.spec.ts"
      via: "test.include pattern 'tests-live/**/*.spec.ts'"
      pattern: "tests-live"
    - from: ".planning/phases/08-*/08-VALIDATION.md"
      to: "ts/tests-live/e2e.live.spec.ts"
      via: "Automated-Opt-In row citing the file path and RUN_LIVE_E2E gate"
      pattern: "Automated-Opt-In|tests-live"
    - from: "docs/tools.md"
      to: "ts/tests-live/README.md"
      via: "Contributors section linking the live-suite README"
      pattern: "RUN_LIVE_E2E"
---

<objective>
Close Phase 8's only remaining gap: the ROADMAP's SC-1 (allowedTools live enforcement) and SC-2 (approvalMode yolo + plan filesystem behavior) success criteria require automated test evidence, but no such tests exist. 08-VERIFICATION.md (2026-04-20) scored Phase 8 as 2/4 automated SCs. 08-VALIDATION.md categorized SC-1/SC-2 as "Manual-Only", which conflicts with the ROADMAP's automated-evidence requirement.

This plan resolves the conflict by:
1. Creating an **opt-in live integration test suite** at `ts/tests-live/e2e.live.spec.ts` that spawns real `gemini-cli`, gated by `RUN_LIVE_E2E=1 AND GEMINI_API_KEY` presence — tests skip (not fail) when the gate is off, preserving the default `pnpm test` as free/fast/hermetic.
2. Placing the suite in `ts/tests-live/` (outside `ts/src/`) so it does NOT break `scripts/diff-test-names.sh` parity (205:205) — diff-test-names scans `ts/src/**/*.spec.ts` only.
3. Wiring a dedicated `vitest.live.config.ts` + `pnpm test:live` script so CI can opt in selectively (e.g., a manual-dispatch workflow) without forcing API spend on every push.
4. **TS-only by design** — recommendation explained in rationale below.
5. **Reconciling** 08-VALIDATION.md to reclassify SC-1/SC-2 as "Automated-Opt-In" and documenting the gate in docs/tools.md + docs/structured-output.md contributor sections.

Purpose: Transform SC-1 and SC-2 from manual-only verifications into automated-opt-in ones, closing the VERIFICATION gap without forcing API spend on every CI run.

Output: One new test file + one new vitest config + one package.json edit + one README + two doc edits + one VALIDATION.md edit.

### Rationale: TS-only, not TS + Python mirror

The gap surface is **CLI-level behavior** (does gemini-cli enforce `--allowed-tools`? does `--approval-mode yolo` write files? does `--approval-mode plan` prevent mutations?). The SDK's argv paths for these flags are already proved byte-identical between TS and Python by:
- `ts/src/query/buildArgv.spec.ts` + `python/tests/test_build_argv.py` — 205:205 parity via diff-test-names.sh
- Both `buildArgv` implementations emit `--allowed-tools <csv>` and `--approval-mode <mode>` identically (verified in 08-02 and 08-05)

Mirroring the live tests in Python would spend ~3x API budget to validate the same CLI behavior. Per "recommend-and-explain": **I recommend TS-only**. If a future reviewer disagrees, adding a Python mirror is additive (new files under `python/tests-live/`) and this plan's infrastructure (env gate, sandbox pattern, validation reconciliation) serves as the template.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-CONTEXT.md
@.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VERIFICATION.md
@.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md
@.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-02-SUMMARY.md
@.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-04-SUMMARY.md
@.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-06-SUMMARY.md

# Public API surface (consumed by live tests)
@ts/src/index.ts
@ts/src/query/types.ts
@ts/src/query/query.ts

# Existing config to mirror
@ts/vitest.config.ts
@ts/package.json
@scripts/diff-test-names.sh

<interfaces>
<!-- Public API exports the live test imports -->
<!-- From ts/src/index.ts / ts/src/query/types.ts / ts/src/query/query.ts -->

// Public functions
export async function* query(options: QueryOptions): AsyncGenerator<MessageChunk>;
export async function queryFull(options: QueryOptions): Promise<QueryResult>;

// Public const + type
export const ApprovalMode = {
  DEFAULT: 'default',
  AUTO_EDIT: 'auto_edit',
  YOLO: 'yolo',
  PLAN: 'plan',
} as const;
export type ApprovalMode = (typeof ApprovalMode)[keyof typeof ApprovalMode] | string;

// Relevant option fields (finalized in 08-02)
interface QueryOptions {
  prompt: string;
  cwd?: string;                        // Phase 4 — subprocess working directory; set to mkdtemp path in live tests
  allowedTools?: string[];             // TOL-01 — CSV at argv boundary; live test passes ['read_file']
  approvalMode?: ApprovalMode | string;// TOL-02 — live test passes 'yolo' | 'plan'
  abortSignal?: AbortSignal;           // not required for live tests
  // ...other fields
}

interface QueryResult {
  text: string;
  chunks: MessageChunk[];               // live SC-1 test asserts no write_file events in this array
  sessionId: string;
  session: Session;
  stopReason: string | null;
  structured?: unknown;
}

// MessageChunk discriminated union (ts/src/parser/types.ts)
type MessageChunk =
  | { type: 'assistant'; ... }
  | { type: 'tool'; toolName: string; toolId: string; ... }   // SC-1 asserts toolName !== 'write_file'
  | { type: 'tool_result'; ... }
  | { type: 'result'; ... }
  | { type: 'system'; ... }
  | { type: 'thinking'; ... }
  | { type: 'rate_limit'; ... }
  | { type: 'workflow_dispatch'; ... };

<!-- Live test gating pattern (Vitest idiom) -->
import { describe, it, expect, beforeAll } from 'vitest';

const LIVE_ENABLED =
  process.env.RUN_LIVE_E2E === '1' && !!process.env.GEMINI_API_KEY;

describe.skipIf(!LIVE_ENABLED)('Phase 8 live E2E: tools + approval mode', () => {
  // ...tests here are skipped unless both env vars are set
});

<!-- Sandbox pattern -->
import { mkdtempSync, rmSync, statSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

let sandbox: string;
beforeAll(() => { sandbox = mkdtempSync(join(tmpdir(), 'gemini-sdk-live-')); });
afterAll(() => { rmSync(sandbox, { recursive: true, force: true }); });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create opt-in live integration test suite (ts/tests-live/e2e.live.spec.ts + vitest.live.config.ts + tests-live/README.md + package.json script)</name>

  <files>
    ts/tests-live/e2e.live.spec.ts,
    ts/vitest.live.config.ts,
    ts/tests-live/README.md,
    ts/package.json
  </files>

  <read_first>
    - ts/vitest.config.ts (mirror its structure, then narrow include to `tests-live/**/*.spec.ts`)
    - ts/package.json (current scripts.test command — `vitest run --passWithNoTests`; add test:live alongside)
    - ts/src/index.ts (confirm exports: query, queryFull, ApprovalMode, SchemaValidationError, UnsupportedFeatureError are all top-level exports)
    - ts/src/query/types.ts (confirm QueryOptions.cwd and QueryResult.chunks shapes)
    - ts/src/query/query.ts (confirm queryFull signature and that it returns QueryResult.chunks as MessageChunk[])
    - ts/src/parser/types.ts (confirm MessageChunk 'tool' variant has a toolName field — this is what SC-1 asserts on)
    - scripts/diff-test-names.sh (confirm lines 24-25 scan only `ts/src` — verify `tests-live/` is OUTSIDE this scan path so parity is preserved)
    - .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VERIFICATION.md (gaps section — the three tests below map 1:1 to the three listed missing test cases)
  </read_first>

  <action>
    **Step 1.** Create `ts/vitest.live.config.ts` with this exact content:

    ```typescript
    import { defineConfig } from 'vitest/config';

    export default defineConfig({
      test: {
        include: ['tests-live/**/*.spec.ts'],
        globals: false,
        environment: 'node',
        // Live tests make real gemini-cli subprocess calls + model inference;
        // allow generous timeout (90s) to accommodate cold-start + network + model latency.
        testTimeout: 90_000,
        hookTimeout: 30_000,
      },
    });
    ```

    **Step 2.** Create `ts/tests-live/README.md` with this exact content:

    ```markdown
    # Live E2E Test Suite (Phase 8: SC-1 + SC-2)

    This directory contains **opt-in integration tests** that spawn a real `gemini-cli`
    subprocess against the live Gemini API. They verify Phase 8 ROADMAP Success Criteria
    SC-1 (allowedTools enforcement) and SC-2 (approvalMode yolo + plan filesystem behavior).

    ## Why opt-in?

    These tests require:
    - A working `gemini-cli` install on PATH (or `GEMINI_BIN_PATH` set)
    - A valid `GEMINI_API_KEY` with quota
    - Network access
    - Real API spend (~3 prompts per run)

    For those reasons, they are **not** part of the default `pnpm test` suite. The default
    suite remains free, fast, and hermetic.

    ## How to run

    From the `ts/` directory:

    ```bash
    RUN_LIVE_E2E=1 GEMINI_API_KEY=sk-... pnpm test:live
    ```

    If either env var is missing, every test in this suite is **skipped** (not failed) via
    `describe.skipIf(!LIVE_ENABLED)` — so accidentally invoking `pnpm test:live` without
    the gate set produces a green no-op run rather than confusing failures.

    ## What runs

    Each test:
    1. Creates an isolated sandbox directory via `mkdtempSync(tmpdir(), 'gemini-sdk-live-')`.
    2. Runs `queryFull({ ..., cwd: sandbox })` so any filesystem mutations land in the sandbox.
    3. Asserts on the returned event stream (SC-1) or on post-run `fs.stat` of the sandbox (SC-2).
    4. Cleans up the sandbox in `afterAll`.

    ## CI guidance

    Do **not** enable `RUN_LIVE_E2E` on every push. Recommended pattern:
    - `workflow_dispatch` manual trigger with a dedicated `GEMINI_API_KEY` secret.
    - Optional nightly run if budget allows.
    - Pre-release gate before tagging.

    The default `pnpm test` / `uv run pytest` paths remain the per-push contract.

    ## Parity with Python

    The Python SDK emits byte-identical argv for `--allowed-tools` and `--approval-mode`
    (proved by `diff-test-names.sh` 205:205 parity and the argv unit tests in both languages).
    Live tests are TS-only because they verify **CLI-level behavior** downstream of the
    argv, which is language-agnostic. Mirroring the suite in Python would spend 3x API
    budget to re-verify the same CLI contract.

    If a future contributor disagrees, adding `python/tests-live/` is additive — this
    suite's gating pattern (`RUN_LIVE_E2E` + api-key presence) serves as the template.
    ```

    **Step 3.** Create `ts/tests-live/e2e.live.spec.ts` with this exact content:

    ```typescript
    /**
     * Phase 8 live E2E suite — opt-in integration tests against real gemini-cli.
     *
     * Gate: RUN_LIVE_E2E=1 AND GEMINI_API_KEY present. Otherwise all tests skip.
     *
     * Covers:
     *   SC-1  — allowedTools: ['read_file'] blocks write_file tool calls in the event stream
     *   SC-2a — approvalMode: 'yolo'  writes a file in the sandbox without prompting
     *   SC-2b — approvalMode: 'plan'  produces no filesystem mutations (fs.stat ENOENT)
     *
     * See ts/tests-live/README.md for run instructions, gating, and CI guidance.
     */

    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { mkdtempSync, rmSync, statSync, writeFileSync } from 'node:fs';
    import { tmpdir } from 'node:os';
    import { join } from 'node:path';
    import { queryFull, ApprovalMode } from '../src/index.js';
    import type { MessageChunk } from '../src/parser/types.js';

    const LIVE_ENABLED =
      process.env.RUN_LIVE_E2E === '1' && !!process.env.GEMINI_API_KEY;

    describe.skipIf(!LIVE_ENABLED)('Phase 8 live E2E: tools + approval mode', () => {
      let sandbox: string;

      beforeAll(() => {
        sandbox = mkdtempSync(join(tmpdir(), 'gemini-sdk-live-'));
        // Seed a readable file so read_file has something to act on for SC-1.
        writeFileSync(join(sandbox, 'README.txt'), 'hello from the sandbox\n', 'utf-8');
      });

      afterAll(() => {
        if (sandbox) rmSync(sandbox, { recursive: true, force: true });
      });

      // ---------------------------------------------------------------------------
      // SC-1: allowedTools enforcement
      // ---------------------------------------------------------------------------
      it('SC-1 allowedTools read_file blocks write_file tool calls in event stream', async () => {
        const result = await queryFull({
          prompt:
            'Read the file README.txt in the current directory, then write its uppercase ' +
            'contents to a new file called OUT.txt in the current directory.',
          allowedTools: ['read_file'],
          approvalMode: ApprovalMode.YOLO,
          cwd: sandbox,
        });

        // Inspect the full event stream — no tool chunk with toolName === 'write_file' may appear.
        const toolChunks = result.chunks.filter(
          (c: MessageChunk) => c.type === 'tool',
        );
        const writeFileCalls = toolChunks.filter(
          (c: any) => c.toolName === 'write_file',
        );

        expect(writeFileCalls).toHaveLength(0);

        // Defensive: also verify the filesystem — OUT.txt should NOT exist.
        expect(() => statSync(join(sandbox, 'OUT.txt'))).toThrow(/ENOENT/);
      });

      // ---------------------------------------------------------------------------
      // SC-2a: approvalMode yolo — file write succeeds without prompting
      // ---------------------------------------------------------------------------
      it('SC-2a approvalMode yolo writes a file end to end in sandbox without prompting', async () => {
        const targetPath = join(sandbox, 'yolo-output.txt');

        const result = await queryFull({
          prompt:
            'Write the single word hello to a file called yolo-output.txt in the current directory. ' +
            'Do not ask for confirmation.',
          approvalMode: ApprovalMode.YOLO,
          cwd: sandbox,
        });

        // Post-run fs.stat must succeed — the file exists.
        const stat = statSync(targetPath);
        expect(stat.isFile()).toBe(true);

        // The query must have completed with a terminal result chunk (not stuck on a prompt).
        expect(result.stopReason).toBeTruthy();
      });

      // ---------------------------------------------------------------------------
      // SC-2b: approvalMode plan — no filesystem mutations
      // ---------------------------------------------------------------------------
      it('SC-2b approvalMode plan produces no filesystem mutations verified via fs stat ENOENT', async () => {
        const targetPath = join(sandbox, 'plan-output.txt');

        const result = await queryFull({
          prompt:
            'Plan how you would write the single word hello to a file called plan-output.txt ' +
            'in the current directory. Describe the plan but do not execute it.',
          approvalMode: ApprovalMode.PLAN,
          cwd: sandbox,
        });

        // Post-run fs.stat MUST throw ENOENT — the file must not exist.
        expect(() => statSync(targetPath)).toThrow(/ENOENT/);

        // The query must have produced some output (the plan text itself).
        expect(result.text.length).toBeGreaterThan(0);
      });
    });
    ```

    **Step 4.** Edit `ts/package.json` to add a `test:live` script alongside the existing `test` script. The `scripts` block must change from:

    ```json
    "scripts": {
      "test": "vitest run --passWithNoTests",
      "test:coverage": "vitest run --coverage --passWithNoTests",
      "build": "tsc",
      "typecheck": "tsc --noEmit",
      "gen:errors": "node ../scripts/gen-errors.mjs"
    }
    ```

    to:

    ```json
    "scripts": {
      "test": "vitest run --passWithNoTests",
      "test:coverage": "vitest run --coverage --passWithNoTests",
      "test:live": "vitest run --config vitest.live.config.ts --passWithNoTests",
      "build": "tsc",
      "typecheck": "tsc --noEmit",
      "gen:errors": "node ../scripts/gen-errors.mjs"
    }
    ```

    Preserve all other fields (`name`, `version`, `type`, `exports`, `dependencies`, `devDependencies`, `engines`) exactly as they currently are.

    **Parity preservation note (for the executor):** After this task, running
    `bash scripts/diff-test-names.sh` from the repo root must still return `OK: TS and Python
    test names match (205 tests).` — because `diff-test-names.sh` scans `ts/src/` (not
    `ts/tests-live/`). If parity breaks, STOP and report rather than attempting to fix.
  </action>

  <verify>
    <automated>
      bash -c 'set -e;
        test -f ts/tests-live/e2e.live.spec.ts &&
        test -f ts/vitest.live.config.ts &&
        test -f ts/tests-live/README.md &&
        grep -c "RUN_LIVE_E2E" ts/tests-live/e2e.live.spec.ts &&
        grep -c "describe.skipIf" ts/tests-live/e2e.live.spec.ts &&
        grep -c "mkdtempSync" ts/tests-live/e2e.live.spec.ts &&
        grep -c "write_file" ts/tests-live/e2e.live.spec.ts &&
        grep -c "ApprovalMode.YOLO" ts/tests-live/e2e.live.spec.ts &&
        grep -c "ApprovalMode.PLAN" ts/tests-live/e2e.live.spec.ts &&
        grep -c "statSync" ts/tests-live/e2e.live.spec.ts &&
        grep -c "ENOENT" ts/tests-live/e2e.live.spec.ts &&
        grep -c "tests-live" ts/vitest.live.config.ts &&
        grep -c "test:live" ts/package.json &&
        grep -c "vitest.live.config" ts/package.json &&
        grep -c "RUN_LIVE_E2E" ts/tests-live/README.md &&
        grep -c "GEMINI_API_KEY" ts/tests-live/README.md &&
        cd ts &&
        pnpm test 2>&1 | tail -5 &&
        RUN_LIVE_E2E=0 pnpm test:live 2>&1 | tail -5 &&
        cd .. &&
        bash scripts/diff-test-names.sh 2>&1 | tail -3'
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f ts/tests-live/e2e.live.spec.ts` exits 0
    - `test -f ts/vitest.live.config.ts` exits 0
    - `test -f ts/tests-live/README.md` exits 0
    - `wc -l ts/tests-live/e2e.live.spec.ts | awk '{print $1}'` returns a number >= 120
    - `wc -l ts/vitest.live.config.ts | awk '{print $1}'` returns a number >= 8
    - `wc -l ts/tests-live/README.md | awk '{print $1}'` returns a number >= 25
    - `grep -c "RUN_LIVE_E2E" ts/tests-live/e2e.live.spec.ts` returns a number >= 2
    - `grep -c "GEMINI_API_KEY" ts/tests-live/e2e.live.spec.ts` returns a number >= 1
    - `grep -c "describe.skipIf" ts/tests-live/e2e.live.spec.ts` returns 1
    - `grep -c "mkdtempSync" ts/tests-live/e2e.live.spec.ts` returns 1
    - `grep -c "rmSync" ts/tests-live/e2e.live.spec.ts` returns 1
    - `grep -c "statSync" ts/tests-live/e2e.live.spec.ts` returns a number >= 3
    - `grep -c "ENOENT" ts/tests-live/e2e.live.spec.ts` returns a number >= 2
    - `grep -c "ApprovalMode.YOLO" ts/tests-live/e2e.live.spec.ts` returns a number >= 2
    - `grep -c "ApprovalMode.PLAN" ts/tests-live/e2e.live.spec.ts` returns 1
    - `grep -c "allowedTools" ts/tests-live/e2e.live.spec.ts` returns a number >= 2
    - `grep -c "write_file" ts/tests-live/e2e.live.spec.ts` returns a number >= 2
    - `grep -c "read_file" ts/tests-live/e2e.live.spec.ts` returns a number >= 2
    - `grep -c "SC-1\|SC-2a\|SC-2b" ts/tests-live/e2e.live.spec.ts` returns a number >= 3
    - `grep -c "queryFull" ts/tests-live/e2e.live.spec.ts` returns a number >= 3
    - `grep -c "tests-live" ts/vitest.live.config.ts` returns a number >= 1
    - `grep -c "testTimeout" ts/vitest.live.config.ts` returns 1
    - `grep -c "test:live" ts/package.json` returns 1
    - `grep -c "vitest.live.config" ts/package.json` returns 1
    - `grep -c "RUN_LIVE_E2E=1" ts/tests-live/README.md` returns a number >= 1
    - `grep -c "GEMINI_API_KEY" ts/tests-live/README.md` returns a number >= 2
    - `grep -c "pnpm test:live" ts/tests-live/README.md` returns a number >= 1
    - `grep -c "SC-1\|SC-2" ts/tests-live/README.md` returns a number >= 1
    - `grep -c "opt-in\|opt in" ts/tests-live/README.md` returns a number >= 1
    - `cd ts && pnpm test 2>&1 | grep -cE "(passed|passing|no test)"` — default suite still completes (>=1 summary line)
    - `cd ts && RUN_LIVE_E2E=0 pnpm test:live 2>&1 | grep -cE "(skipped|no tests|0 passed)"` — with gate off, zero tests run (skipped or collected-zero)
    - `bash scripts/diff-test-names.sh 2>&1 | grep -c "OK: TS and Python test names match (205 tests)"` returns 1 (parity NOT broken)
  </acceptance_criteria>

  <done>
    Four files created/modified:
    1. `ts/tests-live/e2e.live.spec.ts` — three live tests (SC-1, SC-2a, SC-2b) gated by RUN_LIVE_E2E + GEMINI_API_KEY presence, using describe.skipIf + mkdtempSync sandbox.
    2. `ts/vitest.live.config.ts` — dedicated config with include pattern scoped to tests-live/.
    3. `ts/tests-live/README.md` — contributor instructions (opt-in rationale, env vars, run command, CI guidance, Python-parity rationale).
    4. `ts/package.json` — `test:live` script added pointing at vitest.live.config.ts.

    Verified: default `pnpm test` still passes; `RUN_LIVE_E2E=0 pnpm test:live` produces zero failures; `scripts/diff-test-names.sh` reports 205:205 parity unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile 08-VALIDATION.md + add contributor sections to docs/tools.md and docs/structured-output.md</name>

  <files>
    .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md,
    docs/tools.md,
    docs/structured-output.md
  </files>

  <read_first>
    - .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md (current file; specifically the `## Manual-Only Verifications` section near line 65 and the Wave 0 Requirements list)
    - .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VERIFICATION.md (SC-1 and SC-2 gap text — this plan resolves those entries; source the exact wording from the `human_verification` block for the new table)
    - docs/tools.md (existing file created in 08-06; specifically the `## See Also` section at the end — new `## Contributors: Live E2E Suite` section goes just before it)
    - docs/structured-output.md (existing file created in 08-06; same — new `## Contributors: Live E2E Suite` section just before See Also)
    - ts/tests-live/README.md (created in Task 1 above — docs reference this file relatively)
  </read_first>

  <action>
    **Step 1.** Edit `.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md`:

    Replace the `## Manual-Only Verifications` section (currently lines ~64-72, the table headed `| Behavior | Requirement | Why Manual | Test Instructions |`) with a new `## Automated-Opt-In Verifications` section. The new section must contain this exact content (drop into the existing file at the same position):

    ```markdown
    ## Automated-Opt-In Verifications

    > **Status update (2026-04-20):** Previously categorized as "Manual-Only" by Phase 8
    > plans 01-06. As of plan 08-07, these are **Automated-Opt-In** — the test file
    > `ts/tests-live/e2e.live.spec.ts` automates them behind an env-var gate. See
    > `ts/tests-live/README.md` for run instructions.

    | Behavior | Requirement | Automated Command | Test File | Env Gate |
    |----------|-------------|-------------------|-----------|----------|
    | `allowedTools: ['read_file']` blocks `write_file` in the live CLI event stream | SC-1 / TOL-01 | `cd ts && RUN_LIVE_E2E=1 GEMINI_API_KEY=sk-... pnpm test:live` | `ts/tests-live/e2e.live.spec.ts` → "SC-1 allowedTools read_file blocks write_file tool calls in event stream" | `RUN_LIVE_E2E=1` + `GEMINI_API_KEY` present |
    | `approvalMode: 'yolo'` writes a file in sandbox without prompting | SC-2a / TOL-02 | same as above | same file → "SC-2a approvalMode yolo writes a file end to end in sandbox without prompting" | same |
    | `approvalMode: 'plan'` produces no filesystem mutations (fs.stat ENOENT) | SC-2b / TOL-02 | same as above | same file → "SC-2b approvalMode plan produces no filesystem mutations verified via fs stat ENOENT" | same |

    ### Why opt-in, not default?

    These tests require a live `gemini-cli` install, a valid `GEMINI_API_KEY`, network
    access, and real API spend. Running them on every push would tax the project budget
    for negligible additional confidence (the argv unit tests in `buildArgv.spec.ts`
    already cover the SDK's contract; these live tests verify the CLI's own enforcement
    downstream of the argv).

    ### Recommended CI integration

    - **Per push:** default `pnpm test` / `uv run pytest` — free, fast, hermetic. No live tests.
    - **On workflow_dispatch:** manually-triggered CI job runs `pnpm test:live` with a
      dedicated `GEMINI_API_KEY` secret. Use before tagging releases.
    - **Nightly (optional):** if budget allows, schedule a nightly live-suite run to
      catch upstream `gemini-cli` regressions early.

    ### Parity rationale — TS-only

    The Python SDK emits byte-identical `--allowed-tools` / `--approval-mode` argv
    (proved by `diff-test-names.sh` 205:205 parity and the argv unit tests in both
    languages). The live suite verifies CLI-level behavior downstream of the argv,
    which is language-agnostic — mirroring in Python would spend 3x API budget to
    re-verify the same contract. If a future contributor disagrees, `python/tests-live/`
    is additive.
    ```

    Then update the file's frontmatter at the top: set `nyquist_compliant: true` and `wave_0_complete: true` (both are currently false). Leave all other frontmatter fields unchanged.

    Also, update the `## Validation Sign-Off` section at the bottom: flip `Approval: pending` to `Approval: gap closed (plan 08-07)`.

    **Step 2.** Edit `docs/tools.md`:

    Insert a new section `## Contributors: Live E2E Suite` immediately BEFORE the existing `## See Also` section at the end of the file. The new section must contain:

    ```markdown
    ## Contributors: Live E2E Suite

    Phase 8 ships an opt-in live integration test suite at `ts/tests-live/e2e.live.spec.ts`
    that spawns a real `gemini-cli` to verify CLI-level enforcement of `allowedTools` and
    `approvalMode`. It is **not** part of the default `pnpm test` run.

    To execute it locally:

    ```bash
    cd ts
    RUN_LIVE_E2E=1 GEMINI_API_KEY=sk-... pnpm test:live
    ```

    Both env vars are required — missing either one skips (does not fail) all tests in the
    suite. See [`ts/tests-live/README.md`](../ts/tests-live/README.md) for sandbox behavior,
    cost estimates, and CI guidance.
    ```

    Preserve the existing `## See Also` section and all other content exactly as is.

    **Step 3.** Edit `docs/structured-output.md`:

    Insert the same new section `## Contributors: Live E2E Suite` immediately BEFORE the existing `## See Also` section at the end of the file. The new section must contain:

    ```markdown
    ## Contributors: Live E2E Suite

    The `outputSchema` retry loop is covered by mock-spawn unit tests in
    `ts/src/query/query.spec.ts` (TS) and `python/tests/test_query.py` (Python) — no
    live-CLI test is needed here, because validation failures can be induced
    deterministically with mocked responses.

    The companion live suite at `ts/tests-live/e2e.live.spec.ts` covers the sibling
    Phase 8 features (`allowedTools`, `approvalMode`) that can only be verified against
    a real `gemini-cli`. See [`ts/tests-live/README.md`](../ts/tests-live/README.md)
    and [`docs/tools.md`](./tools.md) for run instructions.
    ```

    Preserve the existing `## See Also` section and all other content exactly as is.
  </action>

  <verify>
    <automated>
      bash -c 'set -e;
        grep -c "Automated-Opt-In" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md &&
        grep -c "ts/tests-live/e2e.live.spec.ts" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md &&
        grep -c "RUN_LIVE_E2E" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md &&
        grep -c "nyquist_compliant: true" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md &&
        grep -c "Approval: gap closed" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md &&
        grep -c "Contributors: Live E2E Suite" docs/tools.md &&
        grep -c "RUN_LIVE_E2E" docs/tools.md &&
        grep -c "tests-live/README.md" docs/tools.md &&
        grep -c "Contributors: Live E2E Suite" docs/structured-output.md &&
        grep -c "RUN_LIVE_E2E" docs/structured-output.md &&
        grep -c "tests-live/README.md" docs/structured-output.md'
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "## Automated-Opt-In Verifications" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns 1
    - `grep -c "## Manual-Only Verifications" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns 0 (section replaced, not duplicated)
    - `grep -c "RUN_LIVE_E2E" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns a number >= 2
    - `grep -c "ts/tests-live/e2e.live.spec.ts" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns a number >= 3
    - `grep -c "SC-1\|SC-2a\|SC-2b" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns a number >= 3
    - `grep -c "nyquist_compliant: true" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns 1
    - `grep -c "wave_0_complete: true" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns 1
    - `grep -c "Approval: gap closed (plan 08-07)" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns 1
    - `grep -c "pnpm test:live" .planning/phases/08-tools-approval-mode-structured-output-best-effort/08-VALIDATION.md` returns a number >= 1
    - `grep -c "^## Contributors: Live E2E Suite" docs/tools.md` returns 1
    - `grep -c "RUN_LIVE_E2E=1 GEMINI_API_KEY" docs/tools.md` returns a number >= 1
    - `grep -c "ts/tests-live/README.md" docs/tools.md` returns a number >= 1
    - `grep -c "pnpm test:live" docs/tools.md` returns a number >= 1
    - `grep -c "^## See Also" docs/tools.md` returns 1 (preserved, not duplicated)
    - `grep -c "^## Contributors: Live E2E Suite" docs/structured-output.md` returns 1
    - `grep -c "RUN_LIVE_E2E" docs/structured-output.md` returns a number >= 1
    - `grep -c "ts/tests-live/README.md" docs/structured-output.md` returns a number >= 1
    - `grep -c "^## See Also" docs/structured-output.md` returns 1 (preserved, not duplicated)
    - `wc -l docs/tools.md | awk '{print $1}'` returns a number >= 115 (was 108, gained ~10 lines for new section)
    - `wc -l docs/structured-output.md | awk '{print $1}'` returns a number >= 200 (was 194, gained ~10 lines)
    - Order check: `grep -n "## Contributors: Live E2E Suite\|## See Also" docs/tools.md | head -2` — Contributors line must come BEFORE See Also line
    - Order check: same for docs/structured-output.md
  </acceptance_criteria>

  <done>
    Three files modified:
    1. `08-VALIDATION.md` — Manual-Only section replaced with Automated-Opt-In section citing the new test file, three SC rows, env-gate rationale, CI integration guidance, and TS-only parity note. Frontmatter flags (nyquist_compliant, wave_0_complete) flipped to true. Sign-off marked "gap closed (plan 08-07)".
    2. `docs/tools.md` — new Contributors: Live E2E Suite section inserted before See Also, with `RUN_LIVE_E2E=1 GEMINI_API_KEY=...` run command and link to ts/tests-live/README.md.
    3. `docs/structured-output.md` — same Contributors section inserted before See Also, noting that outputSchema is mock-tested and pointing to the live suite for sibling features.
  </done>
</task>

</tasks>

<verification>
Overall plan verification:

1. **Files exist:** `ts/tests-live/e2e.live.spec.ts`, `ts/vitest.live.config.ts`, `ts/tests-live/README.md` all present; `ts/package.json`, `docs/tools.md`, `docs/structured-output.md`, and `08-VALIDATION.md` all modified.

2. **Default path unchanged:** `cd ts && pnpm test` still exits 0 with all existing tests passing (no regression).

3. **Parity preserved:** `bash scripts/diff-test-names.sh` still reports `OK: TS and Python test names match (205 tests).` — `ts/tests-live/` is outside the scan path.

4. **Gate works correctly:**
   - `cd ts && RUN_LIVE_E2E=0 pnpm test:live` → zero failures (tests skipped).
   - `cd ts && pnpm test:live` (no env vars) → zero failures (tests skipped).
   - `cd ts && RUN_LIVE_E2E=1 GEMINI_API_KEY=<valid> pnpm test:live` → tests actually run (not verified automatically; documented in README).

5. **Documentation reconciled:**
   - 08-VALIDATION.md no longer contains "Manual-Only Verifications" section.
   - 08-VALIDATION.md contains "Automated-Opt-In Verifications" section citing `ts/tests-live/e2e.live.spec.ts` + `RUN_LIVE_E2E`.
   - 08-VALIDATION.md frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`, sign-off notes "gap closed (plan 08-07)".
   - docs/tools.md + docs/structured-output.md both have Contributors: Live E2E Suite sections before See Also.

6. **Gap closure:** Verification rerun after this plan merges should score Phase 8 as 4/4 automated (opt-in counts as automated per reconciled VALIDATION.md), not 2/4.
</verification>

<success_criteria>
- SC-1 automated-opt-in: `ts/tests-live/e2e.live.spec.ts` contains a test that spawns live gemini-cli with `allowedTools: ['read_file']` and asserts `write_file` tool calls are absent from the event stream.
- SC-2a automated-opt-in: same file has a test that runs `approvalMode: 'yolo'` in a temp sandbox and asserts `fs.statSync(targetPath).isFile() === true` post-run.
- SC-2b automated-opt-in: same file has a test that runs `approvalMode: 'plan'` in a temp sandbox and asserts `fs.statSync(targetPath)` throws ENOENT post-run.
- Opt-in gate: both `RUN_LIVE_E2E=1` AND `GEMINI_API_KEY` must be set; absence of either skips the entire describe block via `describe.skipIf`.
- Budget safety: default `pnpm test` never touches the live suite; only `pnpm test:live` with both env vars runs it.
- Parity safety: `diff-test-names.sh` scans `ts/src/**/*.spec.ts`; the new file lives in `ts/tests-live/`, so 205:205 parity is preserved (verified post-task).
- Documentation reconciliation: 08-VALIDATION.md's SC-1/SC-2 rows move from "Manual-Only" to "Automated-Opt-In" with a pointer to the new test file and the env gate.
- Contributor discoverability: docs/tools.md + docs/structured-output.md + ts/tests-live/README.md all document the `RUN_LIVE_E2E=1 pnpm test:live` pattern.
- No Python mirror in v1 of this plan — justified in README + VALIDATION.md ("CLI-level behavior downstream of byte-identical argv"). Deferred as additive future work.
</success_criteria>

<output>
After completion, create `.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-07-SUMMARY.md` documenting:
- Final line counts: e2e.live.spec.ts, vitest.live.config.ts, tests-live/README.md, docs/tools.md (new total), docs/structured-output.md (new total)
- Output of `bash scripts/diff-test-names.sh | tail -3` (must show 205:205 parity preserved)
- Output of `cd ts && pnpm test 2>&1 | tail -3` (must show existing suite still green)
- Output of `cd ts && RUN_LIVE_E2E=0 pnpm test:live 2>&1 | tail -5` (must show zero failures when gate off)
- Confirmation that 08-VALIDATION.md frontmatter has `nyquist_compliant: true` + `wave_0_complete: true`
- Confirmation that 08-VERIFICATION.md gap closure is now possible: SC-1, SC-2a, SC-2b all have automated opt-in tests
- Requirement coverage: this plan does NOT add new SDK code; it closes a test-coverage gap for TOL-01 and TOL-02 that existing plans 08-02 and 08-05 left as "Manual-Only"
- Deferred: Python live-suite mirror (justification: argv parity + CLI-level downstream). Tracked as additive future work if contributors request it.
- Any deviations from the plan (e.g., if vitest.live.config.ts needed additional fields; if diff-test-names.sh behavior differed from assumption; if default `pnpm test` behavior changed)
</output>
