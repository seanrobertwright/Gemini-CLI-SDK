---
phase: 08-tools-approval-mode-structured-output-best-effort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/errors.yaml
  - scripts/gen-errors.mjs
  - scripts/gen-errors.py
  - scripts/lint-errors.sh
  - ts/src/errors/errors.ts
  - python/src/gemini_sdk/errors/errors.py
  - ts/src/errors/index.ts
  - python/src/gemini_sdk/errors/__init__.py
autonomous: true
requirements: []
gap_closure: false

must_haves:
  truths:
    - "SchemaValidationError class exists in TS with retryable=false and bucket='unknown'"
    - "SchemaValidationError class exists in Python with retryable=False and bucket='unknown'"
    - "errors.yaml supports a `source: 'sdk'|'stderr'` discriminator, default 'stderr'"
    - "lint-errors.sh passes with the new SchemaValidationError entry present"
  artifacts:
    - path: "spec/errors.yaml"
      provides: "SchemaValidationError entry with source: sdk"
      contains: "SchemaValidationError"
    - path: "ts/src/errors/errors.ts"
      provides: "Generated SchemaValidationError class"
      contains: "export class SchemaValidationError"
    - path: "python/src/gemini_sdk/errors/errors.py"
      provides: "Generated SchemaValidationError class"
      contains: "class SchemaValidationError"
    - path: "ts/src/errors/index.ts"
      provides: "SchemaValidationError barrel export"
      contains: "SchemaValidationError"
    - path: "python/src/gemini_sdk/errors/__init__.py"
      provides: "SchemaValidationError barrel export"
      contains: "SchemaValidationError"
    - path: "scripts/lint-errors.sh"
      provides: "3-way YAML/TS/Python sync with source:sdk tolerance"
  key_links:
    - from: "spec/errors.yaml"
      to: "ts/src/errors/errors.ts"
      via: "node scripts/gen-errors.mjs"
      pattern: "SchemaValidationError"
    - from: "spec/errors.yaml"
      to: "python/src/gemini_sdk/errors/errors.py"
      via: "python scripts/gen-errors.py"
      pattern: "SchemaValidationError"
    - from: "scripts/lint-errors.sh"
      to: "YAML/TS/Python class name sets"
      via: "comm -3 cross-check"
      pattern: "class set mismatch"
---

<objective>
Extend the Phase-5 error taxonomy codegen pipeline to support SDK-thrown errors (in addition to stderr/exit-code classified ones) and generate the new `SchemaValidationError` class used by Phase 8 structured-output retry.

Purpose: Phase 8's best-effort structured output (OUT-03) throws `SchemaValidationError` directly from `queryFull()` after a single retry fails. This class must exist in both TS and Python, must be generated from the single YAML source (PAR-05 invariant), and must be understood by `lint-errors.sh` as a SDK-source class (no stderr regex expected). Runs FIRST so that 08-04 (TS query wiring) and 08-05 (Python port) can import the class.

Output: Updated `spec/errors.yaml` with new `source` discriminator + `SchemaValidationError` entry. Updated codegen scripts that pass through the source field. Regenerated TS and Python class files. Updated barrel exports. Updated linter that tolerates `source: 'sdk'` entries.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-CONTEXT.md
@.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-RESEARCH.md

# Canonical source file for this plan
@spec/errors.yaml
@scripts/gen-errors.mjs
@scripts/gen-errors.py
@scripts/lint-errors.sh
@ts/src/errors/errors.ts
@python/src/gemini_sdk/errors/errors.py
@ts/src/errors/index.ts
@python/src/gemini_sdk/errors/__init__.py

<interfaces>
<!-- Current errors.yaml structure (Phase 5 baseline) -->
YAML entry shape (existing):
```yaml
- name: <ClassName>
  base: <BaseClassName>         # 'Error' → TS Error / Python Exception; otherwise a previously-defined class
  bucket: rate_limit|auth|model_access|crash|unknown
  retryable: true|false
  message_template: "..."       # optional
  parent: <ClassName>           # optional, for subclasses like NotConfigured(AuthError)
  stream_matchers: [...]        # optional, consumed by ErrorMapper (not by gen-errors codegen)
  stderr_patterns: [...]        # optional, consumed by ErrorMapper
  exit_codes: [...]             # optional, consumed by ErrorMapper
  retry_after_ms_source: "..."  # optional, RateLimitError only
```

YAML entry shape (Phase 8 extension):
```yaml
- name: SchemaValidationError
  base: GeminiError
  source: sdk                   # NEW field: 'stderr' (default, existing behavior) | 'sdk' (this class is thrown by SDK code, not classified from stderr)
  bucket: unknown
  retryable: false
  message_template: "..."
```

Current gen-errors.mjs codegen (TS):
```js
for (const entry of doc.errors) {
  const baseClass = entry.base === 'Error' ? 'Error' : entry.base;
  // ... emits: export class <name> extends <baseClass> { ... }
}
```

Current gen-errors.py codegen (Python):
```python
for entry in doc["errors"]:
    base = "Exception" if entry["base"] == "Error" else entry["base"]
    # ... emits: class <name>(<base>): ...
```

lint-errors.sh class-name extraction (unchanged — the codegen output format is identical, just one more class):
- YAML: `grep -E '^\s*-\s*name:\s*[A-Za-z0-9_]+' spec/errors.yaml | grep -oE '[A-Za-z0-9_]+$'`
- TS: `grep -oE '^export class [A-Za-z0-9_]+' ts/src/errors/errors.ts | awk '{print $3}'`
- Python: `grep -oE '^class [A-Za-z0-9_]+' python/src/gemini_sdk/errors/errors.py | awk '{print $2}'`

Existing TS barrel (`ts/src/errors/index.ts`):
```ts
export * from './errors.js';
export { ErrorMapper } from './ErrorMapper.js';
export type { StreamErrorEvent } from './ErrorMapper.js';
```
Uses `export *` → SchemaValidationError auto-exported once generated; no edit strictly required but we add a named re-export for discoverability.

Existing Python barrel (`python/src/gemini_sdk/errors/__init__.py`): uses explicit `from .errors import (...)` list — MUST add `SchemaValidationError` explicitly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SchemaValidationError to spec/errors.yaml + extend codegen source discriminator</name>

  <files>spec/errors.yaml, scripts/gen-errors.mjs, scripts/gen-errors.py, ts/src/errors/errors.ts, python/src/gemini_sdk/errors/errors.py</files>
  <read_first>
    - spec/errors.yaml (current taxonomy — study shape of existing 15 entries)
    - scripts/gen-errors.mjs (current TS codegen — verify we understand the loop body that emits class declarations)
    - scripts/gen-errors.py (current Python codegen — mirror understanding)
    - ts/src/errors/errors.ts (current generated output — note AUTO-GENERATED header, class-per-entry shape)
    - python/src/gemini_sdk/errors/errors.py (current Python generated output)
  </read_first>

  <behavior>
    - Test 1 (implicit): After running `node scripts/gen-errors.mjs`, `ts/src/errors/errors.ts` contains `export class SchemaValidationError extends GeminiError { ... bucket: 'unknown' ... retryable: false ... }`.
    - Test 2 (implicit): After running `uv run python ../scripts/gen-errors.py` in python/, `python/src/gemini_sdk/errors/errors.py` contains `class SchemaValidationError(GeminiError):` with `bucket = 'unknown'` and `retryable = False`.
    - Test 3: `source: sdk` discriminator is stored on entries that set it; codegen reads it but does NOT emit different class shape (the source field is metadata for ErrorMapper + lint-errors, not for class generation in v1).
    - Test 4: Existing 15 entries (no `source` field) continue to codegen byte-identically (default behavior preserved).
  </behavior>

  <action>
    Edit `spec/errors.yaml`: append new entry at end of `errors:` list (after `GeminiNotFoundError`):
    ```yaml
      - name: SchemaValidationError
        base: GeminiError
        source: sdk
        bucket: unknown
        retryable: false
        message_template: "Schema validation failed after retry"
    ```

    Edit `scripts/gen-errors.mjs`: read `entry.source` field (default to 'stderr' when absent). Pass through as metadata comment above each class declaration so generated TS is self-documenting. Insert inside the `for (const entry of doc.errors)` loop, BEFORE the `out += \`export class ...\`` line:
    ```js
    const source = entry.source || 'stderr';
    out += `// source: ${source}\n`;
    ```
    This is a one-line addition; no other logic changes. Regeneration of existing classes produces identical class bodies with one new comment line above each.

    Edit `scripts/gen-errors.py` symmetrically: read `entry.get("source", "stderr")` and emit a comment above each class. Insert inside the `for entry in doc["errors"]:` loop, BEFORE `out += f"class {entry['name']}({base}):\n"`:
    ```python
    source = entry.get("source", "stderr")
    out += f"# source: {source}\n"
    ```

    Regenerate both language files:
    ```bash
    node scripts/gen-errors.mjs
    (cd python && uv run python ../scripts/gen-errors.py)
    ```

    Verify the output contains SchemaValidationError in both files.
  </action>

  <verify>
    <automated>
      bash -c 'grep -q "name: SchemaValidationError" spec/errors.yaml &&
               grep -q "source: sdk" spec/errors.yaml &&
               grep -q "^export class SchemaValidationError extends GeminiError" ts/src/errors/errors.ts &&
               grep -q "^class SchemaValidationError(GeminiError):" python/src/gemini_sdk/errors/errors.py &&
               grep -q "^// source: stderr" ts/src/errors/errors.ts &&
               grep -q "^// source: sdk" ts/src/errors/errors.ts &&
               grep -q "^# source: stderr" python/src/gemini_sdk/errors/errors.py &&
               grep -q "^# source: sdk" python/src/gemini_sdk/errors/errors.py'
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^  - name: SchemaValidationError$" spec/errors.yaml` returns `1`
    - `grep -c "^    source: sdk$" spec/errors.yaml` returns `1` (exactly one SDK-source entry in v1)
    - `grep -c "^export class SchemaValidationError extends GeminiError {" ts/src/errors/errors.ts` returns `1`
    - `grep -c "^class SchemaValidationError(GeminiError):" python/src/gemini_sdk/errors/errors.py` returns `1`
    - `grep -c "readonly bucket: ArchonBucket = 'unknown';" ts/src/errors/errors.ts | tr -d ' '` returns a number ≥ 5 (existing ParseError/InvalidPromptError/UnsupportedFeatureError/GeminiError/GeminiNotFoundError + new SchemaValidationError)
    - `grep "bucket: ArchonBucket = 'unknown'" ts/src/errors/errors.ts -A 1 | grep -q "retryable: boolean = false"` succeeds (SchemaValidationError's retryable is false)
    - `grep -A 2 "^class SchemaValidationError" python/src/gemini_sdk/errors/errors.py | grep -q "bucket: ArchonBucket = 'unknown'"` succeeds
    - `grep -A 3 "^class SchemaValidationError" python/src/gemini_sdk/errors/errors.py | grep -q "retryable: bool = False"` succeeds
    - `grep -c "^// source:" ts/src/errors/errors.ts` returns `16` (one comment per class, 15 existing + 1 new)
    - `grep -c "^# source:" python/src/gemini_sdk/errors/errors.py` returns `16`
    - `cd ts && pnpm typecheck` exits 0 (regenerated file compiles cleanly)
    - Running `node scripts/gen-errors.mjs` is idempotent: a second run produces no git diff vs the first
  </acceptance_criteria>

  <done>
    errors.yaml has SchemaValidationError entry with source: sdk. Codegen scripts emit source comments. Both generated files contain the new class with retryable=false, bucket=unknown. TS typecheck passes. Python syntactically valid.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update lint-errors.sh to handle source:sdk + update barrel exports</name>

  <files>scripts/lint-errors.sh, python/src/gemini_sdk/errors/__init__.py, ts/src/errors/index.ts</files>
  <read_first>
    - scripts/lint-errors.sh (current linter — study the regeneration + drift-check + class-name set comparison flow)
    - ts/src/errors/index.ts (current barrel — uses `export * from './errors.js'` which auto-picks up new classes)
    - python/src/gemini_sdk/errors/__init__.py (current barrel — explicit import list that MUST be edited)
    - spec/errors.yaml (confirm SchemaValidationError entry from Task 1 is present)
    - ts/src/errors/errors.ts + python/src/gemini_sdk/errors/errors.py (confirm SchemaValidationError was generated by Task 1)
  </read_first>

  <behavior>
    - Test 1: `bash scripts/lint-errors.sh` exits 0 with SchemaValidationError now in the 3-way class-name set.
    - Test 2: Lint output contains "16 classes in sync across YAML, TS, Python" (was 15 before Task 1, now 16).
    - Test 3: If any of the three sources (YAML / TS / Python) is missing SchemaValidationError, the linter exits non-zero with a class-set mismatch message.
    - Test 4: TS barrel unchanged content-wise (export * auto-picks up SchemaValidationError); `import { SchemaValidationError } from '@gemini-sdk/core'` resolves.
    - Test 5: Python barrel adds SchemaValidationError to both the import list and __all__.
  </behavior>

  <action>
    The existing `lint-errors.sh` already uses set-equality between YAML/TS/Python class names — it automatically handles a new class as long as all three sources agree. No changes needed to the linter logic itself. Verify it passes with the new class; if it fails due to regeneration drift (git diff from Task 1 outputs), that is correct and expected — the point is set-equality, which now includes 16 classes in all three sources.

    **No edits to lint-errors.sh needed.** The `source: sdk` discriminator is purely metadata for `ErrorMapper` (Phase 8 query wiring, plan 08-04) and does not require linter changes. The linter's job is class-set equality across YAML/TS/Python, which still holds.

    Edit `python/src/gemini_sdk/errors/__init__.py`:
    - Add `SchemaValidationError` to the `from .errors import (...)` tuple (alphabetically near the end or grouped with other Phase-8 additions — place after `GeminiNotFoundError`).
    - Add `"SchemaValidationError"` to `__all__` list.

    TS barrel (`ts/src/errors/index.ts`) is unchanged: the existing `export * from './errors.js'` re-exports SchemaValidationError automatically. Verify the type resolves via typecheck.

    Run the linter:
    ```bash
    bash scripts/lint-errors.sh
    ```
    Expected: exits 0 with "[lint-errors] OK: 16 classes in sync across YAML, TS, Python."
  </action>

  <verify>
    <automated>
      bash -c 'bash scripts/lint-errors.sh 2>&1 | tee /tmp/lint-errors-out.txt &&
               grep -q "OK: 16 classes in sync" /tmp/lint-errors-out.txt &&
               grep -q "SchemaValidationError" python/src/gemini_sdk/errors/__init__.py &&
               python -c "from gemini_sdk.errors import SchemaValidationError; e = SchemaValidationError(); assert e.bucket == \"unknown\" and e.retryable is False and isinstance(e, Exception)" 2>&1'
    </automated>
  </verify>

  <acceptance_criteria>
    - `bash scripts/lint-errors.sh` exits 0
    - `bash scripts/lint-errors.sh 2>&1 | grep -c "OK: 16 classes in sync"` returns `1`
    - `bash scripts/lint-errors.sh 2>&1 | grep -E "FAIL|mismatch"` returns no matches (exit code 1 from grep)
    - `grep -c "SchemaValidationError" python/src/gemini_sdk/errors/__init__.py` returns `2` (one in import list, one in __all__)
    - `grep -c "\"SchemaValidationError\"" python/src/gemini_sdk/errors/__init__.py` returns `1` (the __all__ entry in double quotes)
    - `cd python && uv run python -c "from gemini_sdk.errors import SchemaValidationError; print(SchemaValidationError().bucket, SchemaValidationError().retryable)"` outputs `unknown False`
    - `cd python && uv run python -c "from gemini_sdk import SchemaValidationError" 2>&1 | grep -c "ImportError\|cannot import"` returns `0` (top-level re-export deferred to 08-05; this test confirms errors module re-export only)
    - `cd ts && pnpm typecheck` exits 0
    - `cd ts && node -e "import('./src/errors/index.ts').then(m => { const e = new m.SchemaValidationError('test'); console.log(e.bucket, e.retryable); })" 2>&1 | grep -q "unknown false"` — OR rely on typecheck as the primary verification
  </acceptance_criteria>

  <done>
    lint-errors.sh exits 0 with 16 classes synced. Python barrel exports SchemaValidationError. TS barrel continues to export * from errors.ts. Both languages can import and instantiate SchemaValidationError with correct metadata (bucket=unknown, retryable=false).
  </done>
</task>

</tasks>

<verification>
Overall plan verification:
1. `spec/errors.yaml` contains SchemaValidationError with `source: sdk`
2. `ts/src/errors/errors.ts` and `python/src/gemini_sdk/errors/errors.py` both contain SchemaValidationError class generated from YAML
3. `bash scripts/lint-errors.sh` exits 0 and reports 16 classes in sync
4. `cd ts && pnpm typecheck` passes
5. `cd python && uv run python -c "from gemini_sdk.errors import SchemaValidationError"` succeeds
6. Regeneration is idempotent: running `node scripts/gen-errors.mjs && (cd python && uv run python ../scripts/gen-errors.py)` twice produces no git diff on the second run
</verification>

<success_criteria>
- PAR-05 invariant preserved: single YAML source generates both language implementations
- Codegen pipeline understands `source: 'stderr' | 'sdk'` discriminator (stored as comment metadata for human + lint-errors readability; ErrorMapper logic untouched)
- SchemaValidationError is importable in both languages with bucket=unknown, retryable=false
- lint-errors.sh 3-way sync check extends to 16 classes without code changes
- Zero drift on pre-existing 15 classes (byte-identical regen except for new source: comment line per class)
</success_criteria>

<output>
After completion, create `.planning/phases/08-tools-approval-mode-structured-output-best-effort/08-01-SUMMARY.md` documenting:
- errors.yaml entry added (copy the YAML block)
- Codegen script changes (one-line additions in each)
- SchemaValidationError available at `@gemini-sdk/core` (TS) and `gemini_sdk.errors.SchemaValidationError` (Python)
- Linter output with 16-class sync confirmation
- Any drift or regeneration oddities encountered
</output>
