{
  "fixture": "error-rate-limit.ndjson",
  "captured_against": "SYNTHETIC (Phase 5 plan 01, explicitly blocked) — derived from simple-text.ndjson init event + constructed 429 error",
  "captured_at": "2026-04-12T00:01:00.000Z",
  "description": "Synthetic fixture (Phase 5 plan 01, explicitly blocked): rate-limit failure scenario. No free-tier GEMINI_API_KEY available for 429 RESOURCE_EXHAUSTED capture; host OAuth quota is generous and does not trigger 429 under rapid-fire requests. The error event shape is derived from the known gemini-cli RESOURCE_EXHAUSTED error format (HTTP 429, status RESOURCE_EXHAUSTED). Real-capture deferred to a follow-up gap-closure phase. Phase 5 convention: rate-limit errors THROW RateLimitError (no rate_limit chunk yielded).",
  "chunks": [
    {
      "type": "system",
      "subtype": "init",
      "sessionId": "<REDACTED_SESSION_ID>",
      "model": "auto-gemini-3"
    }
  ],
  "_throws": true,
  "_errorType": "RateLimitError",
  "exit_code": 1,
  "stderr_patterns": [
    "quota",
    "RESOURCE_EXHAUSTED",
    "429",
    "Too Many Requests"
  ],
  "synthetic": true,
  "synthetic_reason": "No free-tier GEMINI_API_KEY available for 429 RESOURCE_EXHAUSTED capture. Host OAuth quota is generous and does not trigger 429 under rapid-fire requests.",
  "resolution_phase": "follow-up-quota-capped-key",
  "derived_from": "simple-text.ndjson line 1 (init event) + constructed error event",
  "capture_limitation": "Phase 5 plan 01 blocked: quota-capped API key required for real capture. Tracked in follow-up gap-closure phase.",
  "retry_after_field_observed": null,
  "retry_after_note": "Unknown — real 429 response not captured. ErrorMapper must tolerate absence of retryAfter / retry_after / retryDelay / retry_delay_ms fields (asserting undefined/None) until follow-up capture lands."
}
